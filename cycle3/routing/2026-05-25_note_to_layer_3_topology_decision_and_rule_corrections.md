# To Layer 3 — Fork 2 resolved: Candidate 1 selected; two rule-menu corrections

Routing note, drafted by Layer 1 for Mike's routing. The substrate topology is decided; two corrections to the held rule menu travel with it.

## Topology selected — Candidate 1, as a deliberate canonical baseline

- **50 x 50, Moore radius 1 (8-neighbor), toroidal.**
- Chosen as a deliberate fresh decision, not inherited from Cycle 2.
- **Theory basis (on record):** Cycle 3 first needs a no-special-location substrate to validate the measurement apparatus without boundary artifacts. Edges (non-toroidal) and extended-reach coupling (larger radius) become later topology variants, not the first baseline.

Locked consequences:
- **Observable adjacency:** 8-neighbor toroidal (orthogonal + diagonal wrap). This matches the existing Cycle 2 observable encoding — adopted deliberately for minimal rebuild risk, not defaulted to.
- **Control battery:** regenerated at 50 x 50 toroidal Moore-1; scale-parametric case definitions; grid-specific permutation null; interpreted within-topology only (no cross-topology raw or z magnitude comparison).
- **Transient-wave control:** the continuous wrapping band is admissible under toroidal boundaries.

## Rule-menu corrections

The menu stays held and uncommitted. These correct its labels so the eventual rule-commitment phase inherits accurate per-observable consequences — they do not change the menu's status.

### Rule A — relabel: pre-loads neither observable; it is the both-agree baseline

Your sketch labels A (strict density thresholding) as pre-loading Psi_persistence_I. It does not. A strict lower-threshold activation rule with no upper / overcrowding bound moves the two observables TOGETHER, not apart: at low threshold it fills toward uniform (both observables low or degenerate); at high threshold it freezes into locked clusters (both observables high). Across Lambda it produces no divergence, because it has no mechanism for sustained motion of structure — it relaxes to fixed points or uniform states. So A pre-loads neither; it is the least-discriminating rule, the both-agree sanity baseline.

The discriminating options are B and C. B is correctly labeled: range-bound / Life-like action produces shifting structures, hence per-tick organization without persistence, hence it pre-loads Psi_meanI_state. The upper bound is exactly what separates B from A.

### Rule C — keep the description at the micro level; do not attribute the divergence to mu(rho) = 0

Your sketch attributes C's divergence to "the point(s) at which mu(rho) = 0." Correction by the four-level discipline: mu(rho) is a mean-field (coarse-grained) coefficient; a micro contagion rule does not contain it. The relationship between a micro rule and mu(rho) is what the mean-field analysis derives — it is not something a rule sketch asserts.

C's substance is fine stated at the micro level: probabilistic activation scaling with the local active-neighbor fraction, modulated by Lambda, producing a persistent core with continuously fluctuating boundaries — moderate on both observables, with a divergence tunable via the probability scaling and Lambda. State it at that level, not via mu(rho).

(Minor, same family: "fraction of active neighbors" is fine because it is local; keep it from drifting into the quarantined "fraction of the population.")

## Still held

Transition rules remain uncommitted. The corrected menu is design-phase input, not a commitment. Rules are committed in the design phase, reconciled with Layer 2's discrimination analysis and the substrate together. The CTL design phase — building the paired control battery at the selected 50 x 50 toroidal Moore-1 topology — opens next; expect a routing.

Execution channel remains Mike only.

Vocabulary holds: rho = activation density; Psi = coherence; Lambda = structural conduciveness (never "terrain favorability"); agents act (no decision / optimization / utility / cognitive language); "the point(s) at which mu(rho) = 0" (never rho_c); "per-cell active-state values" (never "field"); candidates produce or fail to produce (never "confirm" / "demonstrate").
