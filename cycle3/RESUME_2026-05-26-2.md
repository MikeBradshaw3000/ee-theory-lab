# RESUME - Cycle 3, post-CTL-001, documentation reconciled

**Supersedes:** RESUME_2026-05-26 (post-CTL-001, L2 cleared). That anchor was current on the gate state but predated this session's documentation reconciliation; this one folds it in. The substantive theory state is unchanged from the prior anchor - only documentation changed this session.
**Written:** end of the documentation-reconciliation session (commits b06d6ca, 1fac979 pushed). Set the date/filename to your actual session date if different. (Note the standing filename-date-runs-a-day-ahead quirk: prior anchor was filed 05-26 for an 05-25 event; it still sorts newest, harmless.)

---

## Orientation - where we left off

Cycle 3 is in its precondition-gate phase. TWO of three gates are cleared (ENV-001, CTL-001). The substrate topology is locked. The observable apparatus is validated at L2 on synthetic controls, with both candidate observables grounded against their own grid-local nulls. The next gate is C3-SS-001 (steady-state eligibility), which is NOT yet designed. The substantive probe phase opens only after SS-001.

This session did NO substantive theory work. It was a cold-start orientation plus documentation reconciliation: two canonical-record gaps found and closed, both committed and pushed. No gate state changed; no held material moved; the ontological question did not advance.

## Gates

- **C3-ENV-001 - passed-L1, closed.** Canonical environment: top-level venv, Python 3.14.4, Mesa 3.5.1, numpy pinned 2.4.4. Do NOT re-do or re-capture.
- **C3-CTL-001 - valid-L2.** The synthetic control battery (cycle3/c3_ctl_001_battery.py) at the locked topology produces the designed five-case discrimination - A high on both observables; B and C near null; D the divergence (Psi_meanI_state high, Psi_persistence_I degenerate); E the degeneracy (both near null, P_std flagging uniformity) - with BOTH observables grounded against their own grid-local permutation nulls. Record: cycle3/TEST_RECORD_C3-CTL-001.md (now filled - see "Documentation reconciled this session"). Do NOT re-run. This validates the measurement apparatus only; it establishes nothing about real-substrate values, steady-state eligibility, or Regime-II-as-structural. Interpretation withheld.
- **C3-SS-001 - NOT designed.** The last precondition before any real-substrate value can be read as Regime II. Opening it is Mike's call. Do NOT seed it unprompted.

## Documentation reconciled this session (NEW - not in prior anchor)

Two canonical-record gaps were found during cold-start grounding and closed:

1. **CTL-001 test record filled (commit b06d6ca, pushed).** The closure ops log (1c35283) recorded the CTL-001 test record's execution and post-execution blocks as "filled from Mike's canonical run," but the on-disk file still carried the empty design-phase template. A fresh Layer 1 reading it cold derives "CTL-001 not run" - the trap was hit and caught this session. The blocks are now filled from primary source only: CSV run-of-record values (cycle3/data_out/c3_ctl_001_results.csv), the closure ops log (environment, output path, session date), and the battery script (seed 42, 100 ticks, 199 permutations). Three fields neither source quotes verbatim - exact command line, exact run date - are marked in-record for Mike to confirm, not asserted. This was documentation repair, NOT a state change: the gate was already valid-L2.
2. **Population B gitignored (commit 1fac979, pushed).** The 19 superseded canonical copies in the handoff tree (Cycle 2 closure S3 / S6 follow-up 1, disposition pending) were dispositioned by Mike's call: gitignore, not delete. Added to root .gitignore as a literal 19-path enumeration (NOT pattern-matched), so no tracked file or future copy is caught; the tracked sibling claude_session_handoffs/2026-05-20-8/claude_instantiation_kit_v4.md was deliberately excluded and verified still tracked (canary check passed). git status --short is now clean (only the intended changes showed). Closes CYCLE2_CLOSURE.md S6 follow-up 1.

## Stale follow-ups to clean (carried, low priority, NOT blocking)

- **Stray reproduction-guide file is GONE.** phase_4b/reviews/layer3/# Mesa Environment Reproduction Guide.md was confirmed absent from disk this session (Test-Path False; not tracked, not ignored). The prior anchor and the closure ops log still list it as "disposition pending" - that note is stale. No action needed on the file; the record note is what needs cleaning, folded into this anchor.
- This anchor itself is the cleanup vehicle for the above; the prior anchor's VERIFY-FIRST commit-state warning is now fully discharged (CTL-001 cluster committed AND pushed; origin at 1fac979).

## Topology (locked)

Candidate 1 - 50x50, Moore radius 1 (8-neighbor), toroidal - chosen as a deliberate canonical baseline. Theory basis: Cycle 3 first needs a no-special-location substrate to validate the apparatus without boundary artifacts; edges (non-toroidal) and extended-reach coupling (larger radius) are later topology variants. The observable conforms to the substrate (8-neighbor toroidal Moran's I, parametric in grid size); the Cycle 2 encoding carries over because Candidate 1 matches it.

## Converged measurement-validity finding (Layer 1 + Layer 2, genuine convergence)

Topology and the controls/observable are paired on grid size, adjacency, and boundary condition. The observable conforms to the substrate; controls are regenerated per topology and read WITHIN-topology only (no cross-topology raw or z magnitude comparison); scale-parametric case definitions are necessary but NOT sufficient (fixed-radius adjacency makes magnitudes non-portable across scale); boundary conditions are baked into the observable. This was reached by genuine independent engagement, not soft convergence.

Note (sharpened this session from reading the battery script): the CTL-001 case generators use literal cell counts at 50x50 (Case A a 10x25 block, etc.), correct proportions for this grid and not carried from 20x20, but the script is a per-topology instrument - the constants are fixed to 50x50, not written as fractions. Regenerating controls at a different topology means editing those constants. Consistent with the within-topology reading discipline; recorded for accuracy, changes nothing.

## Held, uncommitted (do NOT treat as committed plan)

- **Transition-rule menu.** Sketched with per-observable consequences: A = the both-agree baseline (pre-loads neither - relaxes to fixed points / uniform states, no sustained motion); B pre-loads Psi_meanI_state (range-bound / Life-like, shifting structures); C = micro contagion with tunable divergence. Rules commit ONLY in the design phase, reconciled with Layer 2's discrimination analysis and the substrate together. The mapping of rules to observable consequences is L1/L2 design reasoning, not L4.
- **Probe-phase design inputs** (cycle3/PROBE_DESIGN_INPUTS_HELD.md): Layer 2's two-dimensional coherence signature (z_meanI_state, z_persistence_I), the four quadrant readings with forbidden interpretations, the mu(rho) distinction (the pair is an observable signature, not mu(rho)), and the Lambda-varying probe sequence. Off the shelf when the probe phase opens - not before.

## The ontological question (rides forward, UNSETTLED)

Which observable, if either, IS Regime-II coherence: Psi_meanI_state (per-tick organization that persists) vs Psi_persistence_I (same cells persistently clustered). Co-equal pair. This is L4 - Mike / Layer 1, settled on relational-mutual-reinforcement grounds. Never settled by statistic robustness, script naming, or substrate mechanics. The CTL-001 Case D divergence is DATA on the pair, not a tiebreaker.

## What's next

1. C3-SS-001 design - the last precondition gate. Mike's call on when to open; do NOT seed it unprompted. When it opens, expect the hybrid pattern: Layer 1 states settled acceptance criteria, Layer 3 leads the parametric/implementation work, Layer 2 reviews validity, Layer 1 reviews the implementation (sufficiency-tested) before Mike's canonical run.
2. The substantive probe phase, after SS-001, drawing on the held probe-design inputs. The probe must VARY Lambda and look for the sequence (Lambda changes -> rho lifts -> coherence signatures stay low over an earned steady-state window -> only under other conditions does coherence lift) - that sequence is what answers the "baked in" objection.

## Commit / record state - VERIFY FIRST

As of this resume's writing, origin/main is at 1fac979 and the working tree was clean (only intended changes showed during the session). Before substantive work:
- Verify HEAD and working tree (git --no-pager log -1; git status --short). Expect HEAD at 1fac979 (or later if a session intervened), working tree clean.
- The CTL-001 cluster is committed AND pushed (no longer the prior anchor's open question). The two documentation commits this session (b06d6ca record fill, 1fac979 Population B gitignore) are pushed.
- If staging anything new: stage with EXPLICIT paths, never git add -A. Population B is now gitignored so it will not reappear as untracked noise, but the explicit-path discipline still holds for any non-total commit scope.
- The cycle2-close tag stands.

## Carry-forward cautions (from this and prior sessions)

- Cold-start grounding sequence that worked this session: manifest check (use a single-pipeline one-liner, NOT the kit's powershell$f=@(...) block, which glues the markdown fence to the assignment and errors) -> read anchor first (primary orientation) -> kit -> CTL-001 record -> Cycle 2 closure. The anchor is primary; kit Section 6 current-state is a Phase 4B-era snapshot superseded by the anchor (anchor governs).
- When a canonical document's blocks look empty but an ops log says they were filled, READ THE LIVE FILE before drafting - do not trust the earlier paste. This session the empty-blocks paste was accurate and the ops log was aspirational; only the live read settled it.
- Fill canonical records from primary source only. Cycle 2 reference values (20x20) are NOT cross-scale-comparable to 50x50; never copy them in as if portable. Mark fields no source quotes verbatim rather than confabulating them.
- Encoding: canonical docs here are BOM-less UTF-8 with the GitHub-template .gitignore also BOM-less. Verify the ORIGINAL on-disk file's first bytes before overwriting (Resolve-Path then ReadAllBytes; .NET resolves relative paths against the process cwd, not the PS location, so Resolve-Path is required). Match the original's BOM state. Browser downloads of dotfiles drop the leading dot (.gitignore -> gitignore); Move-Item renames on placement.
- PowerShell: one command per copy-pane. Select-String emits MatchInfo objects, not strings - do not pipe them into Set-Clipboard (it treats them as paths and errors).
- Your own sandbox runs are predictions, never results of record. Mike's run on the captured venv is the record (numpy pinned 2.4.4, so they tend to match exactly - treat the match as a cross-check, not a pre-confirmation).
- When the next step is clear, produce it; do not stall on "want me to X or hold?"
- After a rest, re-anchor against the conversation, not the document - but the conversation does not survive the rest, so THIS document is what the next instance gets. That is why it was refreshed at session end.

Vocabulary (binding, including at wrap-ups): rho = activation density; Psi = coherence; Lambda = structural conduciveness (never "terrain favorability"); agents act (no decision/optimization/utility/cognitive language); "the point(s) at which mu(rho)=0" (never rho_c); "per-cell active-state values" (never "field" as explanatory mechanism - "field" is permitted ONLY as a genuine technical field-theoretic/continuum term in ABM/MFA work, never in architectural or manuscript prose, per Mike's session-19 arbitration); candidates produce or fail to produce (never "confirm"/"demonstrate"); "fraction of active neighbors" is fine (local) but never "fraction of the population."
