# Resume anchor — Layer 1 session close (2026-05-25)

Read after the instantiation kit and `cycle2/CYCLE2_CLOSURE.md`. This is the "where we left off" note for the next Layer 1. The repo is the continuity; this note just orients you in it. Vocabulary discipline binds (ρ/Ψ/Λ; agents *act*; the point(s) at which μ(ρ)=0, never ρ_c; candidates *produce*, never *confirm*).

## What this session did

Closed Cycle 2 deliberately and opened Cycle 3's documentation. All load-bearing work is committed and pushed to `origin/main`. Nothing is half-done in git.

**Commits this session (newest first):**
- `3a40a26` — Cycle 3 seed: registry precondition tests + Layer 2/3 routing notes
- `9b7a95a` — Cycle 2 closure (3/3): closure record + handoffs marker  ← **tag `cycle2-close` here**
- `0b2b171` — Cycle 2 closure (2/3): phase_4b psi_state_spatial v2 + smoke/patch outputs
- `9172659` — Cycle 2 closure (1/3): session handoff logs + manuscript docx (duplicate copies excluded)
- `3e184d7` — Cycle 3 documentation skeleton

**Why Cycle 2 got a deliberate close:** Cycle 1 never had one — its prior-cycle material was *discovered* mid-Cycle-2 at session 9 and reconciled retrospectively under item 9 (which DID close cleanly, session 10). Cycle 2 was closed proactively by the same item-9 triage method, with the `cycle2-close` tag as the boundary marker Cycle 1 lacked. The apparatus's habit of catching its own unclean boundaries is a recorded strength.

## Repo structure now

- `cycle3/` — `CYCLE3_OVERVIEW.md`, `MESA_ENVIRONMENT_REPRODUCTION.md`, `CYCLE3_TEST_REGISTRY.md` (seeded), `TEST_RECORD_TEMPLATE.md`, and `routing/` (the two layer notes).
- `cycle2/CYCLE2_CLOSURE.md` — the triage record. Read it for the full Cycle 2 disposition.
- `claude_session_handoffs/README.md` — the snapshot marker (dated copies are not canonical).
- Intended end-state: top-level `cycle1/` `cycle2/` `cycle3/`, one per cycle. `cycle1/` not yet created.

## Registry state — what's seeded, what's NOT

Three **precondition gates** seeded (these follow from Cycle 2 lessons, not design choices):
- `C3-ENV-001` — environment reproduction + Mesa smoke (L1 gate).
- `C3-CTL-001` — synthetic control battery (L2 gate; carries forward the Cycle 2 five-case structure).
- `C3-SS-001` — steady-state eligibility apparatus (L3 gate).

**Deliberately NOT seeded:** the `C3-OBS-*` and `C3-INT-*` substantive Regime-II probes. These are the open design question, handed to the multi-AI design phase. Do not seed them from Layer 1 framing — substrate-led design produced cleaner probe structure in Flight 2. Let engagement settle them.

## The open ontological question (rides forward, do not let it be settled by default)

Which observable, if either, IS Regime-II coherence — `Psi_meanI_state` (per-tick state organization that persists) or `Psi_persistence_I` (same cells persistently clustered). Turns on coherence-as-relational-mutual-reinforcement (the baked-in hinge), NOT statistic robustness, NOT script naming. The two are a co-equal pair. The transient-wave case is where they diverge by design; that divergence is data.

## Immediate next moves (in rough priority)

1. **Mike is routing the two layer notes** to ChatGPT (Layer 2) and Gemini (Layer 3), and to NotebookLM for cross-session continuity. This is the live thread — the next substantive work is the layers' engagement on substrate/probe design.
2. **Population B disposition** (closure follow-up #1): 19 untracked duplicate kit/script copies remain on disk. `git status` shows them until Mike decides — gitignore (keep on disk, out of git) or delete (canonical originals are tracked). Not started.
3. **Manuscript canonical home** (follow-up #2): `EE_emergence_overview_v1_71.docx` is committed inside `claude_session_handoffs/2026-05-24/` and is past the v1.5 the foundational set records. Needs a real home.
4. **First real test execution** when the layers return: `C3-ENV-001` — Mike fills the `« FILL »` blocks in `MESA_ENVIRONMENT_REPRODUCTION.md` from the known-good machine (the live `venv`, not `venv_cycle1_archive`).

## Standing for the next instance

- Execution channel: Mike only.
- Don't reopen the B2-source hunt or Flight 6 parquet forensics (closed Cycle 2).
- Don't build Cycle 3 on the Flight 6 parquet or `Psi_local` — those are closed artifacts. Substrate is built fresh around the baked-in defense.
- Framing-asymmetry watch holds: Layer 1 frames, others engage. Read convergence with that in mind; don't manufacture soft convergence by over-framing the probe design.
- Don't trust memory over the committed record. If this note and the repo disagree, the repo wins.
