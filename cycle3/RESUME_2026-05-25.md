# Resume anchor — Layer 1 session close (2026-05-25, rev 2)

Read after the instantiation kit and `cycle2/CYCLE2_CLOSURE.md`. This is the "where we left off" note for the next Layer 1. The repo is the continuity; this note just orients you in it. Supersedes the earlier 2026-05-25 anchor (which froze before ENV-001 completed). Vocabulary discipline binds (rho/Psi/Lambda; agents *act*; the point(s) at which mu(rho)=0, never rho_c; candidates *produce*, never *confirm*).

## State as of this close

Cycle 2 closed and tagged. Cycle 3 opened, documented, and its first precondition gate (ENV-001) fully cleared. Everything committed and pushed to `origin/main`. Nothing half-done in git.

**Commit chain (newest first):**
- `11489d7` — ENV-001: reconcile reproduction guide to captured environment
- `c7d75eb` — ENV-001: record Mesa smoke test pass
- `4668fea` — ENV-001: add Mesa environment smoke test
- `957574d` — Cycle 3 environment capture: known-good venv snapshot + lock file
- `e3bb8aa` — Cycle 3 resume anchor (rev 1, now superseded by this file)
- `3a40a26` — Cycle 3 seed docs / registry / routing notes
- `9b7a95a` — Cycle 2 closure (3/3)  ← **tag `cycle2-close`**

## ENV-001 — fully closed (do not re-do)

`C3-ENV-001` is `passed-L1` and complete at every level it can reach. Done and committed:
- Smoke test `cycle3/mesa_smoke_test.py` ran under the captured venv and printed `SMOKE TEST OK`.
- Environment captured: `cycle3/ENVIRONMENT_SNAPSHOT.md` + `cycle3/requirements.lock.txt`.
- Registry updated: `C3-ENV-001` status `passed-L1`.
- Record written: `cycle3/TEST_RECORD_C3-ENV-001.md`.
- Reproduction guide reconciled to reality (commit `11489d7`).

**Canonical Cycle 3 environment (Mike's arbitration):** the existing top-level `venv` at `C:\Users\vkz244\EE_Theory_Lab\venv\`, Python **3.14.4**, Mesa **3.5.1**. NOT `.venv` (that was an early placeholder name; see guide Sec. 0a). NOT `venv_cycle1_archive`. Do not rebuild or re-capture absent a dependency change.

## The two live forks (next real work)

**Fork 1 — C3-CTL-001, the synthetic control battery (L2 gate).** The next precondition test. Carries forward the Cycle 2 five-case structure (stable cluster / random low-rho / random high-rho / transient wave / uniform-high-persistence degeneracy), uses NO substrate parquet by construction, permutation-null z as the degeneracy separator. This is real design work with Layer 3 (implementation) and Layer 2 (validity), not file-shuffling. It is the gate every substantive observable test runs behind.

**Fork 2 — substrate sequencing (DECIDED 2026-05-25; in flight to Layer 3).** Mike authorized: Layer 3 may draft **topology now** (lattice, neighborhood, boundary conditions — upstream of the observable question), but **transition rules are held for the design phase**. Rules are where the substrate could quietly privilege one Psi candidate and defeat the co-equal-pair guard; they get committed only with Layer 2's discrimination analysis in hand. Layer 3 may *sketch* a rule menu (with each rule's expected consequence per observable made explicit) but not *commit* one. The reply note authorizing this is `cycle3/routing/2026-05-25_note_to_layer_3_env_and_sequencing.md` (commit it if not already).

## The open ontological question (rides forward; do not let it be settled by default)

Which observable, if either, IS Regime-II coherence — `Psi_meanI_state` (per-tick state organization that persists) or `Psi_persistence_I` (same cells persistently clustered). Settled on coherence-as-relational-mutual-reinforcement (the baked-in hinge), NOT statistic robustness, NOT script naming, NOT substrate mechanics. Co-equal pair. The transient-wave case is where they diverge by design; that divergence is data. Layer 2 holds the analytical question: what structure lets a probe *discriminate* between the two rather than merely be consistent with one.

## Registry state

Three precondition gates seeded. `C3-ENV-001` = passed-L1 (closed). `C3-CTL-001` and `C3-SS-001` = planned (Fork 1 opens CTL). The substantive `C3-OBS-*` / `C3-INT-*` Regime-II probes are deliberately NOT seeded — they are the design-phase output, not Layer 1 framing.

## Closure follow-ups (named, not started)

1. **Population B disposition:** 19 untracked duplicate kit/script copies remain on disk; `git status` shows them until Mike decides — gitignore or delete (canonical originals are tracked).
2. **Manuscript canonical home:** `EE_emergence_overview_v1_71.docx` is committed inside `claude_session_handoffs/2026-05-24/`, past the v1.5 the foundational set records; needs a real home.
3. **`cycle1/` folder:** not yet created (would mean relocating historical artifacts; separate deliberate act).

## Standing for the next instance

- Execution channel: Mike only.
- Don't reopen B2-source hunt or Flight 6 parquet forensics (closed Cycle 2).
- Don't build Cycle 3 on the Flight 6 parquet or `Psi_local` — closed artifacts. Substrate built fresh around the baked-in defense.
- Framing-asymmetry watch: Layer 1 frames, others engage. Don't manufacture soft convergence by over-framing probe/rule design. The topology+rule-menu / Layer-2-analysis reconciliation is a soft-convergence risk point.
- Don't trust memory over the committed record. If this note and the repo disagree, the repo wins.
