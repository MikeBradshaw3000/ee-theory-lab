# RESUME anchor — EE-emergence theory project (Layer 1 grounding)

**Grounding-anchor filename note:** This file keeps the `RESUME_2026-05-30.md` name deliberately. It is a grounding anchor, not a date stamp. The actual refresh recorded below is **2026-06-02**, superseding the 2026-05-30 and prior wave-two-design-open content. On any discrepancy between this anchor and a committed findings file or the design contract, the committed file wins.

**Supersession header:** Refreshed 2026-06-02 at HEAD 95ee9a8. The previous content of this file (2026-05-30, wave-two design phase open, nothing seeded) is SUPERSEDED. Between that state and this refresh, wave two was seeded and all three instruments were run, read, and committed. An older instantiation prompt expecting HEAD ead9c08 and describing wave two as "design phase open, nothing seeded" is STALE; that same prompt also mislabels ead9c08 as the contract-placement commit — the contract froze at a925475. The repo wins both points.

---

## Who you are / how this works

Layer 1 (Claude): architectural guardian and vocabulary enforcer, routing-note author, cold-start grounding from this anchor + the contract + findings files before any session work. Layer 2 (ChatGPT): mean-field substantive review. Layer 3 (Gemini): Mesa implementation; never declares its own L2 clearance. Mike: sole execution channel and arbiter of all commits; routes notes between layers. You draft and route; Mike runs. You never execute locally.

REPO: github.com/MikeBradshaw3000/ee-theory-lab (local: C:\Users\vkz244\EE_Theory_Lab\ee-theory-lab). Canonical venv: C:\Users\vkz244\EE_Theory_Lab\venv (workspace top-level, NOT repo root; a venv_cycle1_archive sits next to it — do NOT activate it).

---

## WHERE THINGS STAND (read first)

HEAD 95ee9a8, origin current, tree clean (untracked read_obs001_nearnull_scale.py at repo root is a known-harmless pure-read CSV tool; retain or remove at Mike's discretion).

**Wave-one (C3-OBS-001) synthesis cluster: COMPLETE and committed.** Do NOT redo it. Carry the SIX refinements + TWO precision notes (below) verbatim — never the five-amendment compression.

**Wave-two design phase: OPEN. Seeded. All three instruments run, read, committed.** Seeding of any FURTHER probe remains Mike's call to open.

Wave-two committed artifacts (oldest first):
- a925475 — Rule C DESIGN_CONTRACT.md placed (authoritative wave-two design document; frozen here).
- ff7a904 — pre-seeding resolution memo (PRESEEDING_RESOLUTION.md): resolves Comparator 0 observation protocol + kappa-grid construction rule; seeds nothing.
- 8abb917 — Comparator 0 script + results (apparatus-null floor).
- d150de7 — Rule C M2 first-pass map + findings (RULE_C_M2_FIRST_PASS_FINDINGS.md; floor-adjacent low/low, A-prime).
- 208b5c2 — Comparator epsilon weak-neighbor floor audit + findings (COMPARATOR_EPSILON_FINDINGS.md).
- plus per-session ops logs (preseeding-resolution, comparator0-run, rule-c-m2-run, comparator-epsilon-run); 95ee9a8 is the epsilon ops-log commit.

The contract (cycle3/wave_two/DESIGN_CONTRACT.md, frozen at a925475) governs wave-two design. This anchor summarizes; the contract and the findings files govern on any discrepancy.

---

## WAVE-TWO RESULT STATE (the three instruments, run and read)

**Comparator 0 (Lambda-only apparatus-null floor).** Calibrated on rho-lift and non-degeneracy ALONE; the two observables recorded as OUTPUTS, never tuning targets (the older "choose magnitude so meanI_z stays inside +/-2" wording is rejected and stays rejected). Result: i.i.d. Lambda-only floor reads near-null. This is the item-1 trivial-stochastic reference.

**Rule C M2 first-pass (central instrument).** Form fixed (contract Section 2.1): p_become = sigma(logit(p_Lambda) + kappa*(2q-1)), g(q)=2q-1 fixed centered, coefficient-null separable (kappa=0 IS Lambda-only, confirmed empirically). Anchors Lambda=0.20 and 0.40; common realized-contrast grid c in {0, +/-0.05, +/-0.10, +/-0.20, +/-0.35, +/-0.50, +/-0.60, +/-0.80}; seeds 42/137/256/1024/31415; wave-one window structure; 450 rows; 150 state NPZs; ran at parity.
- **Reach satisfied (precondition met)** at BOTH anchors: negative kappa -> positive meanI / NEGATIVE persistence; positive kappa -> positive meanI / POSITIVE persistence; degenerate endpoint reached (0.20 c=+0.8 fires extinction_degenerate). Because reach is met, any low/low the map emits is NOT rule-forced-by-default.
- **LowLow location:** fires only at kappa=0 and the small flanking settings (|c| <= 0.10), centered on zero, monotone in |kappa|, confined to the smallest contrast settings, density-stable across both anchors.
- **Classification (A-prime, Layer 2-arbitrated):** floor-adjacent weak-coupling low/low band, NOT an away-from-floor substantive low/low candidate. The literal four-way bracketing reading (B) was explicitly rejected as necessary-but-not-sufficient: bracketing protects against a rule whose only behavior is low/low, but does not distinguish a null neighborhood around the OFF-state from a mechanism-produced near-null regime.
- **The discriminating test (correctly applied, not inferred):** NOT nonzero low/low width near zero (any continuous stochastic update has width under a hard LOW_Z_THRESH=2.0). The test is whether LowLow persists OUTSIDE the weak-coupling zone at responsive contrast (|realized_delta_p| >= 0.20). It does not — LowLow fires only through |c| <= 0.10 — so it reads floor-adjacent.
- **What would make a substantive reading live (carried for future passes):** a near-null pocket AWAY from zero; a near-null plateau past |c| >= 0.20; a non-monotone return to near-null AFTER signed structure; or density-specific structure one anchor shows and the other does not. The current map shows NONE of these. Rule C is NOT closed as a mechanism (first pass).
- **Map-texture cautions (downstream, not part of the central call):** at Lambda=0.20 high positive coupling gives positive meanI while rho collapses toward extinction — fence as degenerate-endpoint/boundary contrast, not a clean eligible signed regime; persistence-sign at matched positive coupling not identical across anchors in places.

**Comparator epsilon (weak-neighbor floor audit).** Separate-process object (a) per contract Section 6.2 — NOT a Rule C near-zero densification. Layer 2 confirmed object (a) and corrected A-prime's compressed "epsilon bounds the weak-coupling floor zone" wording to mean the separate-process floor audit. Additive form p_become = clip(p_Lambda + a*g(q), 0, 1), deliberately distinct from Rule C's logit-linear coupling; survival Lambda-only (neighbor-independent — fence holds); no LowLow flag (floor, not candidate). ceiling form |delta_p| <= epsilon*pbar_Lambda fixed before any z-score; epsilon=0.05 (Mike-ratified), choice (a) honor-the-contract-form (per-anchor unequal realized contrast). Per-anchor realized-contrast ceilings 0.0100 (Lambda=0.20) and 0.0200 (Lambda=0.40); additive coefficients a=0.00500/0.01000 hit the ceiling exactly, no clipping. 20 rows, 10 NPZs, parity passed.
- Result: floor stable to weak coupling — meanI-z and persistence-z centered near zero at both anchors, no systematic sign drift. Two isolated marginal exceedances over 2.0 (meanI Lambda=0.40 seed137 win75 at +2.36; persistence Lambda=0.20 seed31415 win0 at +2.09) — both non-replicated, no signed partner, finite-sample tail, inspect-not-halt.
- Binding two-ceilings constraint held: epsilon ceiling (separate-process perturbation scale) != Rule C near-zero boundary (Rule C realized-contrast axis); epsilon calibrates the LANGUAGE "floor-adjacent" only — it does NOT reclassify Rule C rows or move Rule C thresholds.
- Object (b) (Rule C near-zero densification) NOT motivated by this result (floor stable, not sensitive); survives as a separate optional follow-up at Mike's call, never called epsilon.

**Two-direction confirmation — stated precisely (carry this wording).** Comparator 0 establishes the lifted i.i.d. near-null floor. Comparator epsilon establishes that this floor remains near-null under a separate-process weak perturbation below the smallest nonzero Rule C contrast setting. This supports the phrase "floor-adjacent weak-coupling tolerance," but does NOT independently audit the Rule C |c| = 0.05 / 0.10 rows; those remain classified from the internal Rule C M2 geometry. (The "confirmed from two independent directions" shorthand is acceptable ONLY for "the floor itself is not assumed"; it overreaches if read as epsilon validating the Rule C near-zero band.) The epsilon ceiling (0.0100 / 0.0200) sits structurally BELOW the smallest Rule C contrast setting (|c| = 0.05). So epsilon confirms the floor at SUB-0.05 perturbation; it does NOT independently confirm the |c| = 0.05 / 0.10 Rule C rows. M2 Section 6 floated that epsilon, if it found |c|=0.05 above the weak-coupling ceiling, could make the 0.05/0.10 rows "more interesting" — but epsilon's contract-fixed ceiling was below 0.05, so that particular discrimination was never reachable by this epsilon. This does NOT break the A-prime classification: M2's floor-adjacent call rests on the |c| <= 0.10 band being centered-on-zero and monotone, not on epsilon. Read the "confirmed from two independent directions" language as confirmation at sub-0.05 perturbation, not at the Rule C 0.05/0.10 rows.

**Net wave-two reading:** the near-null/near-null region is the apparatus floor and its weak-coupling neighborhood; the Rule C near-zero band sits within it; Rule C reaches both signed regimes and a degenerate boundary. No substantive away-from-floor low/low in first pass. Mechanism not closed.

---

## THE SIX REFINEMENTS + TWO PRECISION NOTES (carry verbatim; never compress to five)

1. Rule B [1,2] correction: boundary / near-steady, Steady_State_Candidate True on 1/25 windows (seed 42 only), NOT earned-steady on 5/5.
2. Signed three-level framework: positive structured / near-null / negative anti-structured per axis, replacing the 2x2. Load-bearing move: distinguishing near-null from negative-anti-structured.
3. Rule A endpoint amendment: saturation tau_A {1,2,3}, collapse {4..8}; no locked-cluster regime under wave-one conditions; scope-limited.
4. LowLow_Nondegenerate_Candidate False on all 300 rows. Apparatus-level negative finding. Do NOT loosen LOW_Z_THRESH (= 2.0).
5. L4 boundary preserved: apparatus-level only.
6. [2,4] earned-but-heterogeneous: 5/5 seed-level occurrence, mixed window-count robustness (multi-window on 137/256/31415, single on 42/1024). NOT grouped with [2,3]. Reading lens: seed-level occurrence vs window-count robustness (window-count load-bearing).

Precision notes:
- Rule A dual-degeneracy: tau_A {4..8}, saturation_degenerate AND extinction_degenerate fire on the same rows; changes no finding. (Re-confirmed 2026-05-31: 125 both-flag rows in the CSV.)
- [3,4] non-eligible contrast: positive meanI, strongly POSITIVE persistence (~+86 z); persistence axis REVERSES sign under the unstable band; fenced as non-eligible / instability-coupled, NOT a signature. Eligibility gates signature-status, not measurement-validity.

---

## GATES — all cleared, do NOT reopen

- C3-ENV-001 — passed-L1. Canonical env: Python 3.14.4, Mesa 3.5.1, numpy 2.4.4. Use & with absolute path to Activate.ps1; verify $env:VIRTUAL_ENV before trusting.
- C3-CTL-001 — valid-L2.
- C3-SS-001 — valid-L2.
- C3-OBS-001 — valid-L2, apparatus-level closure, synthesis cluster fully committed.

Topology LOCKED: 50x50, Moore radius 1 (8-neighbor), toroidal.
Co-equal pair (Psi_meanI_state, Psi_persistence_I) holds; neither named theoretical Psi.
The L4 ontological question rides forward UNSETTLED: which observable, if either, IS Regime-II coherence. Held for Mike / Layer 1. NOT to be resolved by statistical robustness or by which observable a script names.

---

## CONTRACT CORE (pointer — contract at a925475 governs)

- Load-bearing question: NOT "which rule produces low/low," but whether a lifted, non-degenerate activation-density regime can be produced WITHOUT per-tick spatial organization or clustered persistence being structurally forced by the rule — is low/low a genuine earned regime in a mechanism family ALSO capable of producing non-low/low signed regimes.
- Rule C: signed coupling kappa on the BECOMING-ACTIVE transition (NOT survival). Form fixed (above). Positive kappa contagion-dominant, negative divergence-dominant. Bounded-linear form held as RESERVED AUDIT only.
- Construction fences (binding): kappa-sign-through-zero is a SUBSTRATE parameter, NEVER an operationalization of the point(s) at which mu(rho)=0. No kappa-on-global-rho, no dynamic centering against rho, no hidden feedback in the Lambda baseline, no piecewise sign-specific form. Survival must NOT acquire neighbor-count/range dependence (or Rule C collapses toward Rule B).
- Contrastive success criterion (pessimistic-on-passing): low/low earned only if the design discriminates trivial-stochastic / rule-forced / degenerate / substantive-candidate, AND a substantive low/low is BRACKETED by non-low/low signed regimes; the sweep must demonstrably REACH the signed regimes or any low/low is rule-forced. Large-|kappa| is a degeneracy probe, NOT the evidentiary center. (Applied in M2: reach demonstrably met; low/low only floor-adjacent.)
- Comparator split: Comparator 0 (Lambda-only floor, calibrated on rho-lift alone, near-null an OUTPUT) + Comparator epsilon (weak-neighbor audit, magnitude bounded by predeclared realized-contrast ceiling). Both separate from Rule C. (Both now run.)
- WEAK-FORM Rule B prior (PRESERVE — do not harden): Rule B structural overproduction of positive meanI when lifted is the best WORKING PRIOR, not proven by construction. Wave-one CSV read is consistent with it but does not convert it to proof. Door to Rule B stays open. UNTOUCHED by wave-two results.
- Wave-one CSV finding feeding design (primary source): of 97 non-degenerate-lifted rows, Psi_meanI_state_z positive on ALL 97 (min ~139), zero near-null, zero negative — wave one has NO lifted-near-null meanI region. This SHARPENS the diagnostic target; it does NOT predict whether Rule C can reach it. Hardening the absence into a Rule-C prediction would collapse the mechanism-class distinction wave two tests.

---

## ON THE HORIZON (named, not blocking; none motivated by current results)

- Seeding of any further wave-two probe: Mike's call to open. Nothing seeded beyond the three instruments.
- Held / optional follow-ups (Mike's call): Rule C near-zero densification (object b) — held optional Rule C near-zero boundary-refinement; NOT Comparator epsilon; NOT triggered by the epsilon result; NOT necessary for A-prime (A-prime rests on the coarse M2 geometry: LowLow band centered on kappa=0, monotone in |kappa|, density-stable across anchors, disappears before the first responsive setting |c|>=0.20). Has a real use only if the deliverable changes from "classify the first-pass map" to "locate the exact Rule C transition boundary between the floor-adjacent band and signed structure, especially |c|=0.10 to |c|=0.20." Guardrail: a higher-ceiling comparator perturbation ladder is NOT object (b) — it would be a broader comparator-floor sensitivity audit; only Rule C densification on Rule C's own realized-contrast axis can locate the Rule C near-zero boundary. Reserved audits Lambda=0.30 / 0.50; grid densification (+/-0.025 / +/-0.92).
- Layer 2 epsilon update: CLOSED (2026-06-02). Layer 2 confirmed the restricted two-direction reading and pushed back on "object b unmotivated" -> "not triggered, legitimate optional boundary-refinement"; both incorporated above. No A-prime change; no seeding.
- L4 ontological question rides forward unsettled.
- Ops-log backfill: no ops_log entry for the 2026-05-28 wave-one session or the 2026-05-30 synthesis session; draftable from ABM 28 / ABM 29 scrollback Mike holds if wanted.
- Layer 3 v1 code gap: OBS001_10 carries a flagged not-recoverable gap (UUID 46246e1e-...).
- Untracked read_obs001_nearnull_scale.py at repo root: harmless pure-read CSV tool; retain or remove at Mike's discretion.

---

## HOW MIKE WORKS (binding)

- Execution channel is Mike only. You draft and route; he runs.
- PowerShell: ONE command per copy-pane, wait for output before the next. Do NOT batch state-changing commands. Pipe ALL commands through Set-Clipboard (including silent ones like Move-Item / git add); structured objects through Out-String | Set-Clipboard; empty clipboard IS the confirmation for no-output commands. The if/else | Set-Clipboard scope bug: pipe $msg AFTER the if/else, not inside a branch.
- If the PS prompt prefix gets copied and breaks the parser, re-send the command alone. If a command pastes to chat instead of running (echoed back, not its output), re-send it.
- git push prints progress to stderr, rendered RED even on success — the "<old>..<new>  main -> main" line is the success confirmation; do not read red as failure.
- Keep commit messages SHORT (single -m line) — long here-strings hit PS paste limit and hang at >>.
- File edits to canonical docs: deliver finished files via create -> present_files -> Mike Copy-Item/Move-Item (full-file overwrite, -Force for existing). Match the ORIGINAL on-disk file's BOM state and trailing-whitespace state (per-file, not project-wide).
- VERIFY BYTE-STATE AT DESTINATION (BOM + ending + SIZE) after every placement. BOM check MUST pair with SIZE check — Test-Path True does NOT confirm the right file landed; only size discriminates a stale/wrong-source copy. Expect "(1)"/"(2)"/"(3)" suffixes in Downloads when same-named files already sit there; list Downloads with size+timestamp and copy from the correct source by SIZE, not bare name.
- Per-file BOM/trailing conventions: test record / held-inputs / anchor / ops-log / routing-archive / wave_two contract + findings = BOM-less, single trailing LF. Registry = BOM present, trailing blank line. Verify against the on-disk file before generating any overwrite.
- Relative paths: PS location for Test-Path, but process cwd for Python/.NET I/O. cd into repo root (or Resolve-Path to absolute) before any Python/.NET I/O.
- Stage with EXPLICIT paths, never git add -A. Verify git diff --cached --name-only before EVERY commit.
- Notes to other layers delivered as separate copy-pastable files, one per destination, no surrounding commentary inside the file. Fresh Layer 2 context needs SELF-CONTAINED notes (build observables, null convention, apparatus from scratch; genuine question first; invite Layer 2 to flag conflicts). Continuing Layer 2 context does not need the apparatus rebuilt. Carrying the contract to a layer: concatenate note + full text into ONE file (no "[paste here]" placeholder).
- Layer 3 must never declare L2 clearance in its own voice. Frame Layer 3 deliverables as "what the run will produce / has produced."
- Per-seed flag aggregation, not per-band means. Confirming mean_rho is NOT confirming a flag pattern.
- CSV is primary source over the anchor's restatement. Fill canonical records from primary source.
- Honest pushback expected, including when Mike pre-concedes fault or defers authority. Pessimistic-on-passing applies to your own scripts and session-state reads, not just probes. Watch the symmetric risk: do not converge by deferring to a clean Layer-2 argument any more than by your own framing — sandbox/analytic agreement is a prediction, not a confirmation.
- When the next step is clear, PRODUCE it — do not stall on "want me to X or hold?". Ask only when a real fork needs Mike's arbitration.
- Surface scope-steps as FORKS, do not fold them in silently.
- Synthesis is the work; convergence is a phenomenon that sometimes occurs. Watch soft convergence and the framing asymmetry (Claude frames, the other layers engage).
- Session-end commit of the operations log is a standing rule (operations_log/YYYY-MM-DD_slug.md; BOM-less, single trailing LF; H1 + bold metadata block + sections + closing "Drafting partner" line). If a session commits canonical files, the ops log commits alongside, NOT deferred.

---

## VOCABULARY (binding, including at wrap-ups and celebratory moments)

- rho = activation density
- Psi_meanI_state, Psi_persistence_I = the two co-equal observables; neither named theoretical Psi
- Lambda (or Lambda_A, Lambda_B) = structural conduciveness (never "terrain favorability")
- agents ACT (no decision / optimization / utility / cognitive language)
- "the point(s) at which mu(rho) = 0" (never rho_c); the Rule C kappa = 0 sweep point is a substrate parameter, NEVER "the point(s) at which mu(rho) = 0"
- "per-cell active-state values" (never "field" as explanatory mechanism)
- "earned steady-state window" — the SS-001-validated criterion
- LowLow_Nondegenerate_Candidate — apparatus-level flag; NEVER Regime_II in any identifier, comment, column, or prose
- candidates PRODUCE or FAIL TO PRODUCE (never "confirm" / "demonstrate" / "prove" / "validate")
- "fraction of active neighbors" is fine (local); never "fraction of the population"
- The two SS-001 flags Steady_State_Candidate and Lifted_Activation_Candidate remain INDEPENDENT at every stage
- The Rule B earned-window signature is POSITIVE meanI / NEGATIVE persistence. NOT "high meanI / low persistence."
- Reading lens: seed-level occurrence vs window-count robustness — window-count is the load-bearing measure of band robustness.

---

## COLD-START GROUNDING SEQUENCE (next instance)

Ground from this anchor + the contract + the findings files, NOT from any instantiation prompt's where-things-stand summary (prompts go stale; the repo wins). One command per copy-pane, Set-Clipboard everything.
1. git --no-pager log --oneline -1 — expect 95ee9a8 or later. If later, a session intervened; read the newest committed ops log / findings before trusting this anchor's result state.
2. status + presence checks (contract present; wave-one substrate counts 17 archive / 60 NPZ).
3. Read this anchor.
4. Read cycle3/wave_two/DESIGN_CONTRACT.md (governs design).
5. Read the three findings files: RULE_C_M2_FIRST_PASS_FINDINGS.md, COMPARATOR_EPSILON_FINDINGS.md, PRESEEDING_RESOLUTION.md (the result state above is a summary of these; they govern).
6. Confirm grounding, then WAIT for Mike's direction.
