# RESUME - Cycle 3, wave-one synthesis cluster COMPLETE (all four items committed)

**Supersedes:** the prior `RESUME_2026-05-30.md` (synthesis items 1-3 committed; routing archive deferred). This refresh records that the routing archive (item 4) is now built, committed, and pushed - the wave-one synthesis cluster is complete on all four items. Same session-date; this is the later state and absorbs the prior anchor's body in full. The six refinements + two precision notes carry forward verbatim (NOT the anchor-28 five-amendment compression).

**Written:** end of the 2026-05-30 routing-archive session.

---

## Orientation - where we left off

The wave-one (C3-OBS-001, Rules A + B, locked 50x50 Moore-1 toroidal) synthesis cluster is **complete**. All four items are on disk, committed, and pushed:

1. Test record (`TEST_RECORD_C3-OBS-001.md`) - committed dd06f38.
2. Registry advance (C3-OBS-001 to valid-L2) - committed dd06f38.
3. Held-inputs Section 7 amendment (signed three-level framework) - committed 71e51b9.
4. **Routing archive (`cycle3/obs_001_routing/`, 16 artifacts + README) - committed d76880b.** Built this session from VERBATIM 2026-05-28 scrollback (prior Layer 1 instances reproduced the artifact bodies as files; Mike routed them in; Layer 1 wrapped, ASCII-normalized, byte-verified, placed). One body - Layer 3 v1 code (OBS001_10) - was not recoverable verbatim and is flagged as a documented gap, not reconstructed.

The anchor itself was refreshed this session (this file). The session's ops-log entry is committed at 10375ee.

There is no remaining synthesis work. The next decision is wave two (separate phase, not started, not seeded).

## Commits (most recent first)

- **10375ee** - ops log for the 2026-05-30 routing-archive session.
- **d76880b** - OBS-001 wave-one routing archive (16 artifacts + README), 17 files, all create-mode.
- **c31918b** - prior anchor refresh (synthesis items 1-3 committed; routing archive deferred) - now superseded by this file.
- **71e51b9** - held-inputs wave-one amendment (signed three-level framework; positive meanI / negative persistence).
- **dd06f38** - C3-OBS-001 wave-one A+B substrate (script + CSV + 60 NPZs), test record (new), registry advance to valid-L2.

HEAD at **10375ee** when this anchor was written (will be later once this anchor refresh commits). origin/main current; working tree clean. The next instance's first action is `git --no-pager log --oneline -1` to confirm HEAD; HEAD will already include this anchor's commit.

## Gates - unchanged

- **C3-ENV-001 - passed-L1.** Canonical env: Python 3.14.4, Mesa 3.5.1, numpy 2.4.4. Venv at `C:\Users\vkz244\EE_Theory_Lab\venv` (workspace top-level, NOT repo root).
- **C3-CTL-001 - valid-L2.** Unchanged.
- **C3-SS-001 - valid-L2.** Unchanged.
- **C3-OBS-001 - valid-L2 (registry), apparatus-level closure.** Synthesis cluster now fully committed including the routing archive. Highest level cleared in the record is apparatus-level substrate observation; the registry carries the ladder term valid-L2 by convention (registry is a fixed-vocabulary status board; the record holds the nuance).

## The six refinements + two precision notes (canonical record; carried verbatim)

The 2026-05-28 anchor compressed the Layer 2 verdict to five amendments. The committed record carries those five PLUS the [2,4] correction, PLUS two Layer-1 precision notes. Enumerated so the next instance does not regress to the five-amendment compression:

1. **Rule B [1,2] correction.** Boundary / near-steady: Steady_State_Candidate True on 1/25 windows (seed 42 only), NOT earned-steady on 5/5 as Layer 3 first stated.
2. **Signed three-level framework.** Held-inputs Section 7: positive structured / near-null / negative anti-structured per axis, replacing the 2x2 high/low. The load-bearing move is distinguishing near-null from negative-anti-structured.
3. **Rule A endpoint amendment.** Saturation at tau_A in {1,2,3}, collapse at {4..8}; no locked-cluster regime under wave-one conditions; scope-limited.
4. **LowLow_Nondegenerate_Candidate False on all 300 rows.** Apparatus-level negative finding. Do NOT loosen LOW_Z_THRESH (= 2.0).
5. **L4 boundary preserved.** Apparatus-level only; which observable IS Regime-II coherence held for Mike / Layer 1.
6. **[2,4] earned-but-heterogeneous** (surfaced by per-seed aggregation, routed to Layer 2, concurred). 5/5 seed-level occurrence but mixed window-count robustness: multi-window on seeds 137/256/31415, single-window on 42/1024. NOT grouped with [2,3] as uniformly robust. Carries the named reading lens: seed-level occurrence vs window-count robustness (window-count is the load-bearing measure because the apparatus is about earned windows).

Two precision notes (Layer-1, in the record):
- **Rule A dual-degeneracy.** For tau_A in {4..8}, both saturation_degenerate AND extinction_degenerate fire on the same rows: Tick_to_Freeze and Tick_to_Zero coincide, so a grid that zeroes on the same tick it stops changing is both "frozen" and "extinct." Changes no finding.
- **[3,4] non-eligible contrast** (routed to a fresh Layer 2 context, concurred). [3,4] shows positive meanI and strongly POSITIVE persistence (~+86 z mean) - opposite persistence sign from the eligible bands. Fenced from the earned-window signature: recorded as non-eligible contrast / instability-coupled observation, NOT a signature. Layer 2's principle: eligibility gates signature-status, not measurement-validity. Design relevance: the persistence axis REVERSES under the unstable band, which is why the signed three-level framework is needed. Documented input to wave-two design.

## Wave-one substantive findings (apparatus-level; no L4) - condensed

- Rule A non-discriminating across tau_A in {1..8}. Saturation {1,2,3} (tau_A=3 transient structure first, Tick_to_Freeze 41-57), collapse {4..8}. No locked-cluster regime. Both prior pictures (primary-source "high tau", Layer 3 "intermediate tau") not produced under wave-one conditions; scope-limited.
- Rule B eligible-or-near-eligible bands share positive meanI / negative persistence (anti-clustered, NOT near-null). [2,3] robust; [2,4] earned-but-heterogeneous; [1,2] boundary; [3,4] non-eligible / unstable (positive-persistence contrast, fenced).
- LowLow_Nondegenerate_Candidate False on all 300 rows. The wave-one low/low candidate signature did not appear under either rule.
- The co-equal pair separated under Rule B (opposite signs in eligible bands) - apparatus-level separation, NOT a naming of theoretical Psi.

## Remaining synthesis work - NONE

The synthesis cluster is complete. The routing archive was the last load-bearing piece, and it is committed (d76880b) and pushed. Nothing in the synthesis cluster remains open.

## Wave two - separate decision, NOT started, NOT seeded

Still queued, still NOT part of the synthesis cluster. The cluster's completion is the trigger point for the wave-two decision, but opening it is Mike's call - it has not been opened, and no wave-two probe has been seeded. Questions surfaced by wave one (carry forward):
- Rule C (deferred from wave one) as the candidate for the lifted low/low signature?
- Does Rule B's range-bound family structurally produce too much per-tick spatial organization whenever it sustains lifted rho?
- A Lambda-driven independent-activation / weakly-coupled-stochastic rule as a substrate-side low/low generator (Layer 2 flagged)?
- Does the signed three-level framework open probe-design possibilities the 2x2 concealed? (The [3,4] axis-reversal is the documented case that it does.)
- Finer-grained tau_A around the saturation/collapse boundary (tau_A = 3 -> 4)?

Do NOT seed wave-two probes without Mike opening the phase.

## On the horizon (named, not blocking)

- Wave-two design: the next phase decision, Mike's call.
- Ops-log backfill: there is no ops_log entry for the 2026-05-28 wave-one session or the 2026-05-30 synthesis session (test record / registry / held-inputs / prior anchor refresh). The most recent prior ops entry before this session was 2026-05-25 (CTL-001 closure); this session added 2026-05-30 (routing archive). The two intervening sessions were not backfilled because Layer 1 lacks primary source beyond the anchors; if wanted, they would be drafted from the ABM 28 / ABM 29 scrollback Mike holds.
- Layer 3 v1 code gap: OBS001_10 carries a flagged not-recoverable gap. If Mike locates the original Gemini attachment (UUID 46246e1e-...), swapping the verbatim body in would close it.
- Open ontological question (held at L4 unsettled): Psi_meanI_state vs Psi_persistence_I as Regime-II coherence characterization - co-equal, not yet arbitrated.

## Carry-forward cautions

### Reinforced this session (routing-archive build)

- **Verbatim-source discipline over reconstruction.** The archive method (RESUME_2026-05-30 decided) was build-from-verbatim-scrollback, not reconstruct. conversation_search returns anchor text and artifact fragments but NOT complete artifact bodies - confirmed again this session. When the decided source was not initially in hand, Layer 1 searched, located the source sessions (ABM 28 / ABM 29), and routed a request to prior Layer 1 instances to reproduce the bodies, rather than reconstructing. No body was fabricated.
- **Flagged gap over silent reconstruction.** OBS001_10 (Layer 3 v1 code) was attached as a file in 2026-05-28, not pasted inline, and is not recoverable. It is flagged as a documented gap with a derived v1->v2 difference record, never reconstructed.
- **BOM check MUST pair with size check.** Destination verification read Length against known expected byte counts, not BOM alone. The Downloads name collision (normalized "(1)" files alongside un-normalized bare-name originals) was discriminated by size; the archive was assembled from the "(1)" set under corrected names, originals excluded.
- **Hash before delete.** A duplicate code-v1 download was SHA256-checked (identical) before deleting, not assumed identical from matching size alone.

### Carries from prior anchors (still relevant)

- Cold-start grounding: manifest check one-liner -> read THIS anchor first -> kit -> CTL/SS/OBS records -> Cycle 2 closure. Verify HEAD against the live repo; the anchor's self-described HEAD may lag its own refresh commit (repo state wins).
- One PS command per copy-pane; wait for pasted output. `| Set-Clipboard` on ALL commands; `Out-String` for MatchInfo; empty clipboard IS confirmation for no-output commands. The `if/else | Set-Clipboard` scope bug: pipe `$msg` after the if/else, not inside a branch.
- Keep commit messages short (single -m line) to avoid the `>>` continuation hang.
- File delivery: create -> present_files -> Mike Move-Item/Copy-Item (full-file, `-Force` for existing). Verify byte-state AT DESTINATION (BOM + ending + SIZE) before trusting placement.
- Per-file BOM/trailing conventions: test record / held-inputs / anchor / ops-log / routing-archive = BOM-less, single trailing LF. Registry = BOM present, trailing blank line. Verify against the on-disk file before generating any overwrite.
- Stage with EXPLICIT paths, never `git add -A`. For a new directory of files, staging the directory as one pathspec is clean (and sidesteps bracket-glob hazards). Verify `git diff --cached --name-only` before every commit.
- Your sandbox runs are predictions, never results of record. Mike's run on the captured venv is the record.
- When the next step is clear, produce it; don't stall on "want me to X or hold?"
- Synthesis is the work; convergence is a phenomenon. Watch soft convergence and framing asymmetry. Sandbox/analytic agreement is a prediction, not a confirmation.

## Vocabulary (binding, including at wrap-ups)

- rho = activation density
- Psi_meanI_state, Psi_persistence_I - the two co-equal observables; neither named theoretical Psi
- Lambda (or Lambda_A, Lambda_B) = structural conduciveness (never "terrain favorability")
- agents act (no decision / optimization / utility / cognitive language)
- "the point(s) at which mu(rho) = 0" (never rho_c)
- "per-cell active-state values" (never "field" as explanatory mechanism)
- "earned steady-state window" - the SS-001-validated criterion
- `LowLow_Nondegenerate_Candidate` - apparatus-level flag; never `Regime_II`
- candidates produce or fail to produce (never "confirm" / "demonstrate" / "prove" / "validate")
- "fraction of active neighbors" is fine (local); never "fraction of the population"
- The two SS-001 flags `Steady_State_Candidate` and `Lifted_Activation_Candidate` remain independent at every stage
- The Rule B earned-window signature is **positive meanI / negative persistence** (dynamic spatial organization with anti-clustered persistence). NOT "high meanI / low persistence." The held-inputs framework distinguishes positive / near-null / negative on each axis.
- Reading lens: **seed-level occurrence vs window-count robustness** - window-count is the load-bearing measure of band robustness.

Vocabulary discipline holds throughout, including at the next instance's first substantive response after instantiation.
