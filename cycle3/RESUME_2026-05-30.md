# RESUME - Cycle 3, wave-two DESIGN PHASE open; Rule C contract placed canonical

**Supersedes:** the prior `RESUME_2026-05-30.md` (wave-one synthesis cluster complete; wave-two direction conferred-and-arbitrated but phase NOT opened). This refresh, written at the end of the 2026-05-31 session, records that Mike opened the wave-two design phase and accepted the Rule C design contract, now placed canonical at `cycle3/wave_two/DESIGN_CONTRACT.md` (commit a925475). Same filename kept deliberately (the cold-start grounding sequence references this exact path); the date in the filename is a grounding anchor, not a date stamp. The wave-one record (six refinements + two precision notes) carries forward verbatim below, unchanged.

**Written:** end of the 2026-05-31 wave-two-design-opened session.

---

## Orientation - where we left off

The wave-one (C3-OBS-001) synthesis cluster is complete and committed (unchanged from the prior anchor; detail retained below). The new state this session: the **wave-two design phase is OPEN**, and the Rule C design contract is **placed canonical** at `cycle3/wave_two/DESIGN_CONTRACT.md`. NO wave-two probe is seeded - seeding is a separate step, still Mike's call to open. The contract is the authoritative wave-two design document; this anchor summarizes it but the contract governs.

A new `cycle3/wave_two/` subdir was created this session to home the growing wave-two artifact set (rather than spreading it flat across `cycle3/` with prefixes as wave one did). The contract is its first file.

## Commits (most recent first)

- **a925475** - Cycle 3 wave-two: place Rule C design contract (`cycle3/wave_two/DESIGN_CONTRACT.md`) + ops log (`operations_log/2026-05-31_wave-two-design-opened.md`). 2 files, create-mode.
- **(this anchor refresh)** - commit 2 of the 2026-05-31 session; hash live-verified next session.
- **10375ee** - ops log for the 2026-05-30 routing-archive session.
- **d76880b** - OBS-001 wave-one routing archive (16 artifacts + README), 17 files, create-mode.
- **c31918b** - prior anchor refresh (synthesis items 1-3 committed; routing archive deferred) - superseded.
- **71e51b9** - held-inputs wave-one amendment (signed three-level framework; positive meanI / negative persistence).
- **dd06f38** - C3-OBS-001 wave-one A+B substrate (script + CSV + 60 NPZs), test record (new), registry advance to valid-L2.

HEAD at **a925475** when this anchor was written (will be the anchor-refresh commit once this file commits). The next instance's first action is `git --no-pager log --oneline -1` to confirm HEAD; HEAD will already include this anchor's commit.

## Gates - unchanged

- **C3-ENV-001 - passed-L1.** Canonical env: Python 3.14.4, Mesa 3.5.1, numpy 2.4.4. Venv at `C:\Users\vkz244\EE_Theory_Lab\venv` (workspace top-level, NOT repo root).
- **C3-CTL-001 - valid-L2.** Unchanged.
- **C3-SS-001 - valid-L2.** Unchanged.
- **C3-OBS-001 - valid-L2 (registry), apparatus-level closure.** Synthesis cluster fully committed including the routing archive. The registry carries the ladder term valid-L2 by convention; the record holds the nuance.

## The six refinements + two precision notes (canonical record; carried verbatim)

Enumerated so the next instance does not regress to the five-amendment compression:

1. **Rule B [1,2] correction.** Boundary / near-steady: Steady_State_Candidate True on 1/25 windows (seed 42 only), NOT earned-steady on 5/5 as Layer 3 first stated.
2. **Signed three-level framework.** Held-inputs Section 7: positive structured / near-null / negative anti-structured per axis, replacing the 2x2 high/low. The load-bearing move is distinguishing near-null from negative-anti-structured.
3. **Rule A endpoint amendment.** Saturation at tau_A in {1,2,3}, collapse at {4..8}; no locked-cluster regime under wave-one conditions; scope-limited.
4. **LowLow_Nondegenerate_Candidate False on all 300 rows.** Apparatus-level negative finding. Do NOT loosen LOW_Z_THRESH (= 2.0).
5. **L4 boundary preserved.** Apparatus-level only; which observable IS Regime-II coherence held for Mike / Layer 1.
6. **[2,4] earned-but-heterogeneous** (surfaced by per-seed aggregation, routed to Layer 2, concurred). 5/5 seed-level occurrence but mixed window-count robustness: multi-window on seeds 137/256/31415, single-window on 42/1024. NOT grouped with [2,3] as uniformly robust. Carries the named reading lens: seed-level occurrence vs window-count robustness (window-count is the load-bearing measure because the apparatus is about earned windows).

Two precision notes (Layer-1, in the record):
- **Rule A dual-degeneracy.** For tau_A in {4..8}, both saturation_degenerate AND extinction_degenerate fire on the same rows: Tick_to_Freeze and Tick_to_Zero coincide. Changes no finding. (Re-confirmed this session in the wave-one CSV read: 125 rows fire both flags.)
- **[3,4] non-eligible contrast** (routed to a fresh Layer 2 context, concurred). [3,4] shows positive meanI and strongly POSITIVE persistence (~+86 z mean) - opposite persistence sign from the eligible bands. Fenced from the earned-window signature: non-eligible contrast / instability-coupled, NOT a signature. Layer 2's principle: eligibility gates signature-status, not measurement-validity. Documented input to wave-two design.

## Wave-one substantive findings (apparatus-level; no L4) - condensed

- Rule A non-discriminating across tau_A in {1..8}. Saturation {1,2,3} (tau_A=3 transient structure first, Tick_to_Freeze 41-57), collapse {4..8}. No locked-cluster regime. Scope-limited.
- Rule B eligible-or-near-eligible bands share positive meanI / negative persistence (anti-clustered, NOT near-null). [2,3] robust; [2,4] earned-but-heterogeneous; [1,2] boundary; [3,4] non-eligible / unstable (positive-persistence contrast, fenced).
- LowLow_Nondegenerate_Candidate False on all 300 rows.
- The co-equal pair separated under Rule B (opposite signs in eligible bands) - apparatus-level separation, NOT a naming of theoretical Psi.

### Wave-one CSV re-read this session (primary-source, feeds wave-two design)

Pulled `cycle3/data_out/c3_obs_001_results.csv` (300 rows) under the canonical venv. Of the 97 non-degenerate-lifted rows, `Psi_meanI_state_z` is positive on ALL 97 (min ~139), with ZERO near-null and ZERO negative. Persistence has a 6-row near-null band (z ~ -1.85 to -0.06, mean ~ -1.43). The 97 is the reconciled non-degenerate-lifted population (sat=193, ext=135, both=125; the both-count is the dual-degeneracy note made concrete). **Finding: wave one contains NO lifted-near-null meanI region.** This is a target sharpened by absence for wave two (contract Section 5.1), NOT a prediction about Rule C - see the Wave two section.

## Remaining synthesis work - NONE

The synthesis cluster is complete (detail in the prior anchor's body, retained in git history at c31918b/3f5af0f). Nothing in the synthesis cluster remains open.

## Wave two - DESIGN PHASE OPEN; contract placed canonical; NOT seeded

The contract at `cycle3/wave_two/DESIGN_CONTRACT.md` (commit a925475) is authoritative. This section summarizes; the contract governs on any discrepancy.

**Phase state:** design phase open (Mike, 2026-05-31). Contract placed canonical. NO probe seeded; seeding is a separate step, Mike's call to open. The function form is canonically fixed; the comparator is split (see below); two downstream opens remain before seeding (grid density; what Comparator 0 observes).

**The load-bearing question (unchanged from the arbitrated scope):** NOT "which rule produces low/low," but - can a lifted, non-degenerate activation-density regime be produced WITHOUT per-tick spatial organization or clustered persistence being structurally forced by the update rule? Is low/low a genuine earned regime in a mechanism family ALSO capable of producing non-low/low signed regimes?

**The instrument - Rule C (function form FIXED this session via Layer 2):** a single signed coupling coefficient kappa on the BECOMING-ACTIVE transition (NOT survival - this is what makes Rule C a distinct mechanism class from Rule B), as a function of the fraction of active neighbors. Canonical form: `p_become = sigma(logit(p_Lambda) + kappa * g(q))`, with `q = n/8`, `g(q) = 2q - 1` fixed centered, sigma logistic. Committed property: coefficient-null separability - at kappa = 0, p_become = p_Lambda exactly. Positive kappa contagion-dominant, negative divergence-dominant. A bounded-linear probability-space form is held as a RESERVED AUDIT, not canonical.

**Construction fences (binding; in the contract):**
- kappa-sign-through-zero is a SUBSTRATE update parameter, NOT an operationalization of the point(s) at which mu(rho) = 0. The writeup must never treat kappa = 0 as mu(rho) = 0.
- Fence-integrity prohibitions (Layer 2): no kappa depending on global rho; no dynamic centering against rho (use FIXED g(q) = 2q - 1); no hidden rho->Psi or observable-feedback term in the Lambda baseline; no piecewise sign-specific function.
- Survival must NOT acquire neighbor-count or range dependence, or Rule C collapses toward Rule B (Layer 2-originated).

**Contrastive success criterion (pessimistic-on-passing for wave two):** a low/low result is earned only if the design discriminates (1) trivial-stochastic, (2) rule-forced, (3) degenerate, (4) substantive-candidate - and a substantive low/low must be BRACKETED by non-low/low signed regimes in parameter space. The sweep must demonstrably reach positive-meanI/negative-persistence, positive-meanI/positive-persistence, near-null/near-null, and degenerate endpoints, or any low/low is rule-forced by default. Large-|kappa| is a degeneracy probe (curvature can make it near-threshold), NOT the evidentiary center.

**Comparator - SPLIT into two objects (scope refinement this session).** The arbitrated scope named "a Lambda-driven weakly-coupled stochastic comparator," SINGULAR. This session split it into two, on Mike's arbitration (2026-05-31), because Layer 2 found a single comparator calibrated to hold meanI near-null is circular (near-null by selection against the band it is meant to floor). The split:
- **Comparator 0** - Lambda-only, NO neighbor term. Calibrated on rho-lift + non-degeneracy ALONE (never any z-score). Near-null observables are an OUTPUT, recorded, not a target. The clean trivial-stochastic floor. KEY open: if lifted Lambda-only activation already gives positive meanI, near-null is NOT the apparatus's default floor - a finding, not a tuning failure.
- **Comparator epsilon** - small fixed neighbor term, magnitude bounded by a predeclared realized-contrast ceiling `|p(q=1) - p(q=0)| <= epsilon * pbar_Lambda`, epsilon fixed before observing any z-score. A weak-coupling AUDIT around the floor, NOT the floor itself.
This split is the reason the singular-comparator language in the older arbitrated-scope text is superseded; do NOT read the singular form as current and flag the contract as drift.

**Open B (comparator near-null scale) - SPLIT, not closed.** Persistence half: readable from wave one (the 6 near-null persistence rows). MeanI half: NOT readable (wave one has no lifted-near-null meanI), so it is OBSERVED from Comparator 0, not set from wave one.

**Weak-form Rule B prior (PRESERVE - do not harden):** Rule B structural overproduction of positive meanI when lifted is the best WORKING PRIOR, NOT proven by construction. The wave-one CSV read is consistent with it but does not convert it to proof. The door to Rule B stays open. Converting the prior into an asserted result is forbidden.

## On the horizon (named, not blocking)

- **Wave-two next steps (from the contract's Section 10):** (1) set the kappa-grid density against the realized-contrast scale; (2) run Comparator 0 (Lambda-only) and record what it produces, then Comparator epsilon as the weak-coupling audit; (3) seeding is Mike's call to open. The two genuine opens are grid density and what Comparator 0 observes.
- **Layer 3 (Gemini) implementation** of Rule C + the two comparators is not yet routed; it is the natural next routing once grid density is set. Frame Layer 3 deliverables as "what the run will produce," and Layer 3 never declares its own L2 clearance.
- Ops-log backfill: no ops_log entry for the 2026-05-28 wave-one session or the 2026-05-30 synthesis session; would be drafted from the ABM 28 / ABM 29 scrollback Mike holds if wanted.
- Layer 3 v1 code gap: OBS001_10 carries a flagged not-recoverable gap (UUID 46246e1e-...).
- Open ontological question (held at L4 unsettled): Psi_meanI_state vs Psi_persistence_I as Regime-II coherence - co-equal, not yet arbitrated. NOT to be resolved by statistical robustness or by which observable a script names.
- Untracked at repo root: `read_obs001_nearnull_scale.py` (this session's pure-read CSV summary script). Harmless; retain or remove at Mike's discretion.

## Carry-forward cautions

### Reinforced this session (wave-two design)

- **Function form fixed by Layer 2, not Layer 1 framing.** The genuine question was routed open; the centering correction came back from Layer 2 and was taken. Layer-2 divergences (centering, survival fence) recorded as Layer-2-originated, not absorbed.
- **Circularity caught at conferral, not after seeding.** Q2 was flagged by Layer 1 as its least-sure construction point, routed as such; Layer 2 confirmed it; the split resolved it structurally. Pessimistic-on-passing applies to Layer 1's own design.
- **Scope-step surfaced, not folded silently.** The comparator split exceeds the arbitrated singular-comparator scope; put to Mike as a fork, recorded in the contract and this anchor.
- **Open did not close as anticipated; recorded as split, not forced.** Open B's meanI scale is OBSERVED from Comparator 0, not set from a wave that lacks it.

### Carries from prior anchors (still relevant)

- Cold-start grounding: confirm HEAD against the live repo -> manifest check -> read THIS anchor first -> kit -> records. Repo state wins over the anchor's self-described HEAD.
- One PS command per copy-pane; wait for pasted output. `| Set-Clipboard` on ALL commands; `Out-String` for MatchInfo; empty clipboard IS confirmation for no-output commands.
- Keep commit messages short (single -m line) to avoid the `>>` continuation hang.
- File delivery: create -> present_files -> Mike Move-Item/Copy-Item (full-file, `-Force`). Verify byte-state AT DESTINATION (BOM + ending + SIZE) before trusting placement; size discriminates a stale/wrong-source copy that placement-confirmation alone does not.
- Per-file BOM/trailing conventions: test record / held-inputs / anchor / ops-log / routing-archive / wave_two contract = BOM-less, single trailing LF. Registry = BOM present, trailing blank line. Verify against the on-disk file before generating any overwrite.
- Stage with EXPLICIT paths, never `git add -A`. Verify `git diff --cached --name-only` before every commit.
- Your sandbox runs are predictions, never results of record. Mike's run on the captured venv is the record.
- When the next step is clear, produce it; don't stall. Ask only when a real fork needs Mike's arbitration.
- Synthesis is the work; convergence is a phenomenon. Watch soft convergence and framing asymmetry. Sandbox/analytic agreement is a prediction, not a confirmation.

## Vocabulary (binding, including at wrap-ups)

- rho = activation density
- Psi_meanI_state, Psi_persistence_I - the two co-equal observables; neither named theoretical Psi
- Lambda (or Lambda_A, Lambda_B) = structural conduciveness (never "terrain favorability")
- agents act (no decision / optimization / utility / cognitive language)
- "the point(s) at which mu(rho) = 0" (never rho_c)
- "per-cell active-state values" (never "field" as explanatory mechanism)
- "earned steady-state window" - the SS-001-validated criterion
- `LowLow_Nondegenerate_Candidate` - apparatus-level flag; never `Regime_II`
- candidates produce or fail to produce (never "confirm" / "demonstrate" / "prove" / "validate")
- "fraction of active neighbors" is fine (local); never "fraction of the population"
- The two SS-001 flags `Steady_State_Candidate` and `Lifted_Activation_Candidate` remain independent at every stage
- The Rule B earned-window signature is **positive meanI / negative persistence**. NOT "high meanI / low persistence."
- Reading lens: **seed-level occurrence vs window-count robustness** - window-count is the load-bearing measure of band robustness.
- Rule C coupling coefficient kappa: the kappa = 0 sweep point is a substrate parameter, NEVER "the point(s) at which mu(rho) = 0."

Vocabulary discipline holds throughout, including at the next instance's first substantive response after instantiation.
