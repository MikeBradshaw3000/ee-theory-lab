# RESUME anchor - EE-emergence theory project (Layer 1 grounding)

**Grounding-anchor filename note:** This file keeps the `RESUME_2026-05-30.md` name deliberately. It is a grounding anchor, not a date stamp. The actual refresh recorded below is **2026-06-27 (Rule E bounded-gain follow-up RESOLVED at the design level to modified Candidate A; B/C held under formal freeze discipline; still UNSEEDED), on top of 2026-06-12 (Rule E first pass RUN and READ; scoped negative for the predeclared design; mechanism class NOT closed)**, superseding the prior bounded-gain-named-not-triggered content and all earlier wave-two states. On any discrepancy between this anchor and a committed findings file, design contract, or resolution memo, the committed file wins.

**Supersession header (latest, 2026-07-04b):** Refreshed committed together with TWO_CHANNEL_ORDERING_PROBE_CONTRACT.md (same-commit anchor; filename-referenced). Since the prior refresh (design resolution committed; probe named-not-triggered), Mike EXPLICITLY OPENED THE CONTRACT STAGE and it is now CLOSED AT PLACEMENT: the contract was L1-drafted from primary source (window/block constants verified from the committed scripts, not the anchor summary), Layer 2-attacked (rejected as ratification-ready; 17 amendments), taken through a Mike-ordered L1<->L2 convergence round under anti-soft-convergence discipline (C.1/C.3/C.4/C.5 converged on replacement text; B.1-B.3 closed from primary source; Fork 1 closed by EVIDENCE-DRIVEN convergence with an independent L1 ground on record - the degenerate circular-phase null: the 3-block-periodic schedule admits only 3 distinct phase alignments over the 12-block primary set), Layer 2 fidelity-checked CLEAN, and Mike-arbitrated for placement (2026-07-04; L2's optional G2 precision folded). Mike's arbitrations: Lambda = 0.40 SINGLE ANCHOR (identifiability choice; Lambda = 0.20 named-not-triggered density-robustness follow-up; NO density-stability claim from this probe); CM-0 comparator matrix FULL 135 ROWS (joint L1+L2 recommendation); NULL-EXTENSION RUN AUTHORIZED (Lambda = 0.40, kappa = 0, u = 0, 400 ticks, canonical seeds, F3-checked bit-exact against committed ticks 0-199; NEW EXECUTION, not a read; NOT YET EXECUTED). Design fixed by the placed contract: 3-block / 75-tick stepped three-level ramp, u tiers {0.10, 0.25, 0.50} logits, 400-tick runs (16 blocks), primary phase-balanced analysis family (blocks 3-14; window starts blocks 3-11), c-labels {0, +/-0.05, +/-0.10, +/-0.20, +/-0.35} as BASELINE realized-contrast labels, 180 CM-1 runs + 135 CM-0 rows, lag-0 block convention, G1 slope+amplitude+correlation with row-level CM-0 dominance, G2 anchor pure-read-emitted (hand 0.15 removed), G3 bin-eligibility constants + pre-seed common-support feasibility audit, G4 cellwise compression audit, AXIS-SPECIFIC onset (conjunctive-onset prohibited). THE PROBE REMAINS UNSEEDED. Sequencing: null-extension run -> threshold-fixing pure read -> thresholding addendum frozen into the contract -> seeding ONLY on Mike's separate, explicit call. TWO_CHANNEL_ORDERING_PROBE_CONTRACT.md governs the contract; TWO_CHANNEL_ORDERING_PROBE_DESIGN_RESOLUTION.md governs the frame. Any prompt describing the contract stage as unopened, the contract as draft-only, or the null-extension run as executed is STALE.

**Supersession header (2026-07-04, carried):** Refreshed committed together with TWO_CHANNEL_ORDERING_PROBE_DESIGN_RESOLUTION.md (same-commit anchor; filename-referenced). Since the prior refresh (two-channel proposal recorded, ordering probe named), Mike arbitrated the three-way continue-or-rest fork: the TWO-CHANNEL ORDERING PROBE is the chosen next non-seeding path and is now DESIGN-RESOLVED (Layer 2-reviewed twice, Mike-arbitrated). Primary construction is CM-1: an EXOGENOUS FROZEN DETERMINISTIC common-mode driver plus differential-channel (Rule C kappa) sweep - u_t added inside the becoming-active logit, block-constant, identical at every cell, identical across seeds, NO feedback (the observable-feedback fence holds trivially; the bounded-A argument-scale pathology cannot arise). This AMENDS the named instrument's class wording ("admissible Rule E class"): the probe is renamed "Two-channel ordering probe: exogenous common-mode lift with differential-channel sweep" and is NOT a Rule E continuation (Mike-ratified). CM-0 (static uniform offset) = matched-rho comparator only, fails the fence-4 distinguishability test by construction. CM-2 (endogenous Rule E-class common-mode conditioning) = HELD, own design resolution required, SEPARATE from the Rule E B/C hold. Four validity gates (G1 schedule-imprint-above-CM-0; G2 differential-reach at the propensity level; G3 matched-rho overlap; G4 slope audit), seven binding fences, and produce/fail-to-produce falsification conditions are fixed; all numeric specifics (waveform/period/u_max, Lambda anchor + rho band, kappa grid, thresholds) are contract-stage opens. **The ordering probe remains NAMED-NOT-TRIGGERED**; the contract stage opens only on Mike's separate explicit call. Rule E B/C responsive pass HELD (not abandoned); rest-wave-two available (not chosen). TWO_CHANNEL_ORDERING_PROBE_DESIGN_RESOLUTION.md governs the resolution; TWO_CHANNEL_PROPOSAL.md governs the proposal's committed standing (its class wording amended by the resolution). No Comparator 0 reclassification; L4 untouched. Any prompt lacking the two-channel design resolution is STALE.

**Supersession header (2026-07-02c, carried):** Refreshed committed together with TWO_CHANNEL_PROPOSAL.md (same-commit anchor). Since the prior refresh (pass-1 findings), Mike proposed the TWO-CHANNEL READING (common-mode vs differential influence; Narrative as paradigm common-mode contributor; metric-agnostic locality in v,c,r-space); L2 attacked as requested and it survives MODIFIED: floor reinterpretation ruled RELABELING (floor classification STANDS; no reclassification); the ORDERING PREDICTION survives risky-and-falsifiable; covariance corrected (common mode RESCALES differential covariance through the logit slope); **TWO-CHANNEL ORDERING PROBE named-not-triggered** with two binding constraints (kappa substrate-swept / dominance outcome-only; rho range below saturation compression). TWO_CHANNEL_PROPOSAL.md governs the proposal's standing. The continue-or-rest arbitration now has the two-channel direction as a DISTINCT FORK. Phil-facing overview note delivered via Dropbox (transit). Any prompt lacking the two-channel proposal is STALE.

**Supersession header (2026-07-02b, carried):** Refreshed committed together with READING_D_PASS1_FINDINGS.md (same-commit anchor; filename-referenced). Since the prior refresh (synthesis committed, program opened), pass 1 of the Reading-D pure-read program RAN and was ARBITRATED (Mike-concurred): **POSITIVE DISCRIMINATION - Readings B and D JOINTLY CONFIRMED** (B at the propensity level: prop_I ~ +0.38 both coupling signs, ~0 at the kappa=0 floor - the pair-level mechanism directly measured; D at the measurement level: committed z ~4-12 sits on raw Moran ~0.005 - the grid-local null is overpowered). Attenuation measured: meanI retains ~1-3% of propensity structure, persistence ~13-18% (the measured axis asymmetry); C3 confirms genuine synchrony. **Reading A bounded near-empty at the tested base** (admissible Rule E acts through the propensity surface; L2 technical assessment; disposition Mike's). B/C HELD. No class closed; L4 untouched. READING_D_PASS1_FINDINGS.md governs. **The open arbitration is wave two's CONTINUE-OR-REST - Mike's, now fully equipped.** Any prompt lacking the pass-1 result is STALE.

**Supersession header (2026-07-02, carried):** Refreshed committed together with WAVE_TWO_PATTERN_SYNTHESIS.md (same-commit anchor; filename-referenced). Since the prior refresh (bounded-A arc rests), a pattern-level synthesis was routed to Layer 2 and arbitrated: FOUR READINGS carried (A - search incomplete, LIVE NARROWED to the untested clean-responsive rescaled Rule E channel; B - substrate couples lift to organization, NOW THE STRONGEST WORKING APPARATUS-LEVEL HYPOTHESIS, not a closure; C - signature question, fenced L4-adjacent; D - apparatus scale/null sensitivity, NEW, L2 addition). L2 contributed the pair-level structural argument (overlapping Moore neighborhoods => propensity autocorrelation under both coupling signs, [b'_eff]^2 removes the sign) and corrected L1's invariant (non-near-null-on-the-pair, positive meanI most robust axis; NOT positive-both-axes). **The Reading-D PURE-READ PROGRAM IS OPEN** (Mike-arbitrated, non-seeding: raw effect sizes vs z, shuffle controls, propensity-autocorrelation discriminator, on committed NPZs across all instruments; design predeclared in the synthesis document, which governs). **The B/C design question is HELD pending the pure-read results** (A-vs-B hinge: a B/C pass discriminates only if clean-responsive). WAVE_TWO_PATTERN_SYNTHESIS.md governs the synthesis. Any prompt lacking the pattern synthesis or the open pure-read program is STALE.

**Supersession header (2026-07-01b, carried):** Refreshed committed together with RULE_E_BOUNDED_A_FINDINGS.md (same-commit anchor; referenced by filename, not hash). Since the prior refresh (seed complete, read pending), the READ was performed (pure-read script, per-seed aggregation), routed to Layer 2, and ARBITRATED (Mike-concurred): Rule E bounded-A RESTS on a SCOPED NEGATIVE for the 50-run own-direction design - zero LowLow in 450 windows; amplitude runaway REPAIRED (no degeneracy, tiers distinct, non-inert) but the channel SATURATED at every nonzero tier (bounded saturated relay, not clean-responsive conditioning); responsive window for un-conditioned-sigma bounded-A likely empty at practical resolution (L2 quantified); B/C standing STRENGTHENED, NOT TRIGGERED; L4 not moved; mechanism class NOT closed. RULE_E_BOUNDED_A_FINDINGS.md governs. Any prompt describing the bounded-A read as pending is STALE.

**Supersession header (2026-07-01, carried):** Refreshed committed together with the bounded-A run outputs, the one-line path fix, and the corrected spec (same-commit anchor; all referenced by filename, not hash). Since the prior refresh (build committed pre-run), Mike SEEDED the bounded-A run. First attempt halted loudly at F3 on the reintroduced wrong-directory import bug (the first-pass 3abf8dd class; L1 build review missed it - owned); Layer 1 applied the one-line fix (cycle3/wave_two), the re-seed ran CLEAN AND COMPLETE: F3 bit-exact both signs all seeds, split constants reproduced exactly, outputs = window CSV + block CSV + 50 state NPZs. The grid-count discrepancy (spec's predeclared 90 vs as-built 50) was arbitrated to 50 (Option 1, first-pass parity; spec Section 5 corrected with lineage); the cross-direction extension (40 mixed-direction cells) is NAMED, NOT TRIGGERED. **THE READ HAS NOT BEEN PERFORMED** - no classification, no result exists; opening the read is Mike's call. Any prompt describing the bounded-A pass as "not seeded" or the grid as 90 is STALE.

**Supersession header (2026-06-27c, carried):** Refreshed committed together with c3_w2_rule_e_bounded.py (same-commit anchor; the build script is referenced by filename, not hash). Since the prior refresh (bounded-A spec drafted + L2-reviewed), Layer 3 BUILT the bounded-A instrument as c3_w2_rule_e_bounded.py (a modification of the committed first-pass c3_w2_rule_e.py, not overwriting it), and Layer 1 BUILD-REVIEWED it (pessimistic-on-passing, tested against code): the build PASSED - conditioning term delta_j*tanh(g_E) with argument g_E alone, d=0 no-0/0 bypass, F3 self-catching seed-blocking at runtime, leak surface holds, measure_baseline ddof=0 verified identical to first pass. Any prompt describing the bounded-A build as "not built" / "spec only" is STALE; c3_w2_rule_e_bounded.py is the committed build and the spec governs it.

**Supersession header (2026-06-27b, carried):** Refreshed committed together with RULE_E_BOUNDED_GAIN_IMPLEMENTATION_SPEC.md (same-commit anchor; the spec is referenced by filename, not hash). Since the immediately prior refresh (bounded-gain resolution to modified-A), the bounded-A implementation spec was DRAFTED and Layer 2-reviewed (accept with precision amendments incorporated): the cond_term_j = Delta_j*tanh(g_E) operationalization endorsed, the one-sigma prose corrected (Delta_j is cap/nominal-scale), the d=0 division guard added, saturated-channel thresholds set (bound_ratio>=0.90 per-block; >=3/4 blocks window-level). Any prompt describing the bounded-A spec as "not drafted" is STALE; RULE_E_BOUNDED_GAIN_IMPLEMENTATION_SPEC.md governs the build. Everything below the bounded-gain lines (first-pass run narrative, resolution, wave-one / Rule C / Rule D R3 arcs, all gates) is UNCHANGED.

**Prior-refresh header (2026-06-27, carried):** Refreshed committed together with RULE_E_BOUNDED_GAIN_RESOLUTION.md (same-commit anchor; the resolution is referenced by filename, not by hash, since it lands in this same commit). The content before THAT (refreshed at a56731c / Rule E first pass run, scoped negative, bounded-gain pass named-not-triggered) is SUPERSEDED on the bounded-gain line only: the bounded-gain follow-up is now DESIGN-RESOLVED to modified Candidate A (Layer 2-recommended, Mike-arbitrated; RULE_E_BOUNDED_GAIN_RESOLUTION.md governs), B/C held under formal freeze discipline, STILL UNSEEDED - no spec, no run, Layer 3 not engaged. The first-pass scoped-negative finding is NOT moved by this refresh; it stands. Everything below the bounded-gain lines (the first-pass run narrative, wave-one / Rule C / Rule D R3 rested arcs, all gates) is UNCHANGED. Any instantiation prompt describing the bounded-gain pass as "named-not-triggered" / "no construction selected" is STALE; the bounded-gain resolution governs the selected construction.

**Prior-refresh header (2026-06-12, carried):** Refreshed at HEAD a56731c. The state before THAT (refreshed at 4bf8aa0 / describing Rule E as design-contract-canonical, opens 1-6 resolved, lag premise verified, STILL NOT SEEDED) was superseded by the first-pass run: spec -> L2 review -> canonical spec (59b4bd6); L3 build -> L1 build review (three corrections, seed-blocking F3-against-committed-Rule-C) -> build (3738157); first run FAILED LOUDLY at F3 on a wrong-directory path bug before any data was written (the P pre-flight choice vindicated); one-line path fix (3abf8dd); the SEED ran clean, F3 bit-exact both signs all seeds; outputs committed (ab51d36: window CSV, block CSV, 50 state NPZs); read + L2 synthesis + arbitration to a scoped-negative result. Any instantiation prompt describing Rule E as "not seeded" / "design-complete, no run", or describing HEAD as 4bf8aa0 or earlier, is STALE.

---

## Who you are / how this works

Layer 1 (Claude): architectural guardian and vocabulary enforcer, routing-note author, cold-start grounding from this anchor + the contract + findings files before any session work. Layer 2 (ChatGPT): mean-field substantive review. Layer 3 (Gemini): Mesa implementation; never declares its own L2 clearance. Mike: sole execution channel and arbiter of all commits; routes notes between layers. You draft and route; Mike runs. You never execute locally.

REPO: github.com/MikeBradshaw3000/ee-theory-lab (local: C:\Users\vkz244\EE_Theory_Lab\ee-theory-lab). Canonical venv: C:\Users\vkz244\EE_Theory_Lab\venv (workspace top-level, NOT repo root; a venv_cycle1_archive sits next to it - do NOT activate it).

---

## WHERE THINGS STAND (read first)

HEAD a56731c, origin current, tree clean. (read_obs001_nearnull_scale.py at repo root is now gitignored as wave-two read-helper scaffolding - committed 8e431ee, literal-enumeration entry; no longer shows as untracked.)

**Wave-one (C3-OBS-001) synthesis cluster: COMPLETE and committed.** Do NOT redo it. Carry the SIX refinements + TWO precision notes (below) verbatim - never the five-amendment compression.

**Wave-two first pass RESTS on A-prime (2026-06-02, Layer 2-concurred):** floor-adjacent weak-coupling LowLow produced; away-from-floor substantive LowLow NOT produced. FIRST-PASS rest, NOT wave closure - Rule C is NOT closed as a mechanism; the design phase stays open to object (b)/(c) on a separately named theoretical target. Seeding of any FURTHER probe remains Mike's call to open; no seed is required to sustain the first-pass result.

**Rule D, turnover-limited activation - RUN COMPLETE AND READ; ARC RESTS (2026-06-03, 0407e05, Layer 2-concurred).** Findings canonical at cycle3/wave_two/rule_d/RULE_D_R3_FINDINGS.md. The R3 run (theta_turnover grid {0,0.02,0.05,0.10,0.20,0.35,0.50} x matched responsive c=+/-0.35 both signs x 5 seeds, Lambda=0.40, 70 settings x 5 windows = 350 rows; CSV at cycle3/data_out/c3_w2_rule_d_r3_results.csv, committed c1ff920) was read against the contract Section 4 matched-coupling bracket. RESULT: **Rule D R3 FAILS TO PRODUCE the turnover-limited near-null mechanism under the locked topology and predeclared R3 grid.** The signed-anchor precondition is met (theta_turnover=0 recovers the Rule C signed regime, both signs); rho stays lifted and non-degenerate across the whole ladder; coupling exposure stays live; no row degenerates; LowLow_Nondegenerate_Candidate fires nowhere; and the co-equal pair is NEVER jointly near-null at any theta. The two contract guards (4.3 density-confound, 4.4 live-coupling exposure) do not bind - no candidate row to audit. The arc RESTS on this scoped negative; it does NOT close the broader turnover-limited mechanism class, because a stronger-turnover floor/degeneracy endpoint was NOT reached by theta_turnover=0.50 under R3 (which only halves s_Lambda, 0.40 -> 0.20 net persistence). Direct analogue of the Rule C first-pass rest on A-prime.

Separate apparatus-level finding (fenced from L4): **single-axis turnover sensitivity with persistence-sign retention.** Psi_meanI_state is turnover-sensitive (at c=+0.35 its sign reverses; at c=-0.35 it climbs strongly positive); Psi_persistence_I sign is turnover-robust (holds the coupling-side sign across the ladder at both signs). Order-parameter reading (Layer 2): persistence tracks where activity keeps being regenerated across the window (robust to site churn while the becoming-active kernel re-injects neighborhood propensity each tick); meanI tracks the instantaneous configuration (puncturable by churn). The becoming-active transition re-injects the q-dependent birth source every tick, so independent turnover shortens residence time without removing live coupling - which is why rho stayed lifted, exposure stayed live, and persistence held its sign. This does NOT resolve the L4 ontological question and selects neither observable as theoretical Psi.

Rule D mechanism (R3, governing): active-cell persistence is s_Lambda * (1 - theta_turnover) - Rule C's neighbor-independent Lambda-survival (s_Lambda=0.40 at Lambda=0.40) RETAINED, theta_turnover an ADDITIONAL independent churn hazard on top (ASCII theta_turnover; NOT eta), so theta_turnover=0 recovers Rule C exactly. The R3 form replaced an earlier survival-replacement form (persistence = 1 - theta_turnover) under which theta_turnover=0 saturated the lattice; Layer 3 implementation surfaced the saturation, Layer 2 recommended R3, Mike arbitrated it in. The (R3-amended) contract governs Rule D design; the (R3-corrected) resolution memo governs the grid; the findings file governs the result.

**Rule E (lagged-Lambda conditioning) - FIRST PASS RUN AND READ; SCOPED NEGATIVE FOR THE PREDECLARED DESIGN; MECHANISM CLASS NOT CLOSED (2026-06-12, ab51d36 run / a56731c findings, Layer 2-arbitrated, Mike-concurred).** Findings canonical at cycle3/wave_two/rule_e/RULE_E_FIRST_PASS_FINDINGS.md (governs the result). Contract canonical at cycle3/wave_two/rule_e/RULE_E_DESIGN_CONTRACT.md (governs design); opens resolution at cycle3/wave_two/rule_e/RULE_E_OPENS_RESOLUTION.md (governs the resolutions); admissibility rationale at cycle3/wave_two/RULE_E_SCOPING_RESULT.md (governs admissibility). The first pass executed the committed design (contract 2f3ce0e, opens 65a7265, spec 59b4bd6, build 3abf8dd) and is committed at ab51d36 (window CSV, block CSV, 50 state NPZs).

RESULT: **Rule E first pass FAILS TO PRODUCE a substantive multiscale near-null under the locked topology and the predeclared first-pass design.** The failure mode is NOT inertness; it is over-driven block-lag feedback - extinction on the positive base sign, block-cadence saturate/extinct oscillation (temporal cancellation) on the negative. Scoped negative; does NOT close the Rule E mechanism class.

Pre-run gates passed: F3 alpha=0 reproduced committed Rule C M2 bit-exactly over a full 400-tick trajectory at (Lambda=0.40, kappa=+/-0.7599, every seed). Split pre-registered constants measured at block scale on the alpha=0 baseline: M_ref_plus=0.3714 / sigma_M_plus=0.0025; M_ref_minus=0.4187 / sigma_M_minus=0.0017.

Two-sided outcome (per-(setting, seed); window CSV + block CSV):
- Positive base (c=+0.35, kappa=+0.7599): EXTINCTION at all nonzero alpha tiers (block_rho ~0.000, extinction_degenerate=True; cond_logit_term pinned negative driving p_become -> 0). Degenerate; not candidate.
- Negative base (c=-0.35, kappa=-0.7599): window-level lifted near-null flags (mean_rho ~0.326, LowLow_Nondegenerate_Candidate=True on ~all post-burn-in windows), BUT the block record shows a 25-tick-cadence saturate/extinct OSCILLATION (block_rho rings ~0.02 <-> ~0.63; ~50% of post-burn-in blocks <0.05 and ~50% >0.60; raw block meanI sign-alternates 5/5 runs). The window mean is the midpoint of a ring no tick is near. Rejected by the LAG-DYNAMICS GUARD as a temporal-cancellation artifact. The operative rejection is the GUARD, NOT a reach failure.

Failure mechanism (calibration / runaway feedback; diagnosis, not a separate finding): the channel was OVER-DRIVEN, not inert. sigma_M was measured on the UN-CONDITIONED baseline (~0.0017); once conditioning perturbs rho, the standardized g_E=(M-M_ref)/sigma_M divides the conditioned excursion by the tiny un-conditioned sigma_M, reaching +/-230, and the conditioning term reaches +/-100 logits against the intended |d|<=0.10 displacement. The feedback rails. The byte-identical negative tiers (m0_1054 / m0_2136 / m0_4418) share block_rho trajectories because once g_E saturates the logit, larger alpha cannot change the railed per-block outcome. This is the run-time realization of the contract's honest-scope bound (tau_rho=1.66 un-conditioned / 2.86 coupled base did NOT certify CONDITIONED dynamics); the predeclared lag-dynamics guard was the catch and it caught it.

L1 error owned: Layer 1 initially hypothesized an INERT macro channel from the window CSV; the block CSV REFUTED it (over-driven, not inert) - recorded honestly in the findings and ops log. Pessimistic-on-passing on Layer 1's own read, caught by the block instrument.

Vindicated: the BLOCK-RESOLVED RECORDING REQUIREMENT (contract Section 5) is strongly vindicated - window-level data alone would have misread the negative-base run as the first substantive lifted near-null the program has sought. L4: NOT moved; sign-asymmetric over-driven lag-feedback finding is apparatus-level only, fenced exactly as the Rule D R3 single-axis finding; neither observable named theoretical Psi. Hygiene audits (pure read; passed before recording): 50 runs no run_id/NPZ collisions; zero alpha/sign mapping mismatches; railing audit confirms the three-regime read.

Bounded-gain follow-up (Mike's call ONLY; analogous to the Rule D stronger-turnover endpoint extension): **Rule E bounded-gain pass - RESOLVED at the design level to modified Candidate A (2026-06-27, Layer 2-recommended, Mike-arbitrated; RULE_E_BOUNDED_GAIN_RESOLUTION.md governs); **READ COMPLETE 2026-07-01 - ARC RESTS on a SCOPED NEGATIVE; findings canonical at cycle3/wave_two/rule_e/RULE_E_BOUNDED_A_FINDINGS.md (governs).** Construction: same Rule C M2 base / 25-tick block-lag / Lambda-configuration locus / block-resolved guard, with the unbounded standardized conditioning term replaced by a smooth (tanh) TIER-SPECIFIC bounded realized term - each tier d_j capped at its own |Delta_j| by logit inversion at p_Lambda=0.40 (the global-d_max form was an L1 defect caught by L2: a global bound reproduces the first-pass identical-tiers pathology in bounded form). Additional diagnostics required (raw g_E, pre-bound term, post-bound term, effective Lambda, bound-activation frequency) plus a saturated-channel-candidate flag (a near-null appearing only while the bound is active nearly always is recorded as saturated-channel, not clean-responsive, pending audit). Candidates B (recalibrated alpha ladder) and C (two-scale standardization) are HELD as conditionally-admissible alternatives under a SEVEN-CONDITION formal freeze discipline (M_ref stays un-conditioned; conditioned scale measured in a separate run, frozen pre-sweep, never recomputed by seed/window/tier/flag/running-rho; characterization run not tuned on observables or LowLow; scale used only in the becoming-active channel; alpha=0 still bypasses exactly) - not selected for the first pass (both need a separate characterization run = a later seeding decision; both carry linear-gain mis-sizing risk; C additionally keeps the unbounded linear form). Implementation spec DRAFTED and Layer 2-reviewed (2026-06-27, accept with precision amendments incorporated; RULE_E_BOUNDED_GAIN_IMPLEMENTATION_SPEC.md governs the build). The spec operationalizes modified-A as cond_term_j = Delta_j*tanh(g_E) (single-quantity form; alpha_j=Delta_j collapse, no separate gain parameter - L2-endorsed), sets the saturated-channel thresholds (per-block bound_active at bound_ratio >= 0.90; window-level saturated-channel at >= 3/4 constituent blocks; 2/4 mixed), and closes the d=0 division-by-zero hole (both L2 catches). BUILT by Layer 3 as c3_w2_rule_e_bounded.py (modification of the committed first-pass script, not overwriting it) and L1-BUILD-REVIEWED (build passed; conditioning term delta_j*tanh(g_E) with argument g_E alone, d=0 no-0/0 bypass, F3 self-catching seed-blocking at runtime, leak surface holds, measure_baseline ddof=0 verified identical to first pass so split constants reproduce, saturated-channel window logic correct). Build COMMITTED PRE-RUN (matching first-pass build at 3738157). **SEEDED AND RUN COMPLETE (2026-07-01).** First seed attempt halted loudly at F3 on the same wrong-directory import bug the first pass fixed at 3abf8dd (L3 reintroduced it; L1 build review missed it - owned); one-line fix applied (cycle3/wave_two, matching the committed first-pass form), re-seed ran clean: F3 bit-exact both signs all seeds; split constants reproduced exactly (0.3714/0.0025, 0.4187/0.0017); outputs = window CSV + block CSV + 50 state NPZs. **Grid-count discrepancy arbitrated (Option 1):** the spec's predeclared "90 runs" matched NEITHER pass's build; both sweep own-direction tiers per base sign = 50 runs; arbitrated that 50 IS the instrument (first-pass parity; classification logic written for the own-direction ladder); spec Section 5 corrected with full lineage. **Cross-direction extension (the 40 mixed-direction cells) NAMED, NOT TRIGGERED** - requires a design resolution before any such cell runs; Mike's call only. **READ COMPLETE - RESULT (Layer 2-arbitrated, Mike-concurred): Rule E bounded-A FAILS TO PRODUCE a substantive multiscale near-null under the 50-run own-direction design.** Zero LowLow in 450 post-burn-in windows; criterion 3 fails directly. Two-sided: the bound FULLY REPAIRED the first-pass amplitude-runaway / degeneracy / identical-tier pathology (no extinction, no saturate/extinct ring, zero degeneracy flags, all tiers distinct 10/10, channel non-inert; positive base = stable suppressed equilibria, g_E pinned negative, rho stepping 0.37->0.29 with tier; negative base = bounded block-cadence ring 0.36<->0.47 vs the first pass's 0.02<->0.63) - BUT the channel is SATURATED at every nonzero tier (bound_active 12/12 blocks, satflag 9/9 windows, |g_E| never below 2.2 vs threshold 1.472): the run tests a bounded SATURATED RELAY, not clean-responsive macro conditioning. Runaway repaired; relay behavior remains (L2 catch on L1's "fully repaired" overreach; second L2 catch: the relay is the CENTRAL interpretation, not a side note). L2 quantified the responsive window: saturation onsets at block-mean movement of only ~1.472*sigma_M (~0.0025-0.0037 rho), so the clean-responsive region for un-conditioned-sigma bounded-A is likely EMPTY at practical resolution - any future pass must RESCALE THE ARGUMENT, not shrink d tiers. **B/C standing STRENGTHENED, NOT TRIGGERED** (the residual failure is argument scale - exactly what B/C address; still freeze-discipline-gated, Mike's call only). Single z_mI~1.92 touch = completeness note only (single-axis; partner non-near-null; co-equal pair holds; not a near-miss). L4 not moved. Mechanism class NOT closed. Cross-direction extension unchanged NAMED-NOT-TRIGGERED. The standing HOLD on further wave-two seeding is unchanged.

Wave-two committed artifacts (oldest first):
- a925475 - Rule C DESIGN_CONTRACT.md placed (authoritative wave-two design document; frozen here).
- ff7a904 - pre-seeding resolution memo (PRESEEDING_RESOLUTION.md): resolves Comparator 0 observation protocol + kappa-grid construction rule; seeds nothing.
- 8abb917 - Comparator 0 script + results (apparatus-null floor).
- d150de7 - Rule C M2 first-pass map + findings (RULE_C_M2_FIRST_PASS_FINDINGS.md; floor-adjacent low/low, A-prime).
- 208b5c2 - Comparator epsilon weak-neighbor floor audit + findings (COMPARATOR_EPSILON_FINDINGS.md).
- Rule D arc: contract placed (4a8a688), opens resolved (84e4cd9), R3 amendment (2d2e281), R3 script committed pre-run (6857958), R3 run/CSV committed (c1ff920), run-execution ops log (cbb8277), bracket read + findings + ops log (0407e05).
- Rule E arc: anchor refresh to Rule D R3 rest (7c8041a), scoping result + horizon line (f8b067d), lag-realizability cleared - augmented scoping result + tau_rho diagnostic + ops log (e66a340), anchor refresh to lag-realizability (fd511eb), design contract placed (68041f6), contract ops log (5b77c1b), contract amended raw-block + open-6 (2f3ce0e), amendment ops log (df06646), opens 1-5 resolved (65a7265), opens ops log (80b307f), tau_rho-at-base verification cleared (4bf8aa0), implementation spec (59b4bd6), build (3738157), one-line path fix (3abf8dd), first-pass run outputs (ab51d36: window CSV + block CSV + 50 NPZs), gitignore read-helper (8e431ee), result-recording bundle (a56731c: RULE_E_FIRST_PASS_FINDINGS.md + run-and-read ops log + anchor refresh ba79726), bounded-gain design resolution to modified Candidate A (d0bd184: RULE_E_BOUNDED_GAIN_RESOLUTION.md + ops log + anchor refresh; proposal memo and L2 routing packet were route-first transit artifacts, superseded by the resolution), bounded-A implementation spec drafted + L2-reviewed (0ae9458: RULE_E_BOUNDED_GAIN_IMPLEMENTATION_SPEC.md + ops log + anchor refresh; L2 packet and pre-amendment draft were route-first transit artifacts, superseded by the reviewed spec), bounded-A build committed pre-run + L1-reviewed (4d48e57: c3_w2_rule_e_bounded.py + ops log + anchor refresh; Layer 3 build of the modified-A instrument, modification of the committed first-pass script; passed L1 build review), bounded-A seed complete (99c2661: one-line F3 path fix in c3_w2_rule_e_bounded.py + run outputs window CSV + block CSV + 50 NPZs + corrected spec Section 5 grid-count + ops log + anchor refresh), bounded-A findings bundle (0af058f: RULE_E_BOUNDED_A_FINDINGS.md + read ops log + anchor refresh; arc rests on the scoped negative), pattern synthesis + pure-read program opened (99a1649: WAVE_TWO_PATTERN_SYNTHESIS.md + ops log + anchor refresh), Reading-D pass-1 findings (8f4c270: READING_D_PASS1_FINDINGS.md + ops log + anchor refresh; B+D jointly confirmed), two-channel proposal (c3 prior commit: TWO_CHANNEL_PROPOSAL.md + ops log + anchor refresh; ordering probe named), two-channel ordering-probe design resolution (e176a7d: TWO_CHANNEL_ORDERING_PROBE_DESIGN_RESOLUTION.md + ops log + anchor refresh; CM-1 primary, probe named-not-triggered).
- plus per-session ops logs across the wave.

The frozen wave-two contract (cycle3/wave_two/DESIGN_CONTRACT.md, a925475) governs wave-two design; the Rule E design contract (cycle3/wave_two/rule_e/RULE_E_DESIGN_CONTRACT.md) governs Rule E design; the Rule E first-pass findings file governs the Rule E result. This anchor summarizes; the contracts and findings files govern on any discrepancy.

---

## WAVE-TWO RESULT STATE (the instruments, run and read)

**Comparator 0 (Lambda-only apparatus-null floor).** Calibrated on rho-lift and non-degeneracy ALONE; the two observables recorded as OUTPUTS, never tuning targets (the older "choose magnitude so meanI_z stays inside +/-2" wording is rejected and stays rejected). Result: i.i.d. Lambda-only floor reads near-null. This is the item-1 trivial-stochastic reference.

**Rule C M2 first-pass (central instrument).** Form fixed (contract Section 2.1): p_become = sigma(logit(p_Lambda) + kappa*(2q-1)), g(q)=2q-1 fixed centered, coefficient-null separable (kappa=0 IS Lambda-only, confirmed empirically). Anchors Lambda=0.20 and 0.40; common realized-contrast grid c in {0, +/-0.05, +/-0.10, +/-0.20, +/-0.35, +/-0.50, +/-0.60, +/-0.80}; seeds 42/137/256/1024/31415; wave-one window structure; 450 rows; 150 state NPZs; ran at parity. (Filename token = kappa coefficient, {kappa:+.4f} with -/+ -> m/p and . -> _; c=0.35 maps to kappa=0.7599, i.e. kp0_7599 / km0_7599.)
- **Reach satisfied (precondition met)** at BOTH anchors: negative kappa -> positive meanI / NEGATIVE persistence; positive kappa -> positive meanI / POSITIVE persistence; degenerate endpoint reached (0.20 c=+0.8 fires extinction_degenerate). Because reach is met, any low/low the map emits is NOT rule-forced-by-default.
- **LowLow location:** fires only at kappa=0 and the small flanking settings (|c| <= 0.10), centered on zero, monotone in |kappa|, confined to the smallest contrast settings, density-stable across both anchors.
- **Classification (A-prime, Layer 2-arbitrated):** floor-adjacent weak-coupling low/low band, NOT an away-from-floor substantive low/low candidate. The literal four-way bracketing reading (B) was explicitly rejected as necessary-but-not-sufficient: bracketing protects against a rule whose only behavior is low/low, but does not distinguish a null neighborhood around the OFF-state from a mechanism-produced near-null regime.
- **The discriminating test (correctly applied, not inferred):** NOT nonzero low/low width near zero (any continuous stochastic update has width under a hard LOW_Z_THRESH=2.0). The test is whether LowLow persists OUTSIDE the weak-coupling zone at responsive contrast (|realized_delta_p| >= 0.20). It does not - LowLow fires only through |c| <= 0.10 - so it reads floor-adjacent.
- **What would make a substantive reading live (carried for future passes):** a near-null pocket AWAY from zero; a near-null plateau past |c| >= 0.20; a non-monotone return to near-null AFTER signed structure; or density-specific structure one anchor shows and the other does not. The current map shows NONE of these. Rule C is NOT closed as a mechanism (first pass).
- **Map-texture cautions (downstream, not part of the central call):** at Lambda=0.20 high positive coupling gives positive meanI while rho collapses toward extinction - fence as degenerate-endpoint/boundary contrast, not a clean eligible signed regime; persistence-sign at matched positive coupling not identical across anchors in places.

**Comparator epsilon (weak-neighbor floor audit).** Separate-process object (a) per contract Section 6.2 - NOT a Rule C near-zero densification. Layer 2 confirmed object (a) and corrected A-prime's compressed "epsilon bounds the weak-coupling floor zone" wording to mean the separate-process floor audit. Additive form p_become = clip(p_Lambda + a*g(q), 0, 1), deliberately distinct from Rule C's logit-linear coupling; survival Lambda-only (neighbor-independent - fence holds); no LowLow flag (floor, not candidate). ceiling form |delta_p| <= epsilon*pbar_Lambda fixed before any z-score; epsilon=0.05 (Mike-ratified), choice (a) honor-the-contract-form (per-anchor unequal realized contrast). Per-anchor realized-contrast ceilings 0.0100 (Lambda=0.20) and 0.0200 (Lambda=0.40); additive coefficients a=0.00500/0.01000 hit the ceiling exactly, no clipping. 20 rows, 10 NPZs, parity passed.
- Result: floor stable to weak coupling - meanI-z and persistence-z centered near zero at both anchors, no systematic sign drift. Two isolated marginal exceedances over 2.0 (meanI Lambda=0.40 seed137 win75 at +2.36; persistence Lambda=0.20 seed31415 win0 at +2.09) - both non-replicated, no signed partner, finite-sample tail, inspect-not-halt.
- Binding two-ceilings constraint held: epsilon ceiling (separate-process perturbation scale) != Rule C near-zero boundary (Rule C realized-contrast axis); epsilon calibrates the LANGUAGE "floor-adjacent" only - it does NOT reclassify Rule C rows or move Rule C thresholds.
- Object (b) (Rule C near-zero densification) NOT motivated by this result (floor stable, not sensitive); survives as a separate optional follow-up at Mike's call, never called epsilon.

**Two-direction confirmation - stated precisely (carry this wording).** Comparator 0 establishes the lifted i.i.d. near-null floor. Comparator epsilon establishes that this floor remains near-null under a separate-process weak perturbation below the smallest nonzero Rule C contrast setting. This supports the phrase "floor-adjacent weak-coupling tolerance," but does NOT independently audit the Rule C |c| = 0.05 / 0.10 rows; those remain classified from the internal Rule C M2 geometry. (The "confirmed from two independent directions" shorthand is acceptable ONLY for "the floor itself is not assumed"; it overreaches if read as epsilon validating the Rule C near-zero band.) The epsilon ceiling (0.0100 / 0.0200) sits structurally BELOW the smallest Rule C contrast setting (|c| = 0.05). So epsilon confirms the floor at SUB-0.05 perturbation; it does NOT independently confirm the |c| = 0.05 / 0.10 Rule C rows. M2 Section 6 floated that epsilon, if it found |c|=0.05 above the weak-coupling ceiling, could make the 0.05/0.10 rows "more interesting" - but epsilon's contract-fixed ceiling was below 0.05, so that particular discrimination was never reachable by this epsilon. This does NOT break the A-prime classification: M2's floor-adjacent call rests on the |c| <= 0.10 band being centered-on-zero and monotone, not on epsilon. Read the "confirmed from two independent directions" language as confirmation at sub-0.05 perturbation, not at the Rule C 0.05/0.10 rows.

**Rule D R3 (turnover-limited activation).** Scoped negative for the predeclared R3 grid - see WHERE THINGS STAND above and RULE_D_R3_FINDINGS.md (governs). Signed-anchor precondition met both signs; no degeneracy anywhere; no LowLow_Nondegenerate_Candidate; co-equal pair never jointly near-null; guards do not bind. Arc RESTS (does not close; no endpoint reached). Single-axis finding: meanI turnover-sensitive, persistence sign turnover-robust; fenced from L4.

**Rule E (lagged-Lambda conditioning).** FIRST PASS RUN AND READ - scoped negative for the predeclared design; mechanism class NOT closed - see WHERE THINGS STAND above and RULE_E_FIRST_PASS_FINDINGS.md (governs). Positive base drives extinction at all nonzero alpha; negative base produces window-level lifted near-null flags that the block record exposes as a 25-tick saturate/extinct oscillation, rejected by the lag-dynamics guard as temporal cancellation. Failure mode over-driven feedback (g_E divides conditioned excursion by un-conditioned sigma_M ~0.0017, rails the logit), NOT inertness; L1's inert-channel hypothesis refuted by the block CSV and owned. Block-resolved recording strongly vindicated. L4 not moved. Bounded-gain follow-up DESIGN-RESOLVED to modified Candidate A (2026-06-27; smooth tier-specific bounded realized term; B/C held under formal freeze discipline; RULE_E_BOUNDED_GAIN_RESOLUTION.md governs), still UNSEEDED. The contract + opens resolution still govern design; the findings file governs the first-pass result; the bounded-gain resolution governs the selected follow-up construction.

**Net wave-two reading:** the near-null/near-null region is the apparatus floor and its weak-coupling neighborhood; the Rule C near-zero band sits within it; Rule C reaches both signed regimes and a degenerate boundary; Rule D R3 adds that independent turnover does not move the co-equal pair into joint near-null at responsive coupling under the tested grid; Rule E first pass adds that macro-conditioned lagged-Lambda under the unbounded standardized-gain construction rails (extinction one sign, block-cadence oscillation the other); Rule E bounded-A adds that the amplitude-bounded repair of that construction eliminates the runaway (no degeneracy, tiers distinct, channel non-inert) yet operates as a saturated relay at every tested tier - the un-conditioned-sigma standardized argument is structurally mis-scaled for conditioned dynamics on this substrate - and still produces no near-null. The wave-one lifted-implies-positively-structured pattern persists under bounded macro conditioning. No substantive away-from-floor low/low produced. No mechanism closed.

---

## THE SIX REFINEMENTS + TWO PRECISION NOTES (carry verbatim; never compress to five)

1. Rule B [1,2] correction: boundary / near-steady, Steady_State_Candidate True on 1/25 windows (seed 42 only), NOT earned-steady on 5/5.
2. Signed three-level framework: positive structured / near-null / negative anti-structured per axis, replacing the 2x2. Load-bearing move: distinguishing near-null from negative-anti-structured.
3. Rule A endpoint amendment: saturation tau_A {1,2,3}, collapse {4..8}; no locked-cluster regime under wave-one conditions; scope-limited.
4. LowLow_Nondegenerate_Candidate False on all 300 rows. Apparatus-level negative finding. Do NOT loosen LOW_Z_THRESH (= 2.0).
5. L4 boundary preserved: apparatus-level only.
6. [2,4] earned-but-heterogeneous: 5/5 seed-level occurrence, mixed window-count robustness (multi-window on 137/256/31415, single on 42/1024). NOT grouped with [2,3]. Reading lens: seed-level occurrence vs window-count robustness (window-count load-bearing).

Precision notes:
- Rule A dual-degeneracy: tau_A {4..8}, saturation_degenerate AND extinction_degenerate fire on the same rows; changes no finding. (Re-confirmed 2026-05-31: 125 both-flag rows in the CSV.)
- [3,4] non-eligible contrast: positive meanI, strongly POSITIVE persistence (~+86 z); persistence axis REVERSES sign under the unstable band; fenced as non-eligible / instability-coupled, NOT a signature. Eligibility gates signature-status, not measurement-validity.

---

## GATES - all cleared, do NOT reopen

- C3-ENV-001 - passed-L1. Canonical env: Python 3.14.4, Mesa 3.5.1, numpy 2.4.4. Use & with absolute path to Activate.ps1; verify $env:VIRTUAL_ENV before trusting.
- C3-CTL-001 - valid-L2.
- C3-SS-001 - valid-L2.
- C3-OBS-001 - valid-L2, apparatus-level closure, synthesis cluster fully committed.

Topology LOCKED: 50x50, Moore radius 1 (8-neighbor), toroidal.
Co-equal pair (Psi_meanI_state, Psi_persistence_I) holds; neither named theoretical Psi.
The L4 ontological question rides forward UNSETTLED: which observable, if either, IS Regime-II coherence. Held for Mike / Layer 1. NOT to be resolved by statistical robustness or by which observable a script names. (The Rule D single-axis finding and the Rule E sign-asymmetric over-driven finding both touch it but neither resolves it; the Rule E observable-feedback exclusion protects it.)

---

## CONTRACT CORE (pointer - wave-two contract at a925475 governs; Rule E contract governs Rule E)

- Load-bearing question: NOT "which rule produces low/low," but whether a lifted, non-degenerate activation-density regime can be produced WITHOUT per-tick spatial organization or clustered persistence being structurally forced by the rule - is low/low a genuine earned regime in a mechanism family ALSO capable of producing non-low/low signed regimes.
- Rule C: signed coupling kappa on the BECOMING-ACTIVE transition (NOT survival). Form fixed (above). Positive kappa contagion-dominant, negative divergence-dominant. Bounded-linear form held as RESERVED AUDIT only.
- Construction fences (binding): kappa-sign-through-zero is a SUBSTRATE parameter, NEVER an operationalization of the point(s) at which mu(rho)=0. No kappa-on-global-rho, no dynamic centering against rho, no hidden feedback in the Lambda baseline, no piecewise sign-specific form. Survival must NOT acquire neighbor-count/range dependence (or Rule C collapses toward Rule B). theta_turnover (Rule D) is likewise a substrate parameter, never a coherence operationalization, exogenous and neighbor-independent. Rule E lagged-Lambda conditioning is admissible ONLY as a slow block-lagged conditioning of locally-configured Lambda on the becoming-active channel (separation of timescales cleared at tau_rho=1.66 un-conditioned / 2.86 at the c=+/-0.35 coupled base / 25-tick block); it is NOT dynamic centering, NOT kappa modulation, NOT observable feedback, and conditioned Lambda does NOT leak into survival or turnover. (First pass run: the standardized-gain realization of this admissible form railed - see the Rule E findings; the admissible form stands, its first-pass gain construction does not.)
- Contrastive success criterion (pessimistic-on-passing): low/low earned only if the design discriminates trivial-stochastic / rule-forced / degenerate / substantive-candidate, AND a substantive low/low is BRACKETED by non-low/low signed regimes; the sweep must demonstrably REACH the signed regimes or any low/low is rule-forced. Large-|kappa| is a degeneracy probe, NOT the evidentiary center. (Applied in M2: reach demonstrably met; low/low only floor-adjacent. Applied in Rule D R3: signed lower anchor met; no joint near-null bracket emerged. Applied in Rule E first pass with the lag-dynamics guard and the inert-channel guard: the negative-base window near-null was a lag-dynamics artifact, rejected by the guard; criterion-3 for a signed base - away-from-the-alpha=0-baseline - was not satisfiable on railed dynamics.)
- Comparator split: Comparator 0 (Lambda-only floor, calibrated on rho-lift alone, near-null an OUTPUT) + Comparator epsilon (weak-neighbor audit, magnitude bounded by predeclared realized-contrast ceiling). Both separate from Rule C. (Both run.)
- WEAK-FORM Rule B prior (PRESERVE - do not harden): Rule B structural overproduction of positive meanI when lifted is the best WORKING PRIOR, not proven by construction. Wave-one CSV read is consistent with it but does not convert it to proof. Door to Rule B stays open. UNTOUCHED by wave-two results (Rule D R3 and Rule E first pass included).
- Wave-one CSV finding feeding design (primary source): of 97 non-degenerate-lifted rows, Psi_meanI_state_z positive on ALL 97 (min ~139), zero near-null, zero negative - wave one has NO lifted-near-null meanI region. This SHARPENS the diagnostic target; it does NOT predict whether a rule can reach it. Hardening the absence into a prediction would collapse the mechanism-class distinction wave two tests.

---

## ON THE HORIZON (named, not blocking; none motivated by current results)

- **Rule E bounded-A - READ COMPLETE; ARC RESTS on a scoped negative (2026-07-01; RULE_E_BOUNDED_A_FINDINGS.md governs).** Amplitude runaway repaired; responsive conditioning not achieved (saturated relay at every nonzero tier). Mechanism class NOT closed. Named-not-triggered follow-ups, all Mike's call: **B/C or a rescaled-argument bounded variant** (standing STRENGTHENED by this result - the residual failure is argument scale, exactly what B/C address; seven-condition freeze discipline gates any opening; "bounded-A with smaller d" is NOT the indicated follow-up per the L2 responsive-window analysis); **cross-direction extension** (40 mixed-direction cells; requires a design resolution first); and the pattern-level synthesis - now DONE and committed (WAVE_TWO_PATTERN_SYNTHESIS.md governs); the Reading-D pure-read PASS 1 IS COMPLETE AND ARBITRATED (READING_D_PASS1_FINDINGS.md governs: B and D jointly confirmed; Reading A bounded near-empty at the tested base); B/C HELD under that bound; **the open arbitration is wave two's CONTINUE-OR-REST (Mike's) - now a three-way fork: rest / B-C responsive pass (bounded near-empty) / the TWO-CHANNEL ORDERING PROBE (named-not-triggered; TWO_CHANNEL_PROPOSAL.md governs)**; manuscript back-flow to v1.5 named as parallel work.
- Seeding of any further wave-two probe: Mike's call to open. DECIDED 2026-06-02 (Layer 2-concurred): HOLD - no new seed; wave-two first pass rests on A-prime. Future-pass probe requires a separately named theoretical target before opening.
- **Rule D stronger-turnover endpoint extension - NAMED, NOT TRIGGERED (2026-06-03).** Because the R3 ladder did not reach a floor or degeneracy endpoint by theta_turnover=0.50, a stronger-turnover endpoint extension remains AVAILABLE. NOT opened. If such a follow-up changes from R3's additional-churn-hazard form into an absolute-lifetime / stronger lifetime-control variant, it is an endpoint-extension VARIANT - opened and named as such, NOT run silently as "the same R3 grid."
- Held / optional follow-ups (Mike's call): Rule C near-zero densification (object b) - held optional Rule C near-zero boundary-refinement; NOT Comparator epsilon; NOT necessary for A-prime; useful only to locate the |c|=0.10-to-0.20 transition. Guardrail: a higher-ceiling comparator perturbation ladder is NOT object (b). Reserved audits Lambda=0.30 / 0.50; grid densification (+/-0.025 / +/-0.92).
- Layer 2 epsilon update: CLOSED (2026-06-02). No A-prime change; no seeding.
- Layer 2 direct GitHub fetch was NON-FUNCTIONAL in the recent 2026-06-12 sessions (cache/fetch errors on repo page, raw URL, API endpoint); canonical content must be pasted/uploaded to Layer 2 until fetch works. The repo-access note (raw URLs / commit-pinned URLs / API fallback) remains valid for when it does.
- L4 ontological question rides forward unsettled.
- Ops-log backfill: no ops_log entry for the 2026-05-28 wave-one session or the 2026-05-30 synthesis session; draftable from ABM 28 / ABM 29 scrollback Mike holds if wanted.
- Layer 3 v1 code gap: OBS001_10 carries a flagged not-recoverable gap (UUID 46246e1e-...).
- The _r3 filename suffix on the Rule D script/CSV/findings: retained for amendment-history honesty; final naming is Mike's call.

---

## HOW MIKE WORKS (binding)

- Execution channel is Mike only. You draft and route; he runs.
- PowerShell: ONE command per copy-pane, wait for output before the next. Do NOT batch state-changing commands. Pipe ALL commands through Set-Clipboard (including silent ones like Move-Item / git add); structured objects through Out-String | Set-Clipboard; empty clipboard IS the confirmation for no-output commands. The if/else | Set-Clipboard scope bug: pipe $msg AFTER the if/else, not inside a branch.
- If the PS prompt prefix gets copied and breaks the parser, re-send the command alone. If a command pastes to chat instead of running (echoed back, not its output), re-send it.
- Downloaded .ps1 helpers are blocked by execution policy (not digitally signed); run them via powershell -ExecutionPolicy Bypass -File. Calling python on a downloaded .py is NOT blocked (signing applies to .ps1 only).
- git push prints progress to stderr, rendered RED even on success - the "<old>..<new>  main -> main" line is the success confirmation; do not read red as failure.
- Keep commit messages SHORT (single -m line) - long here-strings hit PS paste limit and hang at >>. (Clear a stuck >> with Ctrl+C.)
- File edits to canonical docs: deliver finished files via create -> present_files -> Mike Copy-Item/Move-Item (full-file overwrite, -Force for existing), OR targeted literal .Replace() edits via a helper file read from disk for scattered changes (confirm each old-string count = 1; match single lines, not multi-line spans). Match the ORIGINAL on-disk file's BOM state and trailing-whitespace state (per-file, not project-wide).
- VERIFY BYTE-STATE AT DESTINATION (BOM + ending + SIZE) after every placement. BOM check MUST pair with SIZE check - Test-Path True does NOT confirm the right file landed; only size discriminates a stale/wrong-source copy. Expect "(1)"/"(2)"/"(3)" suffixes in Downloads when same-named files already sit there; list Downloads with size+timestamp and copy from the correct source by SIZE, not bare name. The download channel intermittently fails for large or same-named files (whole 2026-06-12 run session): present under a FRESH unique transit filename, confirm it lands by size, then Copy-Item to the canonical destination name; never place from a stale same-named copy. [System.IO.File]::ReadAllBytes needs an ABSOLUTE path (Resolve-Path) - a repo-relative path resolves against process cwd and reads size 0. (For canonical .md, also an interior-byte check is available - count clean em-dash E2 80 94 sequences - to distinguish on-disk corruption from a console-decoding display artifact; em-dash mojibake on Get-Content read-back can be display-only. Prefer ASCII in canonical docs to avoid the ambiguity.)
- Per-file BOM/trailing conventions: test record / held-inputs / anchor / ops-log / routing-archive / wave_two contract + findings + Rule D contract + resolution memo + Rule E scoping result + Rule E design contract + Rule E opens resolution + Rule E first-pass findings = BOM-less, single trailing LF. Registry = BOM present, trailing blank line. Verify against the on-disk file before generating any overwrite.
- Relative paths: PS location for Test-Path, but process cwd for Python/.NET I/O. cd into repo root (or Resolve-Path to absolute) before any Python/.NET I/O.
- Stage with EXPLICIT paths, never git add -A. Verify git diff --cached --name-only before EVERY commit.
- Notes to other layers delivered as separate copy-pastable files, one per destination, no surrounding commentary inside the file. Fresh Layer 2 context needs SELF-CONTAINED notes (build observables, null convention, apparatus from scratch; genuine question first; invite Layer 2 to flag conflicts). Continuing Layer 2 context does not need the apparatus rebuilt. Carrying a contract or memo to a layer: concatenate note + full text into ONE file (no "[paste here]" placeholder) - used 2026-06-12 when L2 fetch was down.
- Layer 3 must never declare L2 clearance in its own voice. Frame Layer 3 deliverables as "what the run will produce / has produced." Layer 3 may be consulted on realizability / recording feasibility (scope-fenced: not admissibility, not criteria, not architecture); its input returns through Layer 1 synthesis and Mike's arbitration.
- Per-seed flag aggregation, not per-band means. Confirming mean_rho is NOT confirming a flag pattern.
- CSV is primary source over the anchor's restatement. Fill canonical records from primary source. Filename tokens decode from the run script, not from inference (Rule C kappa token = {kappa:+.4f}, m/p/_; c=0.35 -> kp0_7599/km0_7599).
- Honest pushback expected, including when Mike pre-concedes fault or defers authority. Pessimistic-on-passing applies to your own scripts and session-state reads, not just probes. Watch the symmetric risk: do not converge by deferring to a clean Layer-2 argument any more than by your own framing - sandbox/analytic agreement is a prediction, not a confirmation. (Caught 2026-06-03: Layer 1's "signed through to the endpoint" overreach corrected by L2. Caught 2026-06-05: Layer 1's lag upper-bound "shorter than the SS window" framing corrected by L2 to cadence-permits-variation; and a skipped Downloads size-check caught by destination byte-state verification. 2026-06-12: Layer 1 surfaced the coupled-base tau_rho gap itself before any spec; and at the read, Layer 1's inert-channel hypothesis was refuted by the block CSV - pessimistic-on-passing on its own read, caught by the block instrument.)
- When the next step is clear, PRODUCE it - do not stall on "want me to X or hold?". Ask only when a real fork needs Mike's arbitration.
- Surface scope-steps as FORKS, do not fold them in silently.
- Synthesis is the work; convergence is a phenomenon that sometimes occurs. Watch soft convergence and the framing asymmetry (Claude frames, the other layers engage). Genuine convergence is recorded AS genuine (e.g. 2026-06-12 F1 raw-block z-score call).
- Session-end commit of the operations log is a standing rule (operations_log/YYYY-MM-DD_slug.md; BOM-less, single trailing LF; H1 + bold metadata block + sections + closing "Drafting partner" line). If a session commits canonical files, the ops log commits alongside, NOT deferred. Pure pointer/hygiene edits carrying no new arbitration do not require a separate ops log (Mike's call).

---

## VOCABULARY (binding, including at wrap-ups and celebratory moments)

- rho = activation density
- Psi_meanI_state, Psi_persistence_I = the two co-equal observables; neither named theoretical Psi
- Lambda (or Lambda_A, Lambda_B) = structural conduciveness (never "terrain favorability")
- agents ACT (no decision / optimization / utility / cognitive language)
- "the point(s) at which mu(rho) = 0" (never rho_c); the Rule C kappa = 0 sweep point and theta_turnover are SUBSTRATE parameters, NEVER an operationalization of the point(s) at which mu(rho) = 0
- theta_turnover = Rule D's additional independent churn hazard (ASCII in code/CSV/filenames). NOT eta (eta(t) is the shared noise term, HKB/Haken lineage; reuse would collide). s_Lambda = Rule C's retained neighbor-independent Lambda-survival probability.
- Rule E (lagged-Lambda conditioning) = the macro-conditioned local mechanism; slow block-lagged conditioning of locally-configured Lambda on the becoming-active channel, canonical 25-tick non-overlapping block-lag. alpha = the macro-conditioning coefficient (SUBSTRATE parameter; alpha=0 recovers the un-conditioned base exactly; never an operationalization of mu(rho)=0). M = completed prior-block mean rho (the macro block signal); M_ref / sigma_M = pre-registered constants from the un-conditioned base (split per sign: M_ref_plus=0.3714 / sigma_M_plus=0.0025; M_ref_minus=0.4187 / sigma_M_minus=0.0017). g_E(M) = (M - M_ref)/sigma_M, the standardized macro-gain term. tau_rho = macro activation-density autocorrelation time (measured 1.66 ticks un-conditioned kappa=0; 2.86 ticks at the c=+/-0.35 coupled base). Block-lag is load-bearing, not point-lag. First-pass outcome language: the standardized-gain construction RAILED (over-driven, not inert) - extinction (positive base) / block-cadence saturate-extinct oscillation (negative base); apparatus-level, NOT an L4 result.
- "per-cell active-state values" (never "field" as explanatory mechanism)
- "earned steady-state window" - the SS-001-validated criterion
- LowLow_Nondegenerate_Candidate - apparatus-level flag; NEVER Regime_II in any identifier, comment, column, or prose
- candidates PRODUCE or FAIL TO PRODUCE (never "confirm" / "demonstrate" / "prove" / "validate"); a run RESTS on a scoped negative (never "REFUTES")
- "fraction of active neighbors" is fine (local); never "fraction of the population"
- The two SS-001 flags Steady_State_Candidate and Lifted_Activation_Candidate remain INDEPENDENT at every stage
- The Rule B earned-window signature is POSITIVE meanI / NEGATIVE persistence. NOT "high meanI / low persistence."
- Reading lens: seed-level occurrence vs window-count robustness - window-count is the load-bearing measure of band robustness.

---

## COLD-START GROUNDING SEQUENCE (next instance)

Ground from this anchor + the contracts + the findings/resolution files, NOT from any instantiation prompt's where-things-stand summary (prompts go stale; the repo wins). One command per copy-pane, Set-Clipboard everything.
1. git --no-pager log --oneline -1 - expect a56731c or later. If later, a session intervened; read the newest committed ops log / findings before trusting this anchor's result state.
2. status + presence checks (Rule E first-pass findings cycle3/wave_two/rule_e/RULE_E_FIRST_PASS_FINDINGS.md + bounded-gain resolution cycle3/wave_two/rule_e/RULE_E_BOUNDED_GAIN_RESOLUTION.md + bounded-A findings cycle3/wave_two/rule_e/RULE_E_BOUNDED_A_FINDINGS.md + pattern synthesis cycle3/wave_two/WAVE_TWO_PATTERN_SYNTHESIS.md + pass-1 findings cycle3/wave_two/READING_D_PASS1_FINDINGS.md + two-channel proposal cycle3/wave_two/TWO_CHANNEL_PROPOSAL.md + two-channel ordering-probe design resolution cycle3/wave_two/TWO_CHANNEL_ORDERING_PROBE_DESIGN_RESOLUTION.md + bounded-A implementation spec cycle3/wave_two/rule_e/RULE_E_BOUNDED_GAIN_IMPLEMENTATION_SPEC.md + bounded-A build script cycle3/wave_two/rule_e/c3_w2_rule_e_bounded.py (seeded 2026-07-01; run complete, READ COMPLETE, arc rests) + design contract RULE_E_DESIGN_CONTRACT.md + opens resolution RULE_E_OPENS_RESOLUTION.md + scoping result + tau_rho diagnostic + tau_rho_at_base_verify.py present; Rule E first-pass run outputs window CSV + block CSV + 50 NPZs present; bounded-A run outputs c3_w2_rule_e_bounded_windows.csv + c3_w2_rule_e_bounded_blocks.csv + 50 c3_w2_rule_e_bounded_states NPZs present; Rule D contract / resolution / R3 findings present; wave-one substrate counts 17 archive / 60 NPZ).
3. Read this anchor.
4. Read cycle3/wave_two/DESIGN_CONTRACT.md (wave-two design), cycle3/wave_two/rule_e/RULE_E_DESIGN_CONTRACT.md + RULE_E_OPENS_RESOLUTION.md (Rule E design), and for Rule D cycle3/wave_two/rule_d/RULE_D_DESIGN_CONTRACT.md (R3-amended) + D_OPENS_RESOLUTION.md (R3-corrected).
5. Read the findings / scoping / resolution / spec files: RULE_C_M2_FIRST_PASS_FINDINGS.md, COMPARATOR_EPSILON_FINDINGS.md, PRESEEDING_RESOLUTION.md, RULE_D_R3_FINDINGS.md, RULE_E_FIRST_PASS_FINDINGS.md, RULE_E_BOUNDED_A_FINDINGS.md, WAVE_TWO_PATTERN_SYNTHESIS.md, READING_D_PASS1_FINDINGS.md, TWO_CHANNEL_PROPOSAL.md, RULE_E_BOUNDED_GAIN_RESOLUTION.md, RULE_E_BOUNDED_GAIN_IMPLEMENTATION_SPEC.md, RULE_E_SCOPING_RESULT.md (the result state above is a summary of these; they govern).
6. Confirm grounding, then WAIT for Mike's direction.
