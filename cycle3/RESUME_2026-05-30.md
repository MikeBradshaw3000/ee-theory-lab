# RESUME - Cycle 3, wave-one synthesis cluster items 1-3 committed; routing archive deferred

**Supersedes:** `RESUME_2026-05-28.md` (wave-one A+B closed at apparatus level, full synthesis cluster queued). That anchor described the entire synthesis cluster as pending. This one records that synthesis-cluster items 1-3 are now committed, folds in three primary-source corrections the 2026-05-28 anchor's five-amendment compression did not carry, and names the routing archive (item 4) plus this anchor refresh as the only remaining synthesis work.

**Written:** end of the 2026-05-30 session.

---

## Orientation - where we left off

The wave-one (C3-OBS-001, Rules A + B, locked 50x50 Moore-1 toroidal) synthesis cluster is **two-thirds committed**. Items 1-3 (test record, registry advance, held-inputs amendment) are on disk and committed. Item 4 (the routing archive) is deferred to a fresh session by Mike's call, to be built from verbatim 2026-05-28 scrollback rather than degraded reconstruction (the recovery test this session showed `conversation_search` returns the anchor text and artifact fragments but NOT the 16 verbatim artifact bodies).

The 2026-05-30 session did the synthesis work the 2026-05-28 anchor queued, and the per-seed flag aggregation (a carry-forward discipline) surfaced two corrections the prior interpretation had not carried. Both were routed to Layer 2 and concurred. The canonical record now carries SIX refinements plus TWO precision notes, not the anchor-28 five-amendment compression.

## Commits this session

- **dd06f38** - C3-OBS-001 wave-one A+B substrate (script + CSV + 60 NPZs), TEST_RECORD_C3-OBS-001.md (new), CYCLE3_TEST_REGISTRY.md (advanced: C3-OBS-001 row added at valid-L2, brief subsection added, "not yet seeded" note carved to exempt OBS-001). 64 files, 791 insertions, 2 deletions.
- **71e51b9** - PROBE_DESIGN_INPUTS_HELD.md held-inputs wave-one amendment (Section 7 appended; Sections 1-6 preserved verbatim). 1 file, 47 insertions.

HEAD at **71e51b9**. Working tree clean. origin/main: push status not checked this session - verify and push if intended.

## Gates - unchanged

- **C3-ENV-001 - passed-L1.** Canonical env: Python 3.14.4, Mesa 3.5.1, numpy 2.4.4. Venv at `C:\Users\vkz244\EE_Theory_Lab\venv` (workspace top-level, NOT repo root).
- **C3-CTL-001 - valid-L2.** Unchanged.
- **C3-SS-001 - valid-L2.** Unchanged.
- **C3-OBS-001 - valid-L2 (registry), apparatus-level closure.** Committed this session. Highest level cleared in the record is described as apparatus-level substrate observation; the registry carries the ladder term valid-L2 / "L2 measurement validity" by convention (registry is a fixed-vocabulary status board; the record holds the nuance).

## The six refinements + two precision notes now in the canonical record

The 2026-05-28 anchor compressed the Layer 2 verdict to five amendments. The committed record carries those five PLUS the [2,4] correction, PLUS two Layer-1 precision notes. Enumerated so the next instance does not regress to the five-amendment compression:

1. **Rule B [1,2] correction** (anchor-28 amendment 1). Boundary / near-steady: Steady_State_Candidate True on 1/25 windows (seed 42 only), NOT earned-steady on 5/5 as Layer 3 first stated.
2. **Signed three-level framework** (anchor-28 amendment 2). Held-inputs Section 7: positive structured / near-null / negative anti-structured per axis, replacing the 2x2 high/low. The load-bearing move is distinguishing near-null from negative-anti-structured.
3. **Rule A endpoint amendment** (anchor-28 amendment 3). Saturation at tau_A in {1,2,3}, collapse at {4..8}; no locked-cluster regime under wave-one conditions; scope-limited.
4. **LowLow_Nondegenerate_Candidate False on all 300 rows** (anchor-28 amendment 4). Apparatus-level negative finding. Do NOT loosen LOW_Z_THRESH (= 2.0).
5. **L4 boundary preserved** (anchor-28 amendment 5). Apparatus-level only; which observable IS Regime-II coherence held for Mike / Layer 1.
6. **[2,4] earned-but-heterogeneous** (NEW this session; surfaced by per-seed aggregation, routed to Layer 2, concurred). 5/5 seed-level occurrence but mixed window-count robustness: multi-window on seeds 137/256/31415, single-window on 42/1024. NOT grouped with [2,3] as uniformly robust. Carries the named reading lens: seed-level occurrence vs window-count robustness (window-count is the load-bearing measure because the apparatus is about earned windows).

Two precision notes (Layer-1, in the record):
- **Rule A dual-degeneracy** (note-as-observed). For tau_A in {4..8}, both saturation_degenerate AND extinction_degenerate fire on the same rows: Tick_to_Freeze and Tick_to_Zero coincide, so a grid that zeroes on the same tick it stops changing is both "frozen" and "extinct." Changes no finding.
- **[3,4] non-eligible contrast** (NEW this session; routed to a fresh Layer 2 context, concurred). [3,4] shows positive meanI and strongly POSITIVE persistence (~+86 z mean) - opposite persistence sign from the eligible bands. Fenced from the earned-window signature: recorded as non-eligible contrast / instability-coupled observation, NOT a signature. Layer 2's principle: eligibility gates signature-status, not measurement-validity. Design relevance: the persistence axis REVERSES under the unstable band, which is why the signed three-level framework is needed - the 2x2 could not represent it. This is a documented input to wave-two design.

## Wave-one substantive findings (apparatus-level; no L4) - condensed

- Rule A non-discriminating across tau_A in {1..8}. Saturation {1,2,3} (tau_A=3 transient structure first, Tick_to_Freeze 41-57), collapse {4..8}. No locked-cluster regime. Both prior pictures (primary-source "high tau", Layer 3 "intermediate tau") not produced under wave-one conditions; scope-limited.
- Rule B eligible-or-near-eligible bands share positive meanI / negative persistence (anti-clustered, NOT near-null). [2,3] robust; [2,4] earned-but-heterogeneous; [1,2] boundary; [3,4] non-eligible / unstable (positive-persistence contrast, fenced).
- LowLow_Nondegenerate_Candidate False on all 300 rows. The wave-one low/low candidate signature did not appear under either rule.
- The co-equal pair separated under Rule B (opposite signs in eligible bands) - apparatus-level separation, NOT a naming of theoretical Psi.

## Remaining synthesis work (next session)

1. **`cycle3/obs_001_routing/`** - the routing archive, item 4, the ONLY load-bearing remaining synthesis piece. 16 artifacts + README following the `ss_001_routing/` precedent. Build from verbatim 2026-05-28 scrollback (Mike has that chat). The 16 files are enumerated in RESUME_2026-05-28.md Section 6 item 4 (titles/descriptions recoverable; bodies need scrollback). Commit as its own commit, explicit paths.
2. **This anchor (`RESUME_2026-05-30.md`)** - commit it. It was written end-of-session-2026-05-30; if not yet committed when the next instance reads it, commit it. Confirm HEAD first.

## Wave two - separate decision, not synthesis

Still queued, still NOT part of the synthesis cluster. Questions surfaced by wave one (carry from anchor-28, plus this session's sharpening):
- Rule C (deferred from wave one) as the candidate for the lifted low/low signature?
- Does Rule B's range-bound family structurally produce too much per-tick spatial organization whenever it sustains lifted rho?
- A Lambda-driven independent-activation / weakly-coupled-stochastic rule as a substrate-side low/low generator (Layer 2 flagged)?
- Does the signed three-level framework open probe-design possibilities the 2x2 concealed? (The [3,4] axis-reversal is the documented case that it does.)
- Finer-grained tau_A around the saturation/collapse boundary (tau_A = 3 -> 4)?

Opening wave-two design is Mike's call AFTER the routing archive closes. Do NOT seed wave-two probes during the archive work.

## Carry-forward cautions

### New / reinforced this session

- **BOM check MUST pair with size check.** This session a registry Move-Item appeared not to land - BOM and trailing-byte matched (original and amended shared both) but SIZE was the original 6967, not the amended 9195. Only the size discriminated. The move had in fact not run (command pasted to chat, not terminal); re-running landed it (verified 9195). BOM-alone would have falsely passed. Always read Length, and know the expected byte count before verifying.
- **Per-seed flag aggregation earns corrections.** Aggregating Steady_State_Candidate True-counts per (rule, lambda_id, seed) - not per-band means - is what surfaced the [2,4] heterogeneity. The same discipline that caught the [1,2] miss caught [2,4] one band over. Aggregate boolean flag counts per band per seed; do not spot-check means.
- **CSV is primary source over the anchor's restatement.** Pulling z-scores and tick figures from the CSV (not the anchor's findings summary) surfaced the [3,4] positive-persistence sign-flip the anchor had not carried, and confirmed the dual-degeneracy mechanism numerically. Fill canonical records from primary source, not the prior anchor's compression.
- **Relative paths resolve against PS location for Test-Path but against process cwd for Python/.NET.** A pandas read failed FileNotFound because PS location was the workspace root while the CSV is one level down in the repo. `cd` into the repo root (or Resolve-Path to absolute) before Python/.NET I/O.
- **Bracket-named files and git pathspec.** The Rule B NPZs carry literal brackets (`...states_B_[1,2]_42.npz`). Brackets are glob metacharacters in git pathspec. Staging the containing DIRECTORY (`cycle3/data_out/`) as one pathspec sidesteps the glob - git takes a directory path literally. Worked clean this session (all 20 bracket files committed).
- **Fresh Layer 2 context needs self-contained routing notes.** One [3,4] question went to a brand-new ChatGPT chat with no project context. The note built the observables, the null convention, and the earned-window apparatus from scratch, put the genuine measurement-validity question first, and explicitly invited Layer 2 to flag conflicts with my framing rather than absorb them. Fresh Layer 2 engaged on its own ground (named the seed-level-occurrence vs window-count distinction and the eligibility-gates-signature-status principle unprompted) - the framing-asymmetry guard held.

### Carries from prior anchors (still relevant)

- Cold-start grounding: manifest check one-liner -> read THIS anchor first -> kit -> CTL/SS/OBS records -> Cycle 2 closure.
- One PS command per copy-pane; wait for pasted output. `| Set-Clipboard` on ALL commands; `Out-String` for MatchInfo; empty clipboard IS confirmation for no-output commands. The `if/else | Set-Clipboard` scope bug: pipe `$msg` after the if/else, not inside a branch.
- Keep commit messages short (single -m line) to avoid the `>>` continuation hang.
- File delivery: create -> present_files -> Mike Move-Item (full-file overwrite, `-Force` for existing). Verify byte-state AT DESTINATION (BOM + ending + SIZE) before trusting placement.
- Per-file BOM/trailing conventions: test record / held-inputs / anchor = BOM-less, single trailing LF. Registry = BOM present, trailing blank line. Verify against the on-disk file before generating any overwrite.
- Stage with EXPLICIT paths, never `git add -A`. Verify `git diff --cached --name-only` before every commit.
- Your sandbox runs are predictions, never results of record. Mike's run on the captured venv is the record.
- When the next step is clear, produce it; don't stall on "want me to X or hold?"
- Synthesis is the work; convergence is a phenomenon. Watch soft convergence and framing asymmetry. Sandbox/analytic agreement is a prediction, not a confirmation.

## Vocabulary (binding, including at wrap-ups)

- rho = activation density
- Psi_meanI_state, Psi_persistence_I - the two co-equal observables; neither named theoretical Psi
- Lambda (or Lambda_A, Lambda_B) = structural conduciveness (never "terrain favorability")
- agents act (no decision / optimization / utility / cognitive language)
- "the point(s) at which mu(rho) = 0" (never rho_c)
- "per-cell active-state values" (never "field" as explanatory mechanism)
- "earned steady-state window" - the SS-001-validated criterion
- `LowLow_Nondegenerate_Candidate` - apparatus-level flag; never `Regime_II`
- candidates produce or fail to produce (never "confirm" / "demonstrate" / "prove" / "validate")
- "fraction of active neighbors" is fine (local); never "fraction of the population"
- The two SS-001 flags `Steady_State_Candidate` and `Lifted_Activation_Candidate` remain independent at every stage
- The Rule B earned-window signature is **positive meanI / negative persistence** (dynamic spatial organization with anti-clustered persistence). NOT "high meanI / low persistence." The held-inputs framework distinguishes positive / near-null / negative on each axis.
- New reading lens: **seed-level occurrence vs window-count robustness** - window-count is the load-bearing measure of band robustness.

Vocabulary discipline holds throughout, including at the next instance's first substantive response after instantiation.
