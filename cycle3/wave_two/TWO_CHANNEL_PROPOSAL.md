# Two-channel proposal (common-mode vs differential influence) - recorded as live proposal pending its instrument

**Status: THEORETICAL PROPOSAL, Layer 2-reviewed 2026-07-02, Mike-arbitrated. NOT a finding; NOT a synthesis amendment; reclassifies NOTHING. The floor's committed classification stands.** Names one instrument (below), not triggered. Governing on the proposal's standing; READING_D_PASS1_FINDINGS.md and WAVE_TWO_PATTERN_SYNTHESIS.md govern the committed results it builds on.

## The proposal (Mike; architect-originated)

"Local" in the pair-level mechanism is metric-agnostic: it requires only overlapping exposure neighborhoods, in whatever space adjacency lives (Mike's reading: proximity in v,c,r-configuration space, not geography). A genuinely COMMON-MODE channel - a shared signal every agent reads (Narrative as the paradigm case; committed status: one likely effective contributor to mu(rho), not sole) - has no exposure neighborhood and lifts propensities together rather than differentially. Proposed cascade mapping: first transition = common-mode lift (lifted, incoherent, driver-global); second transition = the differential channel crossing into dominance (organization emerges). Wave two's negatives then read as half of a two-part structure: differential channels organize whatever they lift (measured, pass 1); common-mode channels lift without organizing.

## Layer 2 review (attack requested and delivered; modifications binding)

- **Falsifiability ruling (decisive):** reinterpreting the kappa=0 floor as "the Regime-II signature" is RELABELING on existing data - at static uniform Lambda, common-mode-driven and i.i.d. are observationally identical. The proposal enters the record ONLY through its instrument. The ORDERING PREDICTION survives as genuinely falsifiable and risky: under common-mode-conditioned lift with swept differential coupling, coherence onset should track the differential channel's strength at roughly constant lifted rho. Temporal-imprint evidence (a dynamic common driver marking global rho with its timescale while spatial organization stays at floor) is necessary supporting evidence, not sufficient alone.
- **Covariance correction (taken):** "additive common shift leaves Cov(p_i,p_j) unchanged" is first-order only. Through the logit, a common-mode term modulates every cell's local slope and RESCALES the existing differential covariance. Corrected prediction (sharper): organization pressure scales with the DIFFERENTIAL channel's strength, the common channel entering only multiplicatively through the slope; the rescaling at kappa ~ 0 is second-order small against the measured 0.38.
- **Fence condition (binding verbatim on any design resolution):** kappa is swept as a SUBSTRATE parameter; "dominance" appears ONLY in the read as an outcome description, never in the construction. Otherwise the instrument breaches the kappa-not-an-operationalization-of-mu(rho)=0 fence.
- **Saturation constraint (L2 catch; binding):** near saturation the logit slope collapses and the two channels' distinctions compress; the instrument's rho operating range must sit below that compression or the ordering read is confounded.

## Named instrument - TWO-CHANNEL ORDERING PROBE (NAMED, NOT TRIGGERED)

Common-mode-conditioned lift (admissible Rule E class, uniform conditioning; observable-feedback fence intact) with kappa swept subcritical-through-organizing as a substrate parameter; tests the risky ordering prediction; carries the corrected functional form and the rho-range constraint. A design resolution precedes any opening; opening is Mike's call only. L4 untouched throughout (driver types and signatures, apparatus-level; neither observable named theoretical Psi).

- Layer 1 (Claude); Layer 2-reviewed (falsifiability ruling, covariance correction, fence condition, saturation catch are L2 contributions); Mike-arbitrated
