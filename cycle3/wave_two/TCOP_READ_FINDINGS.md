# Two-channel ordering probe — read findings (CANONICAL)

**Status: CANONICAL. Mike-concurred 2026-07-06. Grounded exclusively in `cycle3/data_out/tcop_read_results.json`, the single output of tcop_read.py v2 (script digest d60da1d9…399f7c; L2 hostile build review with blocker + amendments all folded; executed by Mike 2026-07-06, clean completion, no halts). Governed by TCOP_READ_SPEC.md (CANONICAL) + TCOP_READ_SPEC_AMENDMENT_1.md (CANONICAL); contract 8c94d8c; addendum 8a777e6. Every threshold was frozen before any probe output existed; every rule was fixed before any CM-1/CM-0 comparison was computed; nothing was tuned, relaxed, or rerun. Candidate language throughout. Lineage: findings v1 (L1) → L2 interpretation-creep attack (amend-before-canonical; top-line preserved; five replacement texts) → THIS: L1 full-acceptance merge, all L2 replacement texts folded verbatim, no contests, no additions → Mike arbitration: CONCUR (2026-07-06) → CANONICAL (this placement) → deferred-commit cluster (spec + amendment + script + results JSON + findings + ops log).**

## 1. The result

**The read is UNDER-DETERMINED on both co-equal axes: no evaluable eligible comparison family exists in the dataset under the frozen rules. This is an apparatus-limit outcome, not evidence for or against the ordering hypothesis.** The probe neither PRODUCED nor FAILED TO PRODUCE the predicted ordering structure. The discriminating question — can common-mode lift raise ρ without spatial organization, while organization onset tracks the differential channel rather than ρ level itself — was not answered by this dataset. The ordering hypothesis remains open. This execution provides no produces claim, no risky-form failure claim, and no evaluated path-(b) test of the ordering mechanism.

No path-(a) setting-level risky-form failure was recorded on either axis (and none was withheld by Amendment 1's bin-qualification — the `_amendment1_withheld` record is empty; the global flags themselves never fired at c = 0). No path-(b) tracking family was evaluable on either axis. No outcome reclassifies Comparator 0, moves L4, closes Rule E B/C, closes the CM-2 hold, or closes any mechanism class. No density-stability claim (Λ = 0.40 single anchor).

## 2. What the apparatus established (gate record, not outcome)

**Data integrity.** 56,805 recorded-vs-reconstructed checks; worst absolute discrepancy 9.996e-17 against a 1e-9 tolerance. Byte-for-byte apparatus enforcement passed at runtime. The recording apparatus and the driven-path reconstruction convention agree essentially exactly across the full 315-run surface.

**G1 — zero-mode driver imprint: 135/135 nonzero CM-1 rows pass**, including the three-component CM-0 dominance rule (correlation, slope, and block-ρ variance each strictly exceeding the matched comparator maxima; comparator sets digest-frozen at 9d68c551… before any CM-1 processing). At every tier, all five c = 0 rows pass G1, so **verified common-mode lift holds at every tier** under the strict-all rule. Qualified wording binding: this is apparatus-level zero-mode driver imprint of the global activity count — not coherence, not synchrony in the theoretical sense, not Regime II, and it bears on neither observable's relation to theoretical Ψ.

**G4 — 176/180 CM-1 rows in-band.** The four fenced rows are all (u = 0, c = +0.35), seeds 137/256/1024/31415, and Section 4 shows the fencing had no bearing on evaluability.

**G2 — 3/6 tier-sign families pass: the negative-coupling channel activated at all three tiers; the positive-coupling channel failed at all three.** Section 4 characterizes the failure mode.

**Onset record.** 1,980 per-window sentinel exceedances across the dataset (max-observed floors sit well below committed responsive-cell scale, so heavy exceedance in responsive settings is expected texture). At c = 0: across all four tiers, both axes, and 45 primary windows per setting-axis, the c = 0 settings produced exactly 3 single-window exceedances in total, zero row-level flags (k = 2 never met), zero setting-level flags.

## 3. Anatomy of the under-determination (why no family was evaluable)

**Path (b) — realized matched-ρ common support did not materialize.** For the G2-passing negative-sign tracking families (c = 0 vs. |c| ≥ 0.20 within tier), the complete bin census over the global grid reads: 76 out-of-support, 6 count-failed, 4 phase-confounded, **0 eligible**. The dominant mode is out-of-support: under drive, the c = 0 and responsive-|c| settings occupy ρ strata separated by more than the frozen bin width (Δρ_match = 0.00283, deliberately narrow, cap-bound at threshold-fixing). The positive-sign families were additionally barred by G2 failure — no ordering read is permitted from a region where the differential channel FAILED TO ACTIVATE. The pre-seed feasibility audit classified both paths feasible on construction-only mean-field bands and stated explicitly that feasible is necessary, not sufficient, and that realized G3 can still fail per-comparison and be recorded as an apparatus limit. That predeclared contingency is what occurred.

**Path (a) side, recorded descriptively only.** Under verified common-mode lift at every tier, the c = 0 settings did not produce a setting-level risky-form failure on either axis: no setting flag, no row flag, and three isolated sentinel exceedances. This establishes only the absence of a path-(a) failure under the frozen onset rules. It is not a produces claim, not a partial-support claim, and not evidence that the two-channel ordering structure was produced. Because path (b) had no evaluable eligible comparison family, the read remains under-determined.

**The three sentinel risky events, in full (recorded, never suppressed; all G3_eligible = false):** (i) meanI, tier 0.25, seed 137, window 75: raw +0.00210 vs floor 0.00199; (ii) meanI, tier 0.50, seed 256, window 225: raw SIGNED NEGATIVE −0.00233 — a magnitude exceedance in the anti-structured direction, which the signed-effect discipline forbids describing as positive organization; (iii) persistence, tier 0.50, seed 137, window 275: raw signed negative −0.02519. All three are single-window, hair-above-floor events (z in 1.78–2.30, recorded-not-reconstructed, never gating), none aggregating to any flag. Under the frozen rules, these events are sentinel records only: they are single-window, non-G3-eligible exceedances that do not aggregate to row-level or setting-level onset and therefore do not constitute a path-(a) risky-form failure.

## 4. Apparatus sub-findings (fenced)

**4a. Sign-asymmetric G2 failure under drive, with monotone tier dependence.** The positive-sign endpoint lower-support values missed the frozen floor 0.38322: 0.38031 at tier 0.10, 0.37815 at tier 0.25, and 0.37294 at tier 0.50. The deficit grows with common-mode amplitude. The negative-sign families cleared their frozen floor at all three tiers.

**Mechanism status:** this pattern is compatible with a logit-slope attenuation story, but the read does not establish that mechanism. The read establishes only the operational fact that the positive-sign differential channel failed G2 under the frozen full-surface prop_I floors, while the negative-sign channel passed. No slope-attenuation allowance was emitted at thresholding, no allowance was applied at read time, and none may be inferred from this result. Competing explanations — realized-contrast drift under drive, sign-asymmetric local-field geometry, finite-seed lower-support behavior, or other propensity-field effects — remain open unless separately tested. Any decision to introduce or test an attenuation allowance is a new design question for Mike, not a finding of this read.

**4b. G4 fencing is a floor-derivation boundary effect, not compression, and had zero outcome bearing.** The four fenced rows fail ONLY the mean-slope floor, by margins of −2.5e-5 to −2.2e-4; their q05 margins are exactly 0.0 (at the floor, passing on ≥) and tail mass is exactly 0 (no compressed cell anywhere). These are u = 0 parity rows reproducing committed M2 dynamics bit-exactly at the very cell (|c| = 0.35) whose distribution helped set the lower-10% floor — rows sitting at the boundary of the population that defined the boundary. The (tier 0, c = +0.35) setting appears in no pair of the frozen comparison universe (path (a) is c = 0 only; path (b) lives within nonzero tiers), so the fencing removed nothing from any evaluable comparison. Recorded as an A3-strictness apparatus note.

**4c. Realized ρ-stratification under drive is the binding constraint on this instrument.** The lifted c = 0 window-mean ρ observed in the sentinel records (≈0.413 at tier 0.25, ≈0.426–0.441 at tier 0.50) sits where construction predicted, and the responsive settings sit elsewhere at bin resolution. The instrument's ordering read is gated on a bin width smaller than the realized drive-induced separations, so the realized settings did not co-occupy eligible matched-ρ bins under the frozen rule. Any follow-up that seeks an evaluable ordering comparison must confront this at the design level — comparison-family geometry, tier placement, or matching unit — and every such choice is a new design resolution on Mike's explicit call, never an after-the-fact relaxation of this read's frozen rules.

## 5. Named open items and holds (unchanged)

The 9-window earned-window rule remains UNVERIFIED (max-observed floors were operative throughout; the 97.5th-percentile values traveled as diagnostics and licensed nothing). CM-2 held; Rule E B/C held; L4 untouched; Comparator 0 unreclassified; Λ = 0.20 named-not-triggered; no density-stability claim. The ordering probe's question remains OPEN: this dataset, under these frozen rules, could not evaluate it.

## 6. Attribution

Read spec: L1-drafted, L2-attacked (reject-as-ratification-ready), L1 full-acceptance merge with five disclosed refinements, L2 fidelity pass clean, Mike-arbitrated A1–A5. Read script: L1-drafted, Mike-routed to L2 for hostile pre-execution build review (beyond the required sequence), L2 verdict reject-as-execution-ready (one blocker, six amendments), blocker resolved by Mike-arbitrated spec Amendment 1 (bin-qualified path-(a), joint L1+L2 recommendation), all amendments folded in v2, digest-verified at placement, Mike-executed. Findings: L1-authored from the committed results JSON (v1) → L2 interpretation-creep attack (amend-before-canonical: path-(a) partial-support leak removed; sentinel classification narrowed to the frozen rules; 4a rewritten to compatibility-only with competing explanations open; 4c quantification corrected; top-line tightened) → L1 full-acceptance merge (v2, all replacements verbatim) → Mike arbitration. Every claim above chains to a JSON field; the outcome-classification evidence table in the JSON audits every label independently of this prose.
