# Stage 2 mini-contract - retrodictive calibration of the pre-seed evaluability audit machinery (CANONICAL, frozen at placement 2026-07-08)

**Status: CANONICAL - FROZEN AT PLACEMENT (Mike-arbitrated 2026-07-08). Lineage: v1 (L1 draft) -> L2 attack (reject-with-blockers; architecture accepted; two passes, verdicts agreeing, one internal divergence) -> FORK 1 routed to Mike (reconstruction point: full two-stage reconstruction vs. single post-Amendment-1 stack plus Section 4 currency-replay item) -> Mike arbitrated OPTION B -> v2 (L1 full-acceptance merge, all five blockers and all precision amendments folded, none contested, disclosed refinements D1-D5) -> L2 fidelity pass CLEAN (D1-D5 accepted; all three invited fidelity questions answered affirmatively; no missing blocker, distorted fold, internal contradiction, downstream-relaxation leak, TCOP reopening, or L4 movement) -> Mike placement arbitration 2026-07-08, INCLUDING his recorded acceptance of the Section 5 completion-structure constraint on his own option space (at least one speaking case, or an explicit recorded severity waiver, before the amendment governs any prospective design). This contract now GOVERNS the Stage-2 calibration. Case 1 (TCOP) is TRIGGERED by this freeze. Score items, fences, the morphology-menu discipline, the precision rule, and the escalation ladder are frozen as written; per Section 0 they are never relaxed, adjusted, or reinterpreted after calibration outputs exist. Remaining sequence: implementation spec + harness (L2 hostile build review; L2's carried reminder - hostile scrutiny of morphology-menu provenance) -> Mike executes -> control validity review -> instantiation freeze -> unlock and scoring -> fold into the amendment -> L2 fidelity on the fold -> Mike ratifies the fold. The review lineage is preserved in STAGE2_MINI_CONTRACT_REVIEW_RECORD.md.**

**Parent instrument:** PRESEED_EVALUABILITY_AUDIT.md (v3, sha256 d2f12fada35d726a5039696cb06caa4687e5b6506e26749318577c1d601c432a, 32057 bytes; RATIFIED-PENDING-CALIBRATION 2026-07-08, commit c5896b3). Section 7 of the amendment is binding on this document; on any discrepancy, the amendment wins.

**Disclosed refinements register (D1-D5; for L2 fidelity confirmation; each reconciles the two attack passes or organizes accepted text, none contests it):**
- **D1 (Section 5, S3a):** the two passes' S3(a) replacement texts are merged: pass 1's floor-relative criterion (central estimate below the design's own evaluability floor, not a hard-coded 1) is the operative rule; pass 2's predeclared predictive-interval level, non-vacuity requirement, and interval-construction-method reporting are carried as additional requirements on the same item.
- **D2 (Section 5, S4):** pass 2's dual-floor form (evaluability below the seedability floor AND recovery-conditional-on-evaluability above the recovery floor) is adopted as the frozen rule; pass 1's multiplicative-ratio form is recorded as the documented alternative, not operative. Both passes' shared requirement - frozen before instantiation, no prose judgment after unlock - governs.
- **D3 (Section 4, replicates):** pass 2's precision-margin rule (interval half-width bounded against the decision margin to the floor) is adopted as the governing rule; pass 1's predeclared escalation ladder (100 -> 500 -> 1000 on floor-overlap) is adopted as its mechanism. R >= 100 is demoted to smoke-test minimum, per both passes.
- **D4 (Section 5, pass semantics):** pass 1's completion requirement (at least one under-determined case AND at least one speaking case scored before the amendment governs any prospective design) and pass 2's waiver wording (explicit, recorded as a risk acceptance) are merged; the v1 option "accept Case 1 alone as sufficient sanity check" is STRUCK per both passes.
- **D5 (Sections 1/4, morphology fence):** both passes' morphology-fence requirements are merged into one discipline: frozen pre-unlock morphology menu derived from the pre-TCOP target claim; per-morphology provenance table; geometry-sensitive and detection-sensitive coverage; newly-invented-morphology exclusion from scoring absent explicit L2 acceptance.

## 0. What this is and what it never does

This mini-contract governs the retrodictive calibration of the evaluability-audit machinery against wave two's committed record. The machinery is the object under test; wave two is ground truth.

**The epistemic line, absolute (amendment Section 7, carried verbatim in intent):** this is a calibration of the machinery - NEVER a re-read of any probe. Retroactively applied rules cannot issue findings. The TCOP result stands permanently as recorded: UNDER-DETERMINED on both co-equal axes; apparatus limits, NOT evidence; the probe neither PRODUCED nor FAILED TO PRODUCE; the ordering hypothesis OPEN. No output of this calibration reopens, revises, supplements, strengthens, or weakens it - or any other committed finding.

This mini-contract does NOT:
- issue, revise, or gloss any finding about the EE substrate;
- decide the attenuation-allowance or matched-rho geometry design questions (Mike's, named-not-triggered; calibration outputs may INFORM them, never decide them);
- verify the 9-window earned-window rule, reclassify Comparator 0, move L4, or touch CM-2, Rule E B/C, Lambda=0.20, or the no-density-stability discipline;
- authorize any prospective probe design (a Stage-2 pass permits a prospective TRIAL of the amendment; it does not validate the amendment generally - amendment Section 7.5);
- weaken anything in the amendment's Section 5, which applies here reflexively: score items, morphology menus, precision rules, and escalation rules frozen before the calibration runs are never relaxed, adjusted, or reinterpreted after its outputs exist.

## 1. Fences (frozen with this contract, before anything runs)

**F1 - HINDSIGHT CONTAMINATION (magnitudes AND morphologies).** Every synthetic magnitude grounds in PRE-TCOP committed anchors only: the prop_I ~ +0.38 propensity anchor; the committed raw Moran scales ~0.004-0.011 (meanI) / ~0.05-0.07 (persistence); Reading-D's measured attenuation fractions ~1-3% (meanI) / ~13-18% (persistence). No magnitude may be derived from, tuned against, or justified by anything in the TCOP realized record.

**Morphology fence (L2 attack Blocker 2, both passes folded; D5):** morphologies are the larger leak surface and carry their own discipline:
- Every positive-control and wrong-shape morphology must be DERIVED FROM A PRE-TCOP TARGET MORPHOLOGY named in the TCOP contract or design resolution (the target claim's own predeclared theory of effect) - never from the realized TCOP failure anatomy.
- The morphology menu is FROZEN PRE-UNLOCK, before instantiation begins, and receives L2 validity review as part of control-validity-first.
- The instantiated audit carries a MORPHOLOGY TABLE with columns: morphology name; target-claim component it realizes; permitted provenance source; why expected-easy or plausible-difficult; and an explicit statement of why it is NOT selected because of the known realized failure.
- The menu must include AT LEAST ONE geometry-sensitive morphology and AT LEAST ONE detection-sensitive morphology (a morphology whose recovery would fail by detection insensitivity if the machinery were insensitive, not only by matching geometry), unless L2 accepts a written argument that the target claim admits only one morphology class. This is the guard against the machinery routing every miss to geometry.
- A morphology newly invented for Stage 2 (no pre-TCOP naming) must be labeled as such and is EXCLUDED from S1-S4 scoring unless L2 explicitly accepts it as a neutral reconstruction of the pre-TCOP target claim rather than an answer-key-shaped injection.

Provenance - magnitude and morphology - is REQUIRED L2 attack surface at the implementation-spec stage.

**F2 - TEMPTATION QUARANTINE.** Calibration outputs inform the amendment's machinery and NOTHING else. No calibration output is ever citable as evidence about the EE substrate. Every calibration output file carries the mandatory header: "CALIBRATION OUTPUT - INFORMS AMENDMENT MACHINERY ONLY - NOT EVIDENCE ABOUT THE EE SUBSTRATE." No calibration output may be cited in any findings file, synthesis, proposal, design resolution, or manuscript artifact.

**F3 - NO RE-READ, STRUCTURALLY.** The answer-key record (Section 3's held-out list) is COMPARED-AGAINST at scoring, never COMPUTED-FROM during instantiation. All committed repository data is read-only to the calibration. Calibration outputs are written ONLY under cycle3/calibration/stage2/ (new directory); no calibration process writes to cycle3/data_out/ or any committed path. The calibration RNG seed is frozen in the implementation spec before any run; no rerun of a completed calibration computation under the frozen design. Predeclared replicate escalation under the Section 4 precision rule is NOT a rerun.

**F4 - KNOWN-OUTCOME CONTAMINATION, DISCLOSED.** Every party to this calibration (L1, L2, Mike) knows the qualitative TCOP outcome; it is stated in every anchor supersession header and cannot be unknown. This contract therefore claims PROCEDURAL blindness, not epistemic blindness: the calibration computation consults ONLY the permitted-input list (Section 3); held-out material is not opened, parsed, quoted, or numerically referenced during the instantiation phase; every numeric in the instantiated audit must carry a permitted-input provenance tag. What qualitative knowledge cannot supply - and what the calibration therefore genuinely tests - is whether the machinery's required computations, run on permitted inputs alone, produce the verdict, the failure-mode attribution, and the QUANTITATIVE forecast in realized units. The F1 morphology fence is the primary mitigation of the residual steering channel both L2 passes identified. This disclosure remains L2 attack surface: any further path by which qualitative outcome knowledge steers a quantitative instantiation choice must be fenced or the affected component redesigned.

**F5 - NO DESIGN-QUESTION DECISION FORCE.** A calibration forecast that quantifies drive-induced stratification will be adjacent to the matched-rho geometry design question. Adjacency is not decision. Any design implication is routed to Mike as exactly that - an implication for a question that remains his. **Hardened (L2-required):** any numerical implication for future matched-rho or attenuation-allowance design must be RESTATED AS A DESIGN QUESTION and may not be imported into any prospective design as a threshold, prior, or default without a new design-resolution review.

## 2. Case set and staging

Per the amendment's D4 register and Section 7.3:

- **Case 1 - TCOP (under-determined). TRIGGERED by this contract's freeze.** The machinery's first test. The amendment's own failure clause governs: if the machinery says "seedable, many eligible bins" for TCOP, it fails its own first test and is revised before governing anything prospective.
- **Case 2 - Rule E bounded-A (saturated-relay / non-responsive bounded-channel). NAMED-NOT-TRIGGERED within Stage 2.** Opens on Mike's explicit call, under this contract's partition and fence structure, under a frozen companion score-item set drafted at opening.
- **Case 3 - Rule D R3 (scoped negative). NAMED-NOT-TRIGGERED within Stage 2.** Same discipline as Case 2.

**Completion structure (L2 attack Blocker 5, both passes folded; D4):** Cases 2 and 3 are the discrimination test the second spine principle requires - probes that SPOKE. A machinery that classifies evaluable historical designs as non-seedable fails Stage 2 exactly as one that calls TCOP seedable. Case 1 may be the FIRST gate; it may not be the ONLY gate: **Stage 2 is not complete, and the amendment may not govern any prospective design, until at least one under-determined case AND at least one speaking case have been scored** - unless Mike explicitly records a severity waiver accepting that the machinery is being trialed without over-trigger discrimination. The waiver, if given, is recorded as a risk acceptance, never implied.

## 3. Input partition - Case 1 (TCOP)

**Reconstruction point (MIKE-ARBITRATED, Fork 1, Option B, 2026-07-08):** the frozen stack under audit is the stack as of PRE-READ-EXECUTION - after the read spec, Amendment 1, and the thresholding addendum's freezes, before any gate was evaluated. Rationale carried from v1: (i) an evaluability audit examines a frozen pipeline, and TCOP's pipeline reached frozen form only at that point; (ii) Amendment 1's routing of both falsification paths through the G3 bin geometry is precisely the gate coupling the joint budget must see; (iii) the amendment's own Section 4 treats Amendment 1 as its motivating instance.

**Currency-replay item (L2-required; the arbitrated Option B's second half):** auditing the post-amendment stack does not by itself test the Section 4 transition. The instantiation must therefore (a) explicitly state that the reconstructed stack is post-Amendment-1, and (b) SEPARATELY REPORT, as a procedural currency check under the amendment's Section 4, which evaluability components Amendment 1 would have made stale had the evaluability section existed pre-amendment - identifying the affected components and why (the bin-qualification routing both falsification paths through G3 geometry being the known coupling). This is a currency-identification replay, not a second historical audit. The full two-stage reconstruction (pre-amendment instantiation -> Section 4 currency update -> post-amendment instantiation) is the documented heavier alternative, on record, not selected.

Magnitude and morphology provenance remains PRE-TCOP-COMMITTED (F1) regardless; the reconstruction point governs which RULES are audited, never which EFFECTS are injected.

**PERMITTED INPUTS (the instantiation phase may consult ONLY these):**
- TWO_CHANNEL_ORDERING_PROBE_CONTRACT.md and TWO_CHANNEL_ORDERING_PROBE_DESIGN_RESOLUTION.md (the design and its frame; also the source of pre-TCOP target morphologies for the F1 morphology menu);
- TCOP_READ_SPEC.md and TCOP_READ_SPEC_AMENDMENT_1.md (the frozen rule stack);
- THRESHOLDING_ADDENDUM.md (frozen floors; the construction-only feasibility audit that is the machinery's "before" exhibit);
- threshold_fixing_read_results.json (pre-seed pure-read outputs the addendum froze from);
- tcop_read.py at its committed digest d60da1d9...399f7c (the frozen pipeline implementation, for end-to-end replicate traversal);
- the c3_w2_null_extension NPZs (the injection substrate; committed pre-seeding);
- the pre-TCOP committed anchors enumerated in F1, from their committed sources (READING_D_PASS1_FINDINGS.md for the anchor values);
- construction-level arithmetic of the frozen design itself (the committed drive tiers u in {0.10, 0.25, 0.50} logits and the frozen G3 constants Delta_rho_match=0.00283 / N_bin_min=9 / S_bin_min=3 are design content; computing their implied driven-rho geometry from construction is the forecast's job).

**HELD-OUT RECORD (never opened during instantiation; unlocked only at Section 5 scoring):**
- tcop_read_results.json (THE ANSWER KEY);
- TCOP_READ_FINDINGS.md (the realized gate record, bin census, strata values, sentinel record, fenced rows);
- the TCOP run output CSVs (Stage B/C window and block rows - the realized data the forecast is scored against);
- **path-independent extension (L2-required):** ANY document containing realized TCOP quantities - ops logs, anchor passages, synthesis memos, routing notes, instantiation prompts - is treated as held-out FOR NUMERICAL INSTANTIATION USE regardless of its path or its presence elsewhere in the permitted list's ecosystem. Held-out status attaches to the quantities, not only to the files.

**Content-level enforcement (L2-required):** the implementation spec must include a content-level audit that the permitted files, as used, do not contain realized TCOP quantities employed numerically in instantiation. If such quantities appear inside an otherwise-permitted file, they are masked or the file is split into permitted and held-out excerpts before the harness consumes it. Path restriction alone does not satisfy this fence; repository files can be hybrid.

**Harness enforcement:** the implementation spec designates the permitted files by path and digest; the calibration harness takes only those (audited/masked) inputs; fail-closed on any held-out path or masked-region access. L1's instantiated-audit document cites permitted sources for every numeric. Any numeric without a permitted provenance tag is a defect halting the instantiation.

## 4. Procedure - Case 1

1. **Reconstruction freeze.** Enumerate the audited stack (files + digests) and the reconstruction point. Freeze.
2. **Morphology-menu freeze (pre-instantiation; F1 discipline).** Draft the morphology menu and table from the pre-TCOP target claim; route to L2 for validity review (control-validity-first, D2 of the amendment, applies here); freeze the menu on clearance. No morphology enters or leaves the menu after this freeze.
3. **Instantiate the eight components** of the amendment's Section 2 for the reconstructed TCOP design, as the evaluability section would have been written pre-execution, from permitted inputs only:
   - 2.1 target claim tier (the risky-form produce / fail-to-produce outcome on the two co-equal ordering axes, from the contract);
   - 2.2 positive controls: the frozen menu's morphologies (>= 2, per-menu easy/difficult and geometry-/detection-sensitive coverage), magnitudes with F1 provenance, injected into the null-extension substrate. **Injection level (L2-required):** injection occurs at the earliest substrate level compatible with the target morphology; no positive-control injection may directly edit candidate flags, gate outputs, z-scores, bins, or post-read summaries. FULL-pipeline traversal through tcop_read.py's frozen geometry (eligibility, binning, matching, aggregation, claim-tier gates) - no geometry bypass;
   - 2.3 negative controls: >= 1 pure-null and >= 1 wrong-shape-by-the-target-claim's-own-morphology per engaged axis (wrong-shape morphologies drawn under the same F1 menu discipline), with a predeclared stochastic false-positive tolerance;
   - 2.4 compound conservatism budget: end-to-end synthetic replicates through the frozen pipeline; EVALUABILITY probability and RECOVERY probability separately reported; method and uncertainty stated. **Replicate precision rule (L2 attack Blocker 4, both passes folded; D3):** R >= 100 per morphology-magnitude cell is a smoke-test MINIMUM, not a justification. R must be chosen so the Monte Carlo interval half-width on each probability used in a seedability/halt decision is smaller than a predeclared fraction of the decision margin to the relevant floor (fraction frozen in the implementation spec). If the estimated interval overlaps the floor, a PREDECLARED escalation ladder applies (R -> 500 -> 1000), frozen before any run; escalation under the frozen ladder is not a rerun. No S1/S4 hit may be scored from an estimate whose interval straddles the floor without exhausting the ladder;
   - 2.5 evaluability forecast: realized-geometry numbers in the read's own units - eligible bins, windows per bin per side, seed-level counts - INCLUDING under the committed drive construction, with a PREDECLARED predictive-interval level (frozen in the implementation spec), interval-construction method reported, uncertainty bands, approximation disclosure, and the buffer term defined in the instantiation before unlock;
   - 2.6 calibration tranche: not applicable for execution (the geometry exists in committed data), recorded as such - AND (L2-required) the instantiated audit must still state whether the reconstructed design would have required a calibration tranche under the amended protocol, and why;
   - 2.7 claim ladder: the reconstructed ladder as the frozen stack actually specified it (e.g., the traveling 97.5th-percentile floors), with the machinery noting any rung the historical design left unspecified;
   - 2.8 halt rules: applied to the instantiated audit, yielding the machinery's verdict in the three-category taxonomy plus the floor-breach rule.
4. **Currency-replay item** (Section 3): reported as part of the instantiated audit.
5. **Control validity review (L2), before any verdict binds** - control-validity-first applies inside the calibration exactly as prospectively (the morphology menu already cleared at step 2; this step covers the instantiated controls as built).
6. **Freeze the instantiated audit** (document + outputs, digested) BEFORE unlock. From this point the instantiation is immutable.
7. **Unlock the held-out record. Score** against Section 5's frozen items. Pointer-scoring: hit criteria reference realized values by pointer into the held-out files, which are restated only in the scoring record, never in the instantiation.
8. **Record** hits and misses in the Stage-2 calibration record. Misses are machinery-defect findings feeding the Section 7 fold. No score item, menu, precision rule, or escalation rule is adjusted, reweighted, or reinterpreted after unlock.

## 5. Score items - Case 1 (frozen at this contract's placement)

- **S1 - verdict category.** HIT iff the machinery's audit verdict for the reconstructed TCOP design is NOT SEEDABLE, arising from the reconstructed design's evaluability geometry or frozen gate stack (floor-breach or design-level halt). **Exclusions (L2-required):** a not-seedable verdict arising from invalid controls, missing provenance, an uncomputable budget, an unscorable forecast, or harness failure is an INVALID EVALUABILITY SECTION outcome or a machinery/harness defect - it is NOT a hit; it is a defect finding. A verdict of seedable with adequate evaluability ("seedable, many eligible bins") is a MISS - the amendment's own named failure. **Taxonomy precision (L2-required):** an evaluability probability below the design's own floor yields NOT SEEDABLE absent an explicit Mike severity override; SEEDABLE-BUT-SEVERE denotes a design passing its own floor whose reported speaking probability is low by defended severity choice. The v1 wording conflating these is corrected.
- **S2 - failure-mode attribution (computed, not narrative; L2-required).** HIT iff the instantiated audit identifies the G3 / matched-rho comparison geometry as the DOMINANT FAILING COMPONENT, with forecasted eligible comparison support below the design floor UNDER THE COMMITTED DRIVE CONSTRUCTION - a computed claim in the budget/forecast outputs, not a prose resemblance. Detection insensitivity as the attributed dominant mechanism is a MISS. Scored by pointer against the committed anatomy in the held-out findings.
- **S3 - quantitative forecast (D1 merge).** Two sub-items, both required for the hit:
  - **(a)** the eligible-bin forecast is stated at the predeclared predictive-interval level with its construction method reported; the interval is NON-VACUOUS (an interval so broad it includes both seedable and non-seedable geometries without assigning discriminating probability mass is invalid and fails this item); the realized eligible-bin count falls in the forecast's predicted non-speaking region relative to the reconstructed design's evaluability floor; and the central estimate/median falls BELOW the design's own minimum eligible support for the target claim tier (the design-relative form of v1's arbitrary "<= 1", which is struck). No post-hoc interval widening.
  - **(b)** the forecast places the drive-induced rho-strata separation from the c=0 family above Delta_rho_match plus the buffer defined in the instantiation, in the read's own units.
  Pointer-scored against the held-out record.
- **S4 - evaluability/recovery separation (D2 merge; frozen dual-floor form).** HIT iff the budget reports the two probabilities separately AND evaluability probability is below the seedability floor AND recovery-conditional-on-evaluability is above the RECOVERY FLOOR for the positive controls - both floors frozen in the implementation spec before instantiation. MISS iff the two probabilities merge, iff either floor comparison is left to prose judgment, or iff the budget attributes failure to sensitivity. (Documented alternative, not operative: a predeclared multiplicative ratio between the two probabilities.)

**Miss semantics:** any miss is a defect finding against the machinery, entering the fold. A miss never licenses adjusting the score item, re-running the calibration under altered instantiation choices, or reinterpreting the instantiated audit. If the fold revises the machinery, whether the revised machinery re-attempts Case 1 is a NEW Mike arbitration under a fresh score-item freeze - never a silent rerun.

**Pass semantics (D4 merge; v1's "Case 1 alone sufficient" option STRUCK):** all-items hit on Case 1 permits Mike to open a speaking-case calibration (Case 2 or Case 3) under a frozen companion score-item set. It does NOT by itself permit a prospective trial of the amendment. Stage 2 completes - and the amendment may govern its first prospective design (after the Section 7 fold path) - only when at least one under-determined case AND at least one speaking case have been scored, OR Mike explicitly records a severity waiver accepting trial without over-trigger discrimination, entered in the record as a risk acceptance.

## 6. Execution and transit discipline

Mike is the sole execution channel. L1 drafts the calibration implementation spec and harness under this contract; the harness receives Mike-routed L2 hostile build review before execution (the tcop_read.py precedent). Digest gates published before every placement, verified after, size-keyed Downloads selection. One PowerShell command per fenced block. The harness enforces F3 and the Section 3 content-level fence mechanically: permitted (audited/masked) inputs only, output confined to cycle3/calibration/stage2/, frozen RNG seed, predeclared escalation ladder only, fail-closed on any held-out or masked-region access. The session ops log commits alongside any committed calibration artifact.

## 7. Outcome and fold

The calibration record (instantiated audit + scoring record + ops log) is the Stage-2 evidence object. Lessons - hits, misses, and any machinery friction found in the doing - fold back into the amendment as its Section 7.6 requires: fold drafted by L1 -> L2 fidelity on the fold -> Mike ratifies the fold -> the amendment governs its first PROSPECTIVE design, subject to Section 5's completion structure (at least one speaking case scored, or Mike's recorded waiver). Until that ratification the amendment remains RATIFIED-PENDING-CALIBRATION and governs nothing prospective.

## 8. Attribution

Drafted by L1 from PRESEED_EVALUABILITY_AUDIT.md (v3, digest-verified), its review record, and the committed TCOP arc record, under the Stage-2 open arbitrated by Mike 2026-07-08 (commit c5896b3). v1 -> L2 attack (reject-with-blockers; architecture accepted; two passes, verdicts agreeing, one internal divergence) -> Mike arbitrated Fork 1 to Option B (single post-Amendment-1 reconstruction + Section 4 currency-replay item) -> THIS v2: L1 full-acceptance merge, all blocker and precision texts folded, disclosed refinements D1-D5. The morphology fence, currency-replay item, S1 taxonomy and defect-exclusion, S2 computed-claim form, S3 floor-relative rebuild, S4 dual-floor form, replicate precision rule and escalation ladder, completion structure and waiver form, content-level held-out enforcement, path-independent held-out extension, injection-level rule, 2.6 statement requirement, and F5 hardening are L2's contributions. The procedural-blindness formulation (F4) and the fence architecture are L1's, as attacked and amended. Nothing here is frozen until Mike's arbitration places it.
