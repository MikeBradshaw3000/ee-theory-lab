# Stage 2 Case 1 - L2 review record (implementation spec + Amendment 1 + harness build)

**Status: committable review record for STAGE2_CASE1_IMPLEMENTATION_SPEC.md (frozen), STAGE2_MINI_CONTRACT_AMENDMENT_1.md (L2-accepted), and cycle3/calibration/stage2/stage2_case1_harness.py (v9, L2-accepted execution-ready; v4-v8 superseded by the five first-execution repairs recorded below). Non-canonical support artifact preserving the review lineage.**

## Implementation spec

v1 (L1 draft, incl. proposed morphology menu) -> L2 hostile review, TWO verdicts: (A) morphology menu CONDITIONALLY VALID - P1/N2/N3 valid; P2 valid as geometry-sensitive traversal control only, not a distinct morphology (P1/P3 supply diversity); P3 valid with the no-gate-tuning pre-output guard; N1 valid after replacing the brittle setting-level-zero rule with a stochastic tolerance (Wilson-scored, ladder-gated); N4 INVALID as a negative control (the pre-flagged D2 risk realized), retained non-scoring only. (B) implementation review REJECT-AS-BUILD-READY - canonical-seed fence and F3-analog gate ACCEPTED CLEAN; conditioned two-arm budget ACCEPTED AS GENUINELY JOINT with the recovery-conditioning precision; floors accepted with the path-(a) unit clarification (Amendment-1 bin-qualified object, never the global flag); Engine-B inline-block enumeration required; N1 tolerances revised. -> v2 L1 full-acceptance merge (disclosed refinements D1: setting-level tolerance 0.03, L1-set where L2 named a rule but no number, zero-false-pass severity alternative preserved for Mike; D2: N4 retained non-scoring, droppable) -> Mike froze v2 ("draft" arbitration, 2026-07-09).

## Mini-contract Amendment 1 (permitted-input addition)

Blocker surfaced at build time: tcop_read.py reconstructs but never simulates; the committed RNG discipline (legacy global seeding, init shuffle, one shared rand_grid per tick) lives only in the committed run scripts, and the frozen F3-analog bit-exact gate is unbuildable without it; the frozen pipeline's own verify_byte_for_byte consumes both committed sources. Mike approved adding cycle3/wave_two/c3_w2_tcop.py (digest 466455f2...b7e7, matching the committed build record) and c3_w2_rule_c_m2.py (8f1f6ab9...ef59). **L2 ACCEPTED** with the construction-only fence (never used to import, infer, or reconstruct held-out realized outputs) and the required preflight manifest marking ("permitted by Amendment 1, construction-only, zero realized quantities, digest-verified") - both implemented in the harness.

## Harness build review (four rounds, all Mike-routed)

- **v1 -> REJECT (18 required items).** L1 self-flagged ten defects in the routing packet (pessimistic-on-passing); L2 confirmed all ten and added eight: RNG checkpoint/resume corruption; I/O fence gaps (raw open, prefix-vs-commonpath, no read allowlist, importlib bypass); missing rule_c_m2 digest; permutation determinism; recovery not actually conditioned on the evaluable ensemble; and others.
- **v2 (full rebuild) -> REJECT (7 local blockers).** The frozen-source verification forced by the rebuild caught three wrong-values-under-right-names defects in L1's v1 reading: WINDOW_LEN 100 (not 75); onset exceed = abs(raw) > floor AND residue > floor; meanI control shuffle per-cell along time (axis=0; v1 had a spatial shuffle). v2 accepted items: stateless seed derivation replacing CalStream; AST literal extraction of KAPPA_MAP (no run-script execution); offsets verified by reproduction; P3 amplitude frozen derivation (L2 independently verified rho_k = 5/9); per-window deterministic permutation seeding as calibration-side discipline; floor-plus-texture injection semantics. Blockers: P2 fam[:1] and bins-only regeneration; N2 single-tier and N3 row-only; forecast taxonomy incomplete; exec_module surface; dead scipy import; _boot not enforcing preflight; approximation labeling.
- **v3 -> REJECT (2 local blockers), "no remaining conceptual blocker."** P2 all-families with exact stored window keys; N2 all tiers; N3 setting-level; per-component forecast inputs; guarded source-string exec with bytecode disabled and calls-in-assigns whitelist; preflight-manifest enforcement with harness self-digest. Remaining: the P2 pick() filtered on ws only (phase in the stored key unused); the verdict-taxonomy golden fixture was a no-op.
- **v4 -> ACCEPT (execution-ready, gated on Mike).** Full-key (ws AND phase) filter plus ws->phase determinism fixture; verdict machinery refactored to pure functions (control_verdict_items, p1_verdict_items, classify_verdict) with all named branches golden-tested (28 fixtures).

## v5 delta repair (2026-07-17; L2-accepted)

First execution of v4 (Mike's preflight call) HALTED loud and fail-closed at the first permitted-input existence check on a doubled path segment (cycle3/cycle3/data_out/...). Defect: v4 line 38 computed REPO with three dirname calls from the harness location (<repo>/cycle3/calibration/stage2/), resolving REPO to <repo>/cycle3 - a wrong-values-under-right-names defect in the accepted artifact. It survived all four review rounds because every round was code-reading, the golden tests are path-free, and the file had never been executed against the real tree; the preflight fence caught it at the first path check before any computation, write, or held-out contact. No manifest existed, so no digest-drift condition arose. Side effect: a spurious empty cycle3/cycle3/calibration/stage2 directory tree from the pre-check makedirs, inspected and removed at placement.

Mike arbitrated Option A (expedited L2 delta review; no silent patch). Delta (two hunks, both disclosed): line 38 dirname x3 -> x4; docstring version line bumped to v5 for label honesty. L1 verification: single-occurrence replacement both hunks; full diff exactly the two hunks; AST parse clean; path arithmetic verified against a synthetic tree; golden tests path-free hence unaffected; no manifest self-digest interaction. Disclosed L1 self-catch: the first hunk-2 draft said "line 39", corrected to "line 38" before routing. Digests: v4 e033471c32c0246b08b430a7a72d40daa4f6c7e928cca2216ede3518aaec41b8 (54373 bytes) -> v5 3ef443ea4d301f604d0755c3a1e48cf233b475c449f26ed881bb2d27bef4946f (54385 bytes).

**L2 VERDICT: ACCEPT.** Independent inspection confirmed the four-parent repository-root calculation for the canonical harness location; the uploaded source matched the declared v5 byte count and digest and parsed cleanly; no hidden dependency on the erroneous v4 root detected; the existing fail-closed preflight, digest, allowlist, manifest, and _boot() checks ruled sufficient for the narrow repair. Repository-root sentinel hardening noted as a separate consideration, NOT a blocker, not opened. Disposition: place and commit v5; restart from full preflight.

## v6 delta repair (2026-07-17; L2-accepted)

v5's first execution (Mike's preflight re-call after the 54dfb8c commit) HALTED loud and fail-closed at the permitted-input existence check - at the CORRECT path level, confirming the v5 REPO repair in execution. Defect: line 45's null-extension filename template read k0_0000 where the committed token is kp0_0000 (the {kappa:+.4f} rule: + -> p, . -> _, so kappa=+0.0000 -> kp0_0000). Second wrong-values-under-right-names defect, same survival mechanism (token inferred rather than decoded from the run script; never executed against the tree). The fence caught it before any read; no manifest existed.

L1 comprehensive audit before the fix: every repository path the harness references was checked against tree 54dfb8c in one pass - all 8 permitted paths, all 4 held-out deny paths, and the 315 held-out cm1/cm0 state NPZs exist exactly as named; line 45 was the ONLY defective filename reference. Delta (two hunks, both disclosed): line 45 token fix; docstring version line bumped to v6. v6 built from the committed v5 blob (digest-confirmed), fixed template cross-checked character-for-character against all five committed null-extension filenames, AST clean.

**L2 VERDICT: ACCEPT.** Independent source inspection confirmed the canonical v5 and v6 byte counts and digests, clean AST parse, and exactly the two disclosed hunks; the kp0_0000 correction points the permitted-read set and F3-analog gate to the committed null-extension family without changing any fence or scoring semantics. Digests: v5 3ef443ea4d301f604d0755c3a1e48cf233b475c449f26ed881bb2d27bef4946f (54385 bytes) -> v6 310d0dc525a179fffbce31827d4aff35abc3529e9bcbcebbbd6985c306826214 (54449 bytes). Disposition: place and commit v6; restart from full preflight; execution Mike-gated.

## v7 delta repair (2026-07-17; L2-accepted)

v6's first execution (preflight, third call) confirmed both prior fixes in execution - the existence loop and ALL permitted digest checks passed - then HALTED when the I/O self-audit executed for the first time and fired on itself. Defect 1: the v4 self_audit_io regex-scanned raw source and matched its own docstring, comments, and halt-message strings; its pandas substring check was always-true against its own text; the body variable intended to exclude its own function was computed, never used, and would not have excluded the body anyway. AST ground truth: the harness's actual I/O discipline is clean (exactly two raw calls, both inside their guards) - the auditor was broken, not the audited code. Defect 2, found by L1's static audit before it could halt as #4: preflight misapplied the _static_module_check exec-safety gate to c3_w2_tcop.py, a file the harness never executes (digest-verified, AST-literal-only consumption) whose committed top level legitimately carries its own run-script pre-flight block (non-__main__ Ifs, Try); _boot() already read it without the gate.

Delta (three hunks, all disclosed): self_audit_io rewritten AST-based (raw open()/np.load() CALLS outside their guards halt with line and function attribution; pandas checked as an import); the misapplied gate call removed with the fence rationale documented in place (the gate unchanged on tcop_read.py at both its call sites); docstring bumped. L1's deep static audit exhausted the statically-discoverable class: all constant values cross-verified against tcop_read.py at digest (tuple-extractor false alarms disclosed and corrected; ONSET raw/res identical, tcop's p975 keys a superset outside the comparison); computed constructions character-identical; offset bisection arithmetic replicated to <=5.6e-16 vs the 1e-9 threshold; golden-fixture semantics confirmed. New-audit self-tests: passes on v7; fires on four distinct injected violations. Container-verification disclosure: static/AST/deterministic-arithmetic only - no simulation, no calibration RNG, no canonical-seed simulation, no NPZ access, no calibration artifact.

**L2 VERDICT: ACCEPT.** Byte count, digest, and exactly the three disclosed hunks confirmed; the AST-based audit correctly audits actual calls/imports rather than self-triggering on strings and comments; the scope repair on c3_w2_tcop.py does not weaken the execution fence on tcop_read.py; no remaining blocker. Digests: v6 310d0dc525a179fffbce31827d4aff35abc3529e9bcbcebbbd6985c306826214 (54449 bytes) -> v7 654aa1f35b63e83f32e74505b0b2c8be1d84850719f7603e5ff79a87433278d0 (55403 bytes). Disposition: place and commit v7; restart from full preflight, Mike-gated. Named fold item (not opened): whether an execution-ready verdict should require a Mike-executed smoke stage - three first-execution defects shared the single cause that no review round ever ran the file.

## v8 delta repair (2026-07-18; L2-accepted)

v7's first execution (preflight, fourth call) reached deeper than any prior: the permitted digest loop, AST self-audit, import_frozen + verify_frozen, KAPPA_MAP extraction, and offset reproduction all PASSED on Mike's machine. The halt fired inside golden_tests at the precision fixture's identity assertion: precision_gate leaked np.bool_ on its False branch (wilson computes with np.sqrt so the comparison chain yields numpy scalars, and Python's `and` returns its first falsy operand unwrapped). The fixture caught a real downstream defect, not a pedantic one - np.bool_ is not JSON-serializable, so stage_geometry and stage_forecast (which place the value directly into json.dump) would have crashed at their summary writes. Delta (two hunks, both disclosed): precision_gate's return wrapped in bool() with an in-place mechanism comment - value unchanged on every input; docstring bumped.

Verification upgrade this round, disclosed: L1's prior arithmetic replica used Python's math module and was structurally blind to numpy types (value verified, type missed - owned). L1 then executed the ENTIRE preflight body as a container replica against v8 under numpy 2.4.4 (the canonical version): synthetic tree of committed blobs at real relative paths, zero-byte NPZ placeholders existence-checked and never read, F3-analog gate excluded by design, no calibration artifact or RNG consumption, tree destroyed after. Result: permitted digest loop (all three frozen script digests match), self_audit_io, import_frozen + verify_frozen, kappa extraction (9 pairs, c=0.35 -> 0.7599), offset reproduction, and the FULL 28-fixture golden suite all PASS - executed, not reasoned. After v8, every preflight component except the F3-analog gate has executed successfully somewhere; the gate is the sole remaining first-execution step, which is its designed role.

**L2 VERDICT: ACCEPT.** Byte counts, digests, clean parse, and exactly the two disclosed hunks confirmed; the bool() wrap fixes a real numpy-scalar leakage and JSON-serialization defect without changing the precision decision's value; the canonical-numpy replica adequately validates every preflight component before the deliberately excluded F3-analog gate. Digests: v7 654aa1f35b63e83f32e74505b0b2c8be1d84850719f7603e5ff79a87433278d0 (55403 bytes) -> v8 41413911916297e77bf8ec85f33b6415a5fbf2526d97367265610328f51b5817 (55717 bytes). Disposition: place and commit v8; restart from complete preflight, Mike-gated.

## v9 delta repair (2026-07-18; L2-accepted)

v8's first execution (preflight, fifth call) passed every component before the gate live on Mike's machine - digest loop, AST self-audit, verify_frozen, kappa extraction, offset reproduction, and the full 28-fixture golden suite (first live pass) - then the F3-analog gate ran its first genuine simulation and HALTED: F3-ANALOG FAIL seed 42. Engine A was not bit-exact against the committed null-extension record. This is the fidelity finding the gate exists to produce. Diagnosis: line 144 read INIT_ACTIVE = round(0.40 * N_CELLS) (1000 active cells), conflating initial density with the Lambda=0.40 anchor; the committed convention is TARGET_RHO_INIT=0.10 (250 cells) in BOTH committed run scripts (c3_w2_null_extension.py and c3_w2_tcop.py line 52). Because states[0] is stored before the first update, the mismatch necessarily broke bit-exactness at tick 0. Every other element of Engine A's discipline was verified faithful (paired numpy+random seeding, zeros->first-N->shuffle->reshape init, store-before-step, one shared rand_grid per tick for both become and stay, identical propensity formula with kappa and u vanishing on the floor; u_t_for returns 0.0 at tier 0). Fifth wrong-values-under-right-names defect, same survival mechanism.

Delta (two hunks, both disclosed): INIT_ACTIVE = round(0.10 * N_CELLS) with in-place convention comment; docstring bumped. Decisive verification (canonical numpy 2.4.4 replica): committed seed-42 tick-0 active count is 250 (read from data); Engine-A replica with INIT=250 bit-exact across all 400 ticks at ALL FIVE canonical seeds; the COMPLETE preflight replica on v9 - F3-analog gate INCLUDED against the real committed NPZs - passes every component end-to-end. Provenance disclosure: diagnosis read the committed c3_w2_null_extension.py (on no deny list, but not in the harness permitted set) for L1 analysis only; it is not consumed by the harness and the input partition is unchanged; canonical seeds were used solely to reproduce PERMITTED null-extension data via the gate's own designed comparison (the carve-out the canonical-seed fence defines); no held-out file was touched. INIT_ACTIVE's only two consumers (simulate_run, sim_injected) are both substrate-family simulators, so the single-constant fix is correct at both sites.

**L2 VERDICT: ACCEPT.** Byte count, digest, clean parse, and exactly the two disclosed hunks confirmed; L2 independently confirmed the tick-0 store-before-update mechanism as the necessary cause; the corrected constant appropriate at both simulator sites; the diagnosis-only consultation of the non-permitted script does not alter the input partition or calibration evidence. Digests: v8 41413911916297e77bf8ec85f33b6415a5fbf2526d97367265610328f51b5817 (55717 bytes) -> v9 214fb99389c04be1d5526ad32782c7b03d94a3780e048d6ed00dfb1d2dad6b00 (55963 bytes). Disposition: place and commit v9; restart from complete preflight, Mike-gated.

## L2 scope reminder, carried verbatim in intent

Case 1 execution, even if successful, does not complete Stage 2 by itself. The amendment still needs the required fold path and at least one speaking-case calibration unless Mike explicitly records the severity waiver already specified in the mini-contract. Calibration output remains machinery-calibration only: not EE-substrate evidence, not a TCOP re-read, not L4 movement, not prospective governance.

## Disposition

Harness at cycle3/calibration/stage2/stage2_case1_harness.py is v9 (sha256 214fb99389c04be1d5526ad32782c7b03d94a3780e048d6ed00dfb1d2dad6b00), L2-accepted execution-ready. Complete preflight (F3-analog gate included) verified end-to-end in a canonical-numpy replica; the first genuine calibration simulation begins at the geometry stage on Mike's call. Execution NOT authorized by any review; opens on Mike's separate explicit call, stage by stage (preflight -> geometry -> recovery -> controls -> forecast -> instantiation freeze -> Mike-gated unlock and scoring). Source ingestion for this arc ran on the commit-pinned clone channel (zero manual transit; held-out files never copied) - the standing convention for L1 source ingestion.
