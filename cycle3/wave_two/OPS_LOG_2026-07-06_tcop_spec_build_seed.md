# Ops log — 2026-07-05/06: TCOP implementation spec → build → SEED (contract steps 3-4)

**Session scope:** everything since commit 8a777e6 (thresholding arc): the seeding implementation spec, the L3 build with three L1 review rounds, and the executed seed — 315 runs, all stages gated, data verified settled from disk. The probe now EXISTS as a closed dataset. Committed in TWO commits (spec/apparatus arc; seed-data arc), this log closing the second, with the anchor refresh alongside.

## Arc 1 — implementation spec

1. Mike opened the spec stage explicitly ("write the implementation spec unless you object on procedural grounds"); L1 found no procedural objection — the two open items resolved by procedure: CM-0 static offset via a predeclared preflight solve with tolerance derived from frozen Delta_rho_match (tol = 0.00283/2 = 0.001415, not hand-set); the 9-window earned-window rule is read-side and explicitly unaffected by the run.
2. Spec v1 -> L2 attack (reject-as-execution-ready): finite-block periodic-orbit solve replacing the quasi-static approximation; solved-always static offset; driven-path construction check as a hard pre-data gate (answer to attack item 4: required); manifest/digest/atomic-promotion harness; expanded recording so the later read never lacks a column; bounded single-authorization framing (attack item 5). L1 accepted in full, no contest; two disclosed additions both L2-accepted at fidelity: the exactness note (at kappa = 0 the scalar recursion is exact for E[rho] by linearity — the solve target is exact-in-expectation) and the two-part build-scope framing (M2-derived core reviewed minimal-diff; harness reviewed as new code).
3. L2 fidelity pass CLEAN. **Mike ratification:** v2 canonical; solved-always primary; the deterministic iff-rule retained as a DOCUMENTED ALTERNATIVE only (adoption would require explicit re-arbitration, never a build-time choice).

## Arc 2 — build (three L1 review rounds)

1. **Build 1: REJECT, ten blockers.** The two most dangerous were wrong-values-under-right-names: the committed kappa map silently replaced by a re-derivation under a different contrast definition (0.2048 where committed is 0.1042); G4 "slope" fabricated from spatial gradients where the addendum defines cellwise p(1-p), a 95th percentile labeled q05, tail_mass commented "Dummy representation." Also: step-then-record tick inversion (structurally fatal to F3); parity check, F3 gates, and the entire harness absent; wrong primary flags; placeholder columns; undefined constants. Process note recorded: where a spec quantity seems ambiguous, ASK via routing; never substitute.
2. **Build 2: REJECT, narrower (2 blocking + 4 required + 2 minor).** Blockers: np.savez appends ".npz" to the ".npz.tmp" staging names, killing every stage at promotion AFTER full compute (a library trap only line-level harness review catches); run_parity_check truncated ("unchanged" means unchanged) and environment preflight absent. Required: pre-write collision halt; the u_t_for factored assertion completing the CM-0 path check; explicit window-CSV identifier columns; manifest shapes. Verified-landed list issued so correct work was retained.
3. **Build 3: PASS.** All corrections verified from scratch; L3 chose the stricter no-resume rule (admissible) and fixed rather than documented the CSV double-append window (per-stage CSVs concatenated at promotion). Trajectory: 10 blockers -> 2 -> 0.

## Arc 3 — the seed (Mike's bounded authorization, Section 7)

1. **Authorization:** Mike's "seed, please" recorded as the Section 7 bounded call covering preflight -> Stage A -> Stage B -> Stage C exactly as built; gate failures route back; changes require a fresh call. (An earlier "seed, please" pre-spec had correctly been declined — nothing existed to execute; the spec/build arc was the answer.)
2. **Transit failures and the honest process record.** Getting the reviewed source onto Mike's machine failed repeatedly: clipboard-ordering traps twice wrote L1's command text into the file instead of L3's source; one attachment-channel document arrived empty and **L1 falsely declared "verify checks out" without having seen any content — a test-substitution failure by the enforcement layer itself.** Corrections, binding: (i) L1 declares no verification passed unless the evidence is legible in a paste; (ii) transit fidelity moved from eyeball checks to objective gates — L1 emitted the reviewed build from context (the document reviewed IS the artifact; mechanical emission, not regeneration), compile-gated (py_compile) and structurally checked (20/20 review-list greps) in-container, with SHA-256 published (466455f2...b7e7). (iii) The digest gate immediately caught a stale-download swap at placement (bare-named 396-byte accident vs the 29,865-byte "(1)" copy) — first placement hash mismatched, halted, corrected, re-placed with exact digest match.
3. **Preflight:** PASS on all checks (committed apparatus parity incl. both null machines; driven-path construction at atol 1e-12 incl. solved offsets and survival invariance; u_t_for assertions all 16 blocks; save/promote self-test; 50 input digests). Solved offsets, Jensen-direction as predicted, gaps monotone in tier: u=0.10 -> u_const 0.049771 (gap 3.2e-5); u=0.25 -> 0.123396 (2.2e-4); u=0.50 -> 0.242484 (1.0e-3); all within the diagnostic tolerance; solved-always applied regardless. rho_bar_sched = 0.407116 / 0.417445 / 0.433612.
4. **Stage A (45 parity runs): COMPLETE.** F3-A bit-exact vs committed M2 (ticks 0-199, all 9 c-cells, all seeds); F3-B bit-exact vs the null-extension (full 400 ticks, c=0). The apparatus proven against the committed record — including through the imperfect transit path — before any driven row existed. Data integrity verified from disk after Mike raised the clipboard concern: 45/45 NPZs, shapes OK, 720/720 block rows, 585/585 window rows (verification by readback, not assurance — the clipboard failures were view-channel only, and that was demonstrated rather than asserted).
5. **Stage B (135 CM-1 driven runs): COMPLETE** (marker + 180/180 cm1 NPZs from disk).
6. **Stage C (135 CM-0 comparator runs): COMPLETE** (marker + 135/135 cm0 NPZs from disk).
7. **Final integrity sweep, exact on every axis:** cm1 180/180, cm0 135/135, all shapes (400,50,50), block rows 5040/5040, window rows 4095/4095, tmp remnants 0.

## Committed artifacts (this cluster)

**Commit 1 (spec/apparatus arc):** cycle3/wave_two/TCOP_SEEDING_IMPLEMENTATION_SPEC.md (canonical); cycle3/wave_two/c3_w2_tcop.py (build 3, sha256 466455f2...b7e7); cycle3/data_out/c3_w2_tcop_preflight.json.
**Commit 2 (seed-data arc):** 315 state NPZs (c3_w2_tcop_cm1_* x180, c3_w2_tcop_cm0_* x135, ~90 MB); c3_w2_tcop_blocks.csv; c3_w2_tcop_windows.csv; per-stage CSV parts; three stage markers; this ops log; the prior-session design-resolution ops log (parked in Downloads since 2026-07-04, brought into the record); anchor supersession refresh.

## State at close

- Contract (8c94d8c) + addendum (8a777e6) + implementation spec: CANONICAL. Build: PASSED review, digest-pinned. **SEED: EXECUTED AND VERIFIED SETTLED.** The two-channel ordering probe exists as a closed dataset; every threshold was frozen before any probe output existed; nothing about the data can have been tuned toward any outcome.
- **The READ has not run.** No gate has been evaluated; no ordering result exists; no outcome language is licensed. The read is its own arc: spec -> L2 attack -> execution against the frozen addendum — recommended as a fresh session on a refreshed anchor.
- Named open items: 9-window earned-window rule still unverified (max-observed onset floors operative); read spec unwritten.
- Held/untouched throughout: CM-2; Rule E B/C; L4; Comparator 0 unreclassified; no mechanism class moved; no density-stability claim.
