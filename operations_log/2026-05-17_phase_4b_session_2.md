# Operations Log: 2026-05-17 (Afternoon) — Session 2: Pending Decisions Resolution

**Date:** 2026-05-17
**HEAD at session start:** `dd494f1` (Operations log: T2.7 architectural discovery and refactor)
**HEAD at session end:** `b10682a` (Phase 4B Tier 3 first pre-registration: reg_01_scale_interactions)
**Session anchor for resume:** `b10682a` (this log commit will sit on top)

## Session arc

Second session of the day. Morning session closed at `dd494f1` (T2.7 refactor commit `8e3f411` plus its operations log). This afternoon's session opened with rule #6 orientation pointing at the morning's operations log. New Claude (this session) flagged an apparent HEAD discrepancy between current state (`dd494f1`) and the morning log's named session-end anchor (`8e3f411`); on reflection during the same session, this resolved as a misreading rather than a real drift — the log's anchor named the substantive-work commit, and the log commit on top of it had naturally advanced HEAD to `dd494f1` when committed. Prior Claude (morning session, reached via channel-out-and-back) confirmed the framing from its session context and added the reflog suggestion as fallback diagnostic. Verification via `git log --oneline -5` and `git status --short` confirmed clean lineage: `dd494f1` sits directly on `8e3f411` which sits on `df9122e` (yesterday's log) — no intermediate commits, no off-record sessions, working tree state matched morning log's pending items exactly.

After verification, the session worked through the three pending items the morning log named for "next session" in the order the morning log left them: (1) `flights/` rename resolution, (2) canonical output directory convention, (3) first Tier 3 pre-registration. All three were resolved within this session.

## Commits this session

1. **`b10682a`** — Phase 4B Tier 3 first pre-registration: reg_01_scale_interactions (1 file changed, 127 insertions). New `phase_4b/pre_registrations/reg_01_scale_interactions.yaml` per Phase 4B spec v1.1 Section 4.4. IF5 (Scale interactions) family with O2 (logit(p_base)) outcome. Eight required fields, spec-compliant.

Items 1 and 2 did not produce commits: item 1 was resolved by `git restore` (working tree brought into agreement with HEAD, no staged changes); item 2 was a directory rename on disk with the renamed targets still untracked per the deferral discussed below.

## Item 1: `flights/` → `protocols/flights/` rename drift

**Resolution method.** Investigation before destructive action. Verified that the canonical tracked location is `flights/` (git history confirmed: `dcd0d57` and `0522639`); that `protocols/flights/` had zero git history (move occurred outside git); that the three "deleted" files in `git status` had byte-identical copies at `protocols/flights/` (verified via `git ls-tree HEAD` versus `git hash-object` SHA comparison for each of the three files — `flight2_analysis.py` SHA `8e0056dc6dc833c438291f0638c305e01133fdbb`, `flight2_production.py` SHA `31c52da26547cc39ceadc30c4f8a0c13e69d1eba`, `Flight6_Substrate_Specification_v1.1.md.pdf` SHA `85e09f5c58a00d228b378fcc53cbb03de7547eaf`; all matched between git's storage and disk content at the new location). With byte-identical confirmation, restored canonical state by `git restore` for each file (recreating `flights/...` paths on disk from git's stored versions), then removed the drift artifact via `Remove-Item -Recurse protocols\flights\`.

**Critical near-miss.** Before deleting `protocols/`, ran `Get-ChildItem protocols` rather than `Remove-Item -Recurse protocols` blindly. Output revealed `protocols/architectural_reviews/` and `protocols/onboarding/` directories not previously surfaced in the session. Subsequent `git ls-files protocols/` confirmed both directories hold tracked content (five architectural review files, four onboarding primer files — nine files total). Had the cleanup proceeded straight from `Remove-Item protocols\flights\` to `Remove-Item protocols\` without the listing check, nine tracked files would have been deleted from disk and shown as deleted in `git status` requiring `git restore` to recover. The "always investigate before destructive operations" discipline caught it. `protocols/` itself is a legitimate canonical location housing tracked content; it remains intact. Only the `flights/` subtree under it (the drift artifact) was removed.

**Why no commit.** `git restore` brought the working tree into agreement with what git already had; nothing was staged or changed in canonical record. The rename drift existed only in the working tree, and resolution returned the working tree to consistency with HEAD. The session itself is recorded by this operations log; no separate commit was required.

## Item 2: canonical output directory convention

**Empirical investigation.** Three directories with non-canonical naming surfaced in `git status` as untracked: `phase_4b/cross_run_outputs_test/`, `phase_4b/tier2_outputs_test/`, `phase_4b/tier1_reports/` (the last lacking the `_test` suffix despite peer-status). Verification proceeded in three steps:

First, content inventory. Recursive listing of all three revealed the complete Phase 4B output set: 3 files in `cross_run_outputs_test/` (matching the T2.7 + cross-scale outputs from morning session), 63 files in `tier2_outputs_test/` (9 output types × 7 probe/scale combinations, full Tier 2 schema per spec Section 3.1), 8 files in `tier1_reports/` (one report per probe/scale, all PASS per yesterday's session). The outputs are not partial or exploratory — they are the complete spec-compliant Phase 4B output set against the locked A3 baseline.

Second, canonical record consultation. Read Phase 4B specification v1.1 (`phase_4b/phase_4b_specification_v1.1.md`) for any output-path mandate. The spec is silent on output directory naming except for one specific case: pre-registrations live at `phase_4b/pre_registrations/` (Section 4.4). No specification commits any of the three directories in question to a specific name. The `_test` suffix is not from canonical record; it is an implementation choice from execution time.

Third, production code inspection. Examined output-path handling in all five Phase 4B scripts (`tier1_verify.py`, `tier2_analyze.py`, `tier2_postprocess.py`, `cross_run_comparisons.py`, `tier3_regression.py`). Result: an asymmetric pattern. Two scripts (`tier1_verify.py`, `tier3_regression.py`) hardcode canonical output paths with no `_test` suffix (`phase_4b/tier1_reports/`, `phase_4b/tier3_outputs/`). Three scripts (`tier2_analyze.py`, `tier2_postprocess.py`, `cross_run_comparisons.py`) take output directory via CLI `--output-dir` and got `_test` at runtime. The bookend scripts encode canonical convention; the middle scripts were given the `_test` suffix at execution time, presumably to mark first-execution outputs as not-yet-canonical.

**Resolution.** Empirical evidence supports treating the `_test` suffix as misleading: the underlying outputs came from spec-compliant code that passed two-layer review, matching specification exactly. Renamed two directories: `phase_4b/tier2_outputs_test/` → `phase_4b/tier2_outputs/` and `phase_4b/cross_run_outputs_test/` → `phase_4b/cross_run_outputs/`. `tier1_reports/` already conformed to canonical convention.

**Deferred decision (Mike's option B).** A larger question surfaced during this work: the renamed directories remain untracked. So do thirteen Python scripts at repo root (`build_*.py`, `diagnose_*.py`, `distribute_*.py`, `patch_*.py`) and two stdout `.txt` files. The structural question is "How does this repo separate canonical artifacts from working-tree scratch?" — engaging this requires deciding for each set of artifacts whether they should be committed (Approach A: outputs as durable canonical record) or gitignored (Approach B: outputs as derived, regenerable from source).

Mike's call: defer the canonical-vs-scratch decision to a future session, with two safety conditions verified: (1) deferral does not put information at risk (artifacts persist on disk regardless of git tracking; renamed directories remain at canonical paths; operations log captures the deferred state), (2) recoverability is preserved by full documentation in this log. Both conditions met. The deferral is self-documenting: future `git status` will continue to show these items as `??` until decided.

**Specific items deferred to canonical-vs-scratch decision:**
- `phase_4b/tier1_reports/` (8 Tier 1 verification reports, all PASS)
- `phase_4b/tier2_outputs/` (63 Tier 2 diagnostic CSVs, complete schema)
- `phase_4b/cross_run_outputs/` (3 files: cross-scale metrics, F-form comparison, report)
- `phase_4b/diagnostic_stdout.txt`, `phase_4b/t27_diagnostic_stdout.txt` (execution stdout captures)
- 13 Python scripts at repo root, all dated 16-17 May, purpose-classifiable as: patch builders (3), diagnostics (3), distribution helpers (5), other (2)
- 1 text file at repo root: `cross_run_comparisons_df9122e.txt`

## Item 3: first Tier 3 pre-registration

**Framing decision.** Mike noted no paper timeline pressure — the first paper's manuscript stops at MFA which is locked, and Phase 4B work runs ahead of schedule. This reframed the first pre-registration's purpose: not "most architecturally valuable" but "most likely to validate the protocol end-to-end before broader Tier 3 work." The right first pre-registration optimizes for: engaging every required protocol piece, analytical tractability, and a result we already have strong qualitative expectation for (so the regression output is sanity-checkable against expectation; divergence from expectation surfaces protocol bugs rather than substantive findings).

**Selection: IF5 (Scale interactions) + O2 (logit(p_base)).** Per Phase 4B spec Section 4.2, IF5 includes `scale × F_variant`, `scale × rho_global`, `scale × psi_global`. The choice rests on three properties: (1) yesterday's Tier 2 cross-scale verdict already classified F-form distinguishability as `scale_stable` with mean-stable + variance-reducing signature, giving us strong qualitative expectations; (2) IF5 is analytically the simplest of the five families — only two scale levels, interaction interpretation reduces to slope comparison; (3) matched-seed Flight 2 design pairs runs cleanly. O2 (`logit(p_base)`) is the spec's canonical default per Section 4.1.

**Eight required fields drafted, spec-compliant.** Pre-registration drafted with `data_files` (four canonical files: probe1_overcrowding × {20×20, 40×40} for F_2_symmetric, probe2_starvation_FLR × {20×20, 40×40} for F_LR — shadow copies excluded by the same selection logic `tier2_postprocess.py` used in morning session), `outcome` (logit_p_base with η-floor inversion construction per Section 4.1 O2), `model_family` (linear OLS via statsmodels), `predictors` (Section 4.3 primary list minus collinear items), `interactions` (IF5 core, no sensitivity-version extensions), `excluded_variables` (six items with rationale: p_act and p_base for collinearity with outcome, Drive_Raw for tautological equivalence with logit(p_base), PRNG_draw as realization-level noise, Delta_v/u/r for temporal misalignment), `uncertainty_method` (cluster_robust SE clustered by run × tick primary, by cell as sensitivity per Section 4.3), `interpretation_boundary` (5 licenses, 4 does-not-adjudicate items).

**Two extensions removed from YAML, captured here instead.** The first draft included two subfields not in the spec's eight-field list: `expected_signs_and_magnitudes` and `protocol_validation_purpose`. Discussion on whether to keep them in the YAML resolved on the safer position: drop both, keep the YAML spec-compliant, capture the framing in this log entry. Reasoning: the eight fields commit the analysis; the expected signs and protocol-validation framing describe session intent, which is what the operations log is for. The YAML stays clean against the spec's schema. Audit trail is fully preserved by the combination of YAML + this log entry.

**Expected signs and magnitudes (recorded here, not in YAML):**

These are pre-stated expectations against which Tier 3 output will be sanity-checked. They are stated for pre-registration discipline; the analysis is bound to report actual estimates regardless of expectation.

- **F_variant main effect** (F_2_symmetric vs F_LR baseline): expected nonzero based on Flight 2 finding 1 (sharp F-form distinguishability). Sign and magnitude empirical — no prior commitment.
- **scale × F_variant interaction:** expected small or null based on yesterday's Tier 2 cross-scale verdict (`scale_stable`: F-form ratios scale-invariant within 1%). A large interaction would contradict yesterday's verdict and warrants protocol investigation before treating as substantive.
- **scale × rho_global interaction:** expected small. ρ_global itself is largely scale-invariant in Tier 2 output (mean-level signature scale-stable per cross-scale analysis).
- **scale × psi_global interaction:** open. Flight 2 finding 3 (no macroscopic Ψ coherence at A3 + U(0.6, 0.9) + 3000 ticks) suggests psi_global is small in magnitude across scales, which would make any scale × psi_global interaction also small. But this is the most empirically open of the three interaction terms.

**Protocol-validation purpose (recorded here, not in YAML):**

This first pre-registration is structured to validate the Tier 3 protocol end-to-end. The expected signs and magnitudes above are stated for sanity-check purposes — a Tier 3 output diverging substantially from yesterday's robust Tier 2 cross-scale findings is more likely to indicate a protocol bug than a substantive finding, and would trigger protocol investigation before two-field classification. If results match expectations, the protocol validates as designed and the eight-field schema is confirmed as sufficient for Tier 3 work going forward. If results diverge meaningfully, the divergence becomes the next investigation focus rather than a Phase 4B finding.

**File created and committed.** `phase_4b/pre_registrations/reg_01_scale_interactions.yaml` created via PowerShell here-string with `Set-Content -Encoding UTF8`. Content verified intact via `Get-Content` readback. Staged, committed at `b10682a`, pushed to origin/main.

## Methodological observations

**The pager incident.** Mid-session diagnostic work invoked `git diff` commands without `--no-pager`, triggering Windows PowerShell's pager which displayed output paginated with `:` prompts. The user noted "something strange happened" when output appeared to dump one character per line — diagnosis: the temp file extracted via PowerShell's `>` redirect was UTF-16 LE encoded (PowerShell's default for text output), while `fc.exe` was reading it byte-by-byte and displaying UTF-16 bytes as individual characters. The encoding mismatch presented as a content mismatch. Switched to `git hash-object` for SHA comparison, which sidesteps the encoding question entirely (git computes SHA on the canonical blob content, not on the redirected text representation). The shift to SHA-based comparison was the cleanest available path; it also produced exact verification rather than approximate diff-based confirmation. Future analogous work should default to `git hash-object` for file-identity verification on Windows, not `fc.exe` or `git diff --no-index`.

**The protocol holding throughout.** Session involved substantial decision work — empirical investigations, multi-step verification chains, drafting the eight-field pre-registration with substantive analytical content. The one-command-at-a-time, round-trip-through-Mike discipline was maintained throughout, with one notable mid-session correction: Claude sent two diagnostic commands in a single block at one point, and Mike reminded "we use one command at a time to mitigate my mistakes." Correction adopted immediately; remainder of session held the protocol. The catch on `protocols/` (nine tracked files that would have been deleted if the cleanup had proceeded without a listing check) is concrete payoff for the discipline.

**Claude fabrication caught at log review.** Initial draft of this operations log included a claim that the session ran "approximately three hours of continuous decision work." Mike caught the claim: actual session duration was approximately 35 minutes. Claude had no clock reading and produced the duration from nowhere — a fabrication of the same character as the Gemini fabrication-pattern this session's morning log corrected. The correction is recorded here because (a) the pattern is non-obvious from the inside — Claude had no internal signal flagging the claim as ungrounded — and (b) it confirms that the protocol's redundant-detection structure works on Claude as well as on Gemini. Mike's discipline of reading responses with the same suspicion structure Claude operates with caught a Claude fabrication that Claude did not catch in itself. Going forward: Claude should not produce specific duration claims, time elapsed claims, or any quantitative claim about session metadata without explicit ground-truth input from Mike. The same restriction applies to any quantitative claim Claude has no source for.

**No multi-AI routing this session.** Today's afternoon session worked through pending items between Claude and Mike directly. No ChatGPT or Gemini engagement. This is a different operational mode from morning session (Gemini Layer 3 implementer, ChatGPT Layer 2 review, Claude Layer 1 router and Mike commit authority) and from yesterday (full multi-layer protocol). The pending items today were either working-tree hygiene (items 1 and 2) or single-author analytical drafting under Mike's direction (item 3). The protocol's natural mode for this type of work appears to be direct Claude-Mike, with multi-AI routing engaged when implementation work or independent review is needed. The pre-registration itself, when it goes to execution, will likely route to Gemini (Layer 3 implementation) with Claude Layer 1 review on the implementation script.

**The HEAD-discrepancy false alarm.** Worth noting explicitly. New Claude flagged a HEAD discrepancy at session open and asked for diagnostic confirmation before proceeding. The discrepancy resolved on reflection within the same session, before any diagnostic commands ran — the morning log's "session anchor for resume" naturally referenced the substantive-work commit (`8e3f411`) rather than the log commit on top of it (`dd494f1`). A self-referential limitation: a log commit cannot reference its own hash. The flag itself was appropriate caution; the resolution was correct re-reading rather than empirical discovery. Two takeaways: (1) the orientation phase benefited from prior Claude's confirmation via channel-out-and-back even though the resolution was logical rather than empirical — Mike's reading of the situation was confirmed independently; (2) future logs may benefit from explicitly noting "the session anchor below names the substantive-work commit, not this log commit on top of it" to prevent the same misreading.

## Pending decisions for next session

1. **Canonical-vs-scratch decision** (deferred from this session per Mike's option B). Untracked items listed in Item 2 section above. Decision requires settling: (a) commit policy for derived outputs (Tier 1 reports, Tier 2 CSVs, cross-run outputs), (b) disposition of working-tree scratch scripts (commit, gitignore, or relocate to a `scratch/` or `tooling/` subdirectory). Either approach is defensible; the decision is about long-run repo hygiene and the canonical record's separation from working notes.

2. **Tier 3 regression execution against reg_01.** Now that the pre-registration is committed, the next substantive Phase 4B step is implementing `tier3_regression.py` against the YAML. Per Phase 4B spec Section 4.4: `python tier3_regression.py --prereg phase_4b/pre_registrations/reg_01_scale_interactions.yaml`. The existing `tier3_regression.py` (committed at `8e3f411` lineage) writes to `phase_4b/tier3_outputs/`; implementation against the YAML schema requires inspection and likely refactoring. Will likely route to Gemini as Layer 3 implementer per the multi-AI protocol pattern, with Claude Layer 1 reviewing the implementation script before execution and Layer 2 review (ChatGPT) for substantive analytical content.

3. **Second pre-registration sequencing.** With IF5 + O2 committed as protocol shakedown, subsequent pre-registrations will engage substantive architectural questions (IF1 F-form × Λ pathway being the most load-bearing for the EE Theory paper). Order of subsequent pre-registrations not yet decided; depends partly on Tier 3 execution against reg_01 outcome.

4. **Operations log structural question** (minor). This session is the second of the day; the morning's log is `2026-05-17_phase_4b_t27_refactor.md`, and this log will need a filename that distinguishes it. Convention: append `_session_2` or `_afternoon` or use a time suffix. This log uses `_session_2` for clarity; future multi-session days may want a more structured naming convention.

## Resume anchor for next session

When this conversation resumes (this chat or a new one):

1. Verify HEAD: `git rev-parse HEAD` should return the commit hash for this log entry (which will be `b10682a` plus one — the hash created when this log is committed). The log itself names `b10682a` as session-end anchor because that is the last substantive-work commit; this log will be the commit on top.
2. Verify working tree state: `git status --short`. Expect: untracked items as listed in Item 2 above (`tier1_reports/`, `tier2_outputs/`, `cross_run_outputs/`, the diagnostic stdout files, and 13 root-level Python scripts plus 1 text file). The `flights/` rename drift resolved this session should not reappear.
3. Read this log entry: `Get-Content operations_log/2026-05-17_phase_4b_session_2.md`.
4. First architectural decision: canonical-vs-scratch decision (pending item 1 above), OR Tier 3 regression execution against reg_01 (pending item 2). Order to be decided by Mike.

— Mike (drafted with Claude, session 2 end operations log entry, post-pre-registration commit)

