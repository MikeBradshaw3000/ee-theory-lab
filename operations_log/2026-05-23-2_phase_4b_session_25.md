# Phase 4B Operations Log — Session 25

**Date:** 23 May 2026
**Session start HEAD (live read):** `12e3106e3128a8c2873c221bf9a4d3cd003368af` — = session-24 close-out HEAD (the self-referential close verified live this session, not reconciled against a recorded value). Working tree at standing state (`D README.md` + untracked `claude_session_handoffs/`); `origin/main` at parity (`00`), confirming the session-24 push held.
**Session end HEAD:** [to be filled live next session via `git rev-parse HEAD`; left self-referential per the established close pattern].
**Kit operative:** kit-revision-5.1.

**STATUS: Item 12(3) (general schema/load adapter) SETTLED-AND-VERIFIED — `substrate_adapter.py` built by Layer 3, node-reviewed across iterations, Layer-2-accepted, disclosed Layer-1 node-repair applied, Mike-verified on a real two-branch run. Item 12 sub-items (1) directory-naming and (2) which-copy SETTLED. Item 12 narrowed to a single doc-sync residual (the `canonical_artifacts_index.md` §5 amendment, carried per Mike's election); item 12 stays OPEN until §5 lands. Probe3 byte-verification conflict cross-referenced into item 16 component 2. Close-out commits session-25 deliverables to `origin/main`.**

**NEXT INSTANCE — FIRST PRIORITY: open (Mike sequences at instantiation).** Candidate open work: the `canonical_artifacts_index.md` §5 amendment (small; closes item 12 fully); item 16 component 2 (downstream session-16-era claims review — keystone-sized, now carrying the probe3 conflict, its own session); item 17 (current_state full 19–22 reconciliation — needs the session 19 and 21 ops logs as primary source); kit-revision-5.2 (Layer 2 acceptance pass; candidate pile now substantial — see §5). Do NOT re-litigate the session-16 determination or re-run the substrate forensics — both complete (sessions 20–23).

---

## §1 — What this session did

Session 25 instantiated from kit-revision-5.1 to act on the session-24 first-priority: integrate the Gemini item-12(3) adapter iteration-2 return at the node. It carried that adapter from an iteration-2 design return through to a Mike-verified, settled-and-built `load_substrate_file` — via a full Layer-1↔Layer-2↔Layer-3 arc — and settled item 12's two remaining sub-items (directory-naming, which-copy) against a Mike-run substrate inventory. Item 12 narrows to a single doc-sync residual. Two Layer-1 disciplines slipped and are recorded straight in §4; both were caught by the verification steps that exist for exactly that purpose.

## §2 — The arc

1. **Instantiation + state checks.** Kit-5.1 read; session-24 ops log read; cross-cycle context held. HEAD live = `12e3106` (advanced past the session-24 open `61629bd`; self-referential close confirmed). Working tree standing; `origin/main` parity confirmed (push gap stayed closed). STANDING_ITEMS pulled as primary source (via the UTF-8-clipboard idiom after an initial scroll-dump was corrected — see §5). Trigger pull: item 12 active-and-unblocked the lead; item 16 component 2 / item 17 / kit-5.2 eligible; carried-forward trigger-check ≥7 sessions owed.
2. **Item-12(3) iteration-2 node review.** Gemini's iteration-2 return read at the node (role 4). §0 gate clean (stable text-inference mode confirmed). The integrity-critical iteration-1 fix (`expected_shadow_copy` naming-derived, not byte-identity) confirmed correctly carried. Five corrections surfaced against primary source; the canonical "25-column manifest" phrase from the s24 anchor was found to conflate the manifest *artifact* (tracks a column count) with the substrate column *names/casing* (which live in FSS §14.1 / the files) — a schema dump, not the manifest doc, was the right primary source.
3. **Substrate schema dump (Mike-run, primary source).** A read-only pyarrow dump on `flight6_probe1_overcrowding_20x20.parquet` established: 25 row columns, exact mixed-case canonical (`Tick`, `Psi_local`, `Delta_*`, etc.); byte-keyed/byte-valued file metadata (`b'F_variant'=b'F_2_symmetric'`, `b'Scale'=b'20'` NOT `20x20`, plus `PRNG_seed`/`Substrate_version`/`Total_ticks`/`Execution_timestamp`). Closed every iteration-2 pending input.
4. **Five design corrections.** (i) drop casing normalization — mixed-case is canonical; (ii) scale check is a normalization (`20x20`↔`20`), not equality; (iii) bytes-first decode confirmed; (iv) provenance broadcast flagged as an open fork; (v) collision guard made concrete.
5. **Layer 2 acceptance pass on the iteration-3 contract.** Mike-arbitrated full-25 exact-match + Layer-2-round-trip-before-Layer-3. Layer 2 returned accept-with-revisions: provenance broadcast IN with `meta_` prefix + `source_filename`; F.2 probe3 conflict excised from the Gemini ask (kept as a Layer-1/item-16 record-integrity matter); right-to-left parser language; deferred `load_substrate_directory()` design-only. A genuine engagement (split a fork, reframed one, caught the directory loader) — recorded as substantive convergence, not the framing-asymmetry artifact, though it is the third consecutive clean L2 engagement on this adapter and the no-preserved-divergence pattern stays on watch (Rule 6; Mike's external diagnostic).
6. **Iteration-3 contract → Gemini → return.** Self-contained contract (25 columns + metadata inlined; F.1 IN; F.2 excised; right-to-left parser; directory loader deferred) routed to a continuing Gemini instance. Gemini returned a compliant `substrate_adapter.py` (design + runnable `load_substrate_file`; directory loader design-only).
7. **Node review of the return + the telemetry hold.** Gemini's return included a "Live Execution Telemetry" block. Per pessimistic-on-passing + the s16 determination, Layer 1 held it as an unverified claim (it arrived inside the return; Gemini's gate says it does not execute). Three findings: a real `probe_name`-vs-manifest vocabulary collision (silent-join hazard); the telemetry exercised only the False branch; `surrogate_run_id` cross-copy non-uniqueness.
8. **Layer 2 consult on the node-review findings.** Conferred before resolving (framing-asymmetry discipline, since the proposed fix skipped a Gemini round-trip). Layer 2: rename to `probe_base_name` (reserve `probe_name` for the manifest full form); direct-write node-repair is protocol-clean *if disclosed and Mike-verified*; require two-branch verification. Clean agreement.
9. **Disclosed Layer-1 node-repair.** `probe_name`→`probe_base_name` across parser/injection/`expected_shadow_copy`/`INJECTED_COLUMNS`; two docstring warnings added. Recorded as a Rule-4 disclosed boundary-crossing (Layer 1 editing Layer 3 code), Layer-2-endorsed.
10. **Delivery + verification.** The corrected file did not reach disk via pane download (recurring this session); delivered by direct-write here-string (line-count guard). Mike-run two-branch verification: probe1_overcrowding 20×20 → `expected_shadow_copy=False`; probe2_starvation_F2sym 20×20 → `expected_shadow_copy=True`; both 1.2M rows; all seven fail-fast checks passing. The discriminating True branch (which the in-return telemetry never exercised) passed. Item 12(3) settled-and-verified on a real Mike-run result.
11. **`test_adapter_run.py` identified.** A stray root file surfaced in `git status`; identified as the Gemini scratch harness behind the telemetry (real harness, False-branch-only, pre-rename). Resolves the telemetry as not-fabricated while confirming the hold was the correct procedure. Deleted (Gemini scratch, superseded).
12. **Item 12 (1) and (2) settled.** Mike-run tree-wide inventory of `*probe1_overcrowding*.parquet`: four copies per scale, DISTINCT content, three dates. (1) directory-naming SETTLED documented-not-renamed; (2) which-copy SETTLED `flight2_outputs/` canonical (5/16-root + 5/12-Flight_6 non-canonical-by-decision; 5/16-root provenance a noted loose thread). Item 12 narrowed to the §5 doc-sync residual (carried per Mike's election); stays OPEN until §5 lands.
13. **Close-out cluster.** `test_adapter_run.py` deleted; `substrate_adapter.py` staged; `STANDING_ITEMS.md` patched (item 12 trigger+acceptance, item 16 component 2 probe3 cross-ref, four maintenance entries) and diff-verified; this log; handoff folder; commit + push.

## §3 — The deliverables

- `phase_4b/scripts/substrate_adapter.py` — the verified general schema/load adapter (`load_substrate_file`; `load_substrate_directory` deferred design-only). Layer 3 design + Layer 1 disclosed node-repair; Mike-verified two-branch.
- `STANDING_ITEMS.md` — targeted patch: item 12 trigger + acceptance (three sub-items SETTLED + §5 residual; item stays OPEN); item 16 component 2 (probe3 cross-ref + honest-record note); four session-25 maintenance entries. Diff-verified (+13/−3, item 15 untouched).
- `operations_log/2026-05-23-2_phase_4b_session_25.md` — this log.
- **Routing artifacts (carried to layers, not committed):** the iteration-3 Layer 2 routing, the iteration-3 Gemini contract, the node-review Layer 2 routing.

## §4 — Honest Layer-1 record (two slips, both caught by verification)

**(a) Secondary-vs-primary over-statement (probe3).** Before the canonical manifest was read, Layer 1 asserted "only probe3 is not byte-verified" as settled — matching the s24 framing but stated without the manifest in hand. The manifest read then surfaced the session-6 record (probe3 `verified_shadow_copy` at 20×20), contradicting it. Corrected at the node; the conflict is now cross-referenced into item 16 component 2 rather than resolved. Finding-A family (compressed/secondary source spoken before primary). Caught by the manifest read.

**(b) Reconstruction-drop caught by diff (STANDING_ITEMS).** The first STANDING_ITEMS delivery hand-reconstructed the whole file from context and silently DROPPED the entire item-15 entry (retired session 24, but "retired, not deleted" — still in the committed file) plus a two-word stray edit ("by trigger") in a session-14 maintenance entry. The line-count guard (191 lines) matched and told nothing about *which* lines; the `git diff` (−16 net, where additions were expected) caught it. Reverted via `git checkout` (non-destructive, tracked file) and redone as a targeted guarded patch operating on the real committed bytes — item 15 never addressed, so it could not be dropped. The lesson: whole-file reconstruction-from-context is the error surface (working memory drops a block it "knows" is retired); for large tracked files, a guarded targeted patch + git-diff verification is the discipline. Recorded straight — the session that exercised the verification disciplines should not under-represent the slips those disciplines caught.

## §5 — Held / not done

- **`canonical_artifacts_index.md` §5 amendment** — the single remaining item-12 residual; carried per Mike's election (keeps §5's "resolution deferred per item 12" pointer accurate until landed). Item 12 closes when §5 is amended.
- **`load_substrate_directory()`** — deferred design-only per the iteration-3 contract; coding declined at session tail as scope-extension, not part of 12(3)'s deliverable.
- **Item 16 component 2 (downstream-claims review)** — open; keystone-sized; its own future session; now carries the probe3 byte-verification conflict.
- **Item 17 (current_state 19–22 reconciliation)** — open; needs the session 19 and 21 ops logs as primary source (not loaded this session).
- **Kit-revision-5.2** — not done; candidates accrued: (i) the "canonical 25-column manifest" conflation (manifest doc tracks a column *count*; column *names/casing* live in FSS §14.1 / the files — reach for a schema dump, not the manifest doc); (ii) the venv fact (`venv` at project root — parent of repo — with `venv_cycle1_archive` as superseded sibling; surface on the cold-start surface so the next Layer 1 doesn't rediscover it through failed activations); (iii) the UTF-8-clipboard read idiom adopted in practice (`Set-Clipboard -Value [System.IO.File]::ReadAllText(path, UTF8-no-BOM)` for one-paste file reads); (iv) the pane-download-failed → direct-write here-string path re-exercised (download did not reach disk this session); (v) whole-file-reconstruction-vs-guarded-targeted-patch for large tracked files (the §4(b) lesson). Plus the s23/s24 carried candidates. Wants a Layer 2 acceptance pass per the protocol-infrastructure convention.
- **Carried-forward independent-trigger check (6b/6c/7/13/14)** — still owed; now ≥7 sessions; not the binding path this session; disposition still owed (one consolidated pass, or explicit retire).
- **Items 6b/6c/7/13/14** — triggers unmet or ungated; not active.

## §6 — Collateral / process notes

1. **Pane download failed again.** Both `substrate_adapter.py` and `STANDING_ITEMS.md` did not reach Downloads via `present_files`; both delivered by direct-write here-string with line-count + last-line guards. Treated as the documented direct-write-fallback case, not investigated further this session.
2. **Telemetry hold resolved benignly.** The in-return "Live Execution Telemetry" was held as unverified-claim (correct procedure); the discovery of `test_adapter_run.py` (a real Gemini scratch harness producing exactly that output format) resolves it as not-fabricated. The hold was still correct: from inside the node, real-Mike-run could not be distinguished from narration, and the telemetry only exercised the False branch — the two-branch Mike-run verification is what closed 12(3).
3. **Routing experiment continued.** The L1↔L2↔L3 traffic ran heavier than a minimal-centrality session (two L2 consults, one L3 contract, a node-repair-after-L2-consult). The node held; both Layer-1 slips were caught at verification points, not in routing. Consistent with the load-sharing note (consult freely; verify-at-the-node the same).
4. **`probe_base_name` vocabulary distinction now canonical for adapter output.** Adapter emits the base probe token (`overcrowding`/`starvation`/`fusion_residual`); the manifest's full-form `probe_name` is reserved. A future manifest-join consumer should add an explicit separate field, not overload or reconstruct.

## §7 — Resume anchor for next instance

1. Instantiate from kit-revision-5.1; verify HEAD live (self-referential close).
2. Verify working-tree state (standing: `D README.md` + untracked handoffs). Confirm `origin/main` parity (this session pushed; expect local = remote unless new local commits).
3. Read this log. STANDING_ITEMS pull for triggered items.
4. **First substantive work — open; Mike sequences among:** the `canonical_artifacts_index.md` §5 amendment (small; closes item 12 fully); item 16 component 2 (downstream-claims review, keystone, its own session, carries the probe3 conflict); item 17 (current_state reconciliation, needs s19/s21 logs); kit-revision-5.2 (Layer 2 acceptance pass; candidate pile substantial).
5. Do NOT re-litigate the session-16 determination; do NOT re-run the substrate forensics. Both complete.

---

— Drafted by Claude (Layer 1 central node), session 25. Item 12(3) settled-and-verified (`substrate_adapter.py`, Layer 3 design + disclosed Layer 1 node-repair, Mike-verified two-branch); item 12 sub-items (1) and (2) settled; item 12 narrowed to the `canonical_artifacts_index.md` §5 doc-sync residual (carried); probe3 byte-verification conflict cross-referenced into item 16 component 2; session-25 deliverables pushed to `origin/main`. Two Layer-1 slips recorded straight in §4 — a secondary-vs-primary over-statement (probe3) caught by the manifest read, and a whole-file-reconstruction drop of the item-15 entry caught by `git diff` and recovered via a guarded targeted patch.