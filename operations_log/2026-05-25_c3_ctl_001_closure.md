# Cycle 3 — C3-CTL-001 cleared at L2; substrate topology locked

**Date:** 25 May 2026
**Phase:** Cycle 3 — precondition gates (2 of 3 cleared)
**Status:** C3-CTL-001 valid-L2. Substrate topology locked (Candidate 1). Measurement-validity finding converged. Transition rules and probe-design inputs held. Next gate: C3-SS-001. Committed at 1c35283.

---

## Closure summary

This session locked the Cycle 3 substrate topology and cleared the second of three precondition gates, C3-CTL-001, at L2. The synthetic control battery, run on the captured C3-ENV-001 environment, produces the designed five-case discrimination at the locked topology, with both candidate observables grounded against their own grid-local permutation nulls. The gate validates the measurement apparatus only; interpretation is withheld. C3-SS-001 (steady-state eligibility) remains the last precondition before any real-substrate value can be read as Regime II.

## Canonical record produced / referenced

- `cycle3/c3_ctl_001_battery.py` — the control battery (run of record)
- `cycle3/TEST_RECORD_C3-CTL-001.md` — valid-L2; execution and post-execution filled from Mike's canonical run
- `cycle3/CYCLE3_TEST_REGISTRY.md` — C3-CTL-001 row advanced to valid-L2; the stale `.venv` placeholder in the C3-ENV-001 pass-criterion corrected to the top-level `venv`
- `cycle3/data_out/c3_ctl_001_results.csv` — run output
- `cycle3/RESUME_2026-05-26.md` — post-CTL resume anchor (supersedes RESUME_2026-05-25 rev2)
- `cycle3/claude_initial_prompt_post_CTL-001.md` — new-session Layer 1 instantiation prompt
- `cycle3/TEST_RECORD_C3-ENV-001.md` (prior) — C3-ENV-001 passed-L1; canonical environment Python 3.14.4, Mesa 3.5.1, top-level venv

## Substrate topology decision (locked)

Candidate 1 — 50x50, Moore radius 1 (8-neighbor), toroidal — selected as a deliberate canonical baseline. Theory basis: Cycle 3 first needs a no-special-location substrate to validate the measurement apparatus without boundary artifacts; edges (non-toroidal) and extended-reach coupling (larger radius) are catalogued as later topology variants, not the first baseline. The observable conforms to the substrate (8-neighbor toroidal Moran's I, parametric in grid size); because Candidate 1 matches the Cycle 2 observable encoding, no observable rebuild was required. Two alternatives (a bounded Von Neumann grid; an extended-reach radius-2 torus) were declined for the baseline and held as later variants.

## Converged measurement-validity finding (Layer 1 + Layer 2)

Topology and the controls/observable are paired on three dimensions — grid size, adjacency, boundary condition. The observable conforms to the substrate; controls are regenerated per topology and read within-topology only (no cross-topology raw or z magnitude comparison); scale-parametric case definitions are necessary but not sufficient, because a fixed-cell-radius neighborhood makes raw and z magnitudes non-portable across grid sizes; boundary conditions are baked into the observable (toroidal wrap is part of the adjacency). Reached by genuine independent engagement, not soft convergence: Layer 2 independently derived the length-scale / fixed-neighborhood point and the boundary-condition coupling; Layer 1 had raised the z-inflation correction; the divergence on the portability clause was routed back to Layer 2 and converged on its own analysis.

## C3-CTL-001 — design and closure

Opened as a hybrid design phase: Layer 1 stated the settled acceptance criteria, Layer 3 led the scale-parametric implementation at 50x50 toroidal Moore-1, Layer 2 reviewed validity, Layer 1 reviewed the implementation before Mike's run. Five cases: A stable cluster (positive), B random low-rho and C random high-rho (negatives, exact-count), D transient wave (the divergence case), E uniform high persistence (degeneracy). The battery produces, on Mike's canonical run:

- A: high on both observables (MeanI_Z ~914, Persistence_Z ~91).
- B, C: near null on both.
- D: the divergence — MeanI_Z ~834 (per-tick organization high), Psi_persistence_I 0.0 (uniform persistence map, degenerate).
- E: near null on both, with P_std = 0 flagging the uniformity degeneracy; cleanly separated from D on MeanI_Z (~1 vs ~834).

Both observables are grounded against their own grid-local permutation nulls (a per-tick within-tick-shuffle null for Psi_meanI_state; a persistence-map null for Psi_persistence_I).

## What C3-CTL-001 establishes

- The observable apparatus, at 50x50 toroidal Moore-1, computes the intended state-based, density-adjusted quantities and discriminates correctly on synthetic controls, with both observables null-grounded. The apparatus is trustworthy for measurement at this topology.

## What C3-CTL-001 does NOT establish

- Nothing about real-substrate values, steady-state eligibility, or Regime-II-as-structural.
- It does not settle which observable IS Regime-II coherence; Case D's divergence is recorded as data on the co-equal pair, not a tiebreaker.
- It is not a substantive probe; it is machinery validation.

## Held (uncommitted design input)

- Transition-rule menu: A is the both-agree baseline (pre-loads neither — relaxes to fixed points / uniform states with no sustained motion); B pre-loads Psi_meanI_state (range-bound / Life-like shifting structures); C is a micro contagion rule with tunable divergence. Rules commit only in the design phase, reconciled with Layer 2's discrimination analysis and the substrate together. The mapping of rules to observable consequences was clarified this session as L1/L2 design reasoning, not L4.
- Probe-phase design inputs (Layer 2): the two-dimensional coherence signature (z_meanI_state, z_persistence_I), four quadrant readings with forbidden interpretations, the mu(rho) distinction, and the Lambda-varying probe sequence (the probe must vary Lambda and look for the sequence Lambda -> rho lifts -> coherence stays low over an earned steady-state window -> only later does coherence lift; that sequence is what bears on the "baked in" objection). Captured this session; see follow-ups — the capture file was not yet placed/committed.

## The ontological question (rides forward, unsettled)

Which observable, if either, IS Regime-II coherence — Psi_meanI_state vs Psi_persistence_I. Co-equal pair, L4 (Mike / Layer 1), settled on relational-mutual-reinforcement grounds, never by statistic robustness, script naming, or substrate mechanics.

## Discipline notes (honest record — what the structure caught and what it missed)

- The Layer 2 validity pass on the CTL draft was routed cold (no Layer 1 catch attached). It independently caught Case E (a 35-column moving band reading high per-tick, failing the near-zero criterion and collapsing into a wide twin of Case D) and also caught a B/C Bernoulli-vs-exact-count looseness that Layer 1 had reasoned past. The cold-route discipline produced both an independent catch and a catch of the framer's own miss.
- Layer 1 sufficiency-tested the implementation (sandbox review run) and found a missing per-tick permutation null for Psi_meanI_state — the refactor had grounded only the persistence observable. The addition restored parity with the Cycle 2 v2 apparatus and satisfied the converged "thresholds from this grid's own null" rule.
- Miscalibrations recorded: (1) Layer 1 initially over-read a Layer 3 request for the ENV-001 environment values as an instance-state mismatch, when it was a normal request from a layer with no repo access; corrected within the same exchange. (2) Layer 1 once stalled with a "draft or hold?" question instead of producing the obvious next artifact; flagged by Mike. (3) A multi-statement PowerShell block failed when pasted (variable assignments did not carry into the if-block); the file-edit workflow was corrected to finished-file delivery via create -> present_files -> Move-Item, with edits applied and diff-verified in Layer 1's sandbox.

## Process notes

- Execution channel was Mike only throughout. Layer 1 sandbox runs were treated as predictions; the run of record is Mike's on the captured venv. numpy is pinned at 2.4.4, so the canonical run reproduced the sandbox prediction exactly — treated as a cross-check, not a pre-confirmation.
- Commit 1c35283 staged with explicit paths; the 19 untracked Population B copies and the stray reproduction-guide file were deliberately excluded.

## Follow-ups (named, not blocking)

- Probe-design-inputs capture (`PROBE_DESIGN_INPUTS_HELD.md`) and the six session routing notes were drafted but not placed on disk or committed this session. Optional supplementary trail; the substantive content is captured in the registry, the test record, the resume anchor, and this entry.
- `MANIFEST.md` tracks no cycle directory — neither `cycle2/` nor `cycle3/`. A find-files gap; a deliberate addition of both is the proper fix, deferred.
- Stray `phase_4b/reviews/layer3/# Mesa Environment Reproduction Guide.md` — disposition pending.
- Date note: the resume anchor was filed as `RESUME_2026-05-26.md` while the machine event date is 25 May 2026; functionally harmless (it still sorts newest), but the filename date runs a day ahead of the event.

---

*Drafting partner: Claude (Layer 1), with Mike.*
