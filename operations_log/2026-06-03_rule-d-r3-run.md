# Operations log — 2026-06-03 Rule D R3 run executed

**Session:** Cycle 3 wave two (continuation of the R3 amendment session). The Layer 3 R3 implementation (c3_w2_rule_d_r3.py, committed 6857958) was executed. Parity pre-flight cleared; the 70-setting run completed and wrote cycle3/data_out/c3_w2_rule_d_r3_results.csv (350 rows). A theta_turnover = 0 integrity check confirmed the R3 fix holds (recovers the M2 Rule C signed regime, does not saturate). The full bracket read is DEFERRED to a fresh session (cold-start economy; the read is the consequential analysis).
**Layer:** Layer 1 (Claude) run setup, integrity check, commit; Layer 3 (Gemini) the implementation that ran; Mike sole execution channel (launched the run) and arbiter. Layer 2 not yet engaged on the run output (reads AFTER the run, against its own mean-field prediction — not before).
**Opened from:** HEAD 1443ac0 (R3 amendment ops log).
**Closed at:** HEAD (this ops log commit), origin current after push.
**Outcome:** Rule D R3 behavioral map produced and committed (c1ff920). Run sound; theta = 0 signed anchor confirmed. Next step: read the map against the Section 4 matched-coupling bracket.

---

## Sequence

Venv confirmed active at the canonical path (not the cycle1 archive). Layer 3's R3 script was delivered as L3's verbatim bytes (pasted by Mike, written to file by Layer 1 without modification, to preserve L3 provenance after a direct Gemini-to-disk save failed), syntax-checked (parses, 17183 bytes), placed at cycle3/wave_two/rule_d/c3_w2_rule_d_r3.py, and committed pre-run (6857958) so the run is against committed code. Mike launched the run with stdout/stderr teed. The script is silent on success; empty output plus the returned prompt signaled clean completion. Layer 1 verified the artifact rather than trusting the silence.

## Run result (what the run produced)

- Parity pre-flight CLEARED. The open watch from the R3 re-route — whether the parity check's 2-tuple unpacking of ctl.compute_persistence_null / ctl.compute_meanI_state_null matched the control battery's actual return signature — resolved in favor: the battery returns 2-tuples, the assertions passed, the run proceeded. No L3 fix was needed.
- 350 rows = 70 settings (7 theta_turnover x 2 kappa x 5 seeds) x 5 windows (window_start 0/25/50/75/100). Header carries the R3 columns: s_Lambda, theta_turnover, effective_persistence, plus the two-guard diagnostics (q_mean, q_var, mean_abs_perturbation, realized_churn_rate) and the full observable/flag set. CSV 60027 bytes.

## theta = 0 integrity check (the R3 fix, confirmed — NOT the bracket read)

A single targeted read at theta_turnover = 0, window_start 100, both signs, all five seeds, confirmed the R3 amendment did what it was for:
- mean_rho 0.417-0.420 (c = -0.35) and 0.371-0.373 (c = +0.35) — matching the M2 Rule C regime at this anchor almost exactly; the distributional reproduction is essentially identical.
- saturation_degenerate False on all ten rows. theta = 0 does NOT saturate — the old survival-replacement failure mode is absent.
- effective_persistence = 0.40 = s_Lambda * (1 - 0) exactly.
- Psi_persistence_I_z signed correctly: ~ -4.3 to -5.2 at c = -0.35, ~ +4.6 to +6.6 at c = +0.35. Both signs cleanly signed at the lower anchor.

So theta = 0 is the genuine signed Rule C reference on both signs; the matched-coupling bracket has a valid signed lower end. This is the precondition for readability, met. It is NOT the bracket read — what intermediate theta_turnover produces (near-null while lifted/non-degenerate? signed-through-to-degeneration?) and whether the two guards (4.3 density-confound, 4.4 live-coupling exposure) are satisfied at any near-null is the consequential analysis, deferred.

## State at close

- HEAD at this ops-log commit, origin current after push, tree otherwise clean (untracked read_obs001_nearnull_scale.py at root unchanged; no run log committed — Tee wrote no file on the silent clean run).
- Rule D R3 behavioral map committed at cycle3/data_out/c3_w2_rule_d_r3_results.csv (c1ff920). Run sound; parity cleared; theta = 0 signed anchor confirmed on both signs.
- NEXT STEP: read the map against the Section 4 matched-coupling bracket (per-(setting, seed) flag counts, never per-band means; both guards applied to any near-null; theta = 0 reproduces M2 distributionally, not bit-identically). Then route to Layer 2 for its reading against its own mean-field prediction (the run TESTS the prediction; do not converge by deferring to L2's clean argument). Then a findings file, then seeding is Mike's call. The next-session instantiation prompt carries this.
- Rule C first pass still rests on A-prime; Comparator 0 / epsilon floor findings unchanged; weak-form Rule B prior untouched; L4 ontological question rides forward unsettled.

Drafting partner: Layer 1 (Claude).
