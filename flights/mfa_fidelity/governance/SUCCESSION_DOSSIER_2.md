# Succession Dossier 2 — MFA-Fidelity Flight, Second L1 Succession
**Author:** outgoing L1 (successor conversation to the founding L1). **Date:** 2026-08-24. **Repo-bound:** commit under `flights/mfa_fidelity/governance/` at next session.

## 1. State of record at succession
- **HEAD (pushed): `0fe473a`** — substrate instrument Phase 1, **L2 SOURCE-REVIEW REGISTER CLOSED**. 16 files (mfa_instrument package + 5 test modules + .gitattributes), 70/70 suite, LF pinned (`i/lf` verified pre-commit; two empty `__init__.py` are `i/none`, correct).
- **Frozen and governing:** Merge Specification v0.4 FROZEN (`c5cf7ac`); Contract E1 v0.8 (all L2 textual blockers cleared; advancing to stage-1 freeze qualification); threshold-object ruling; MFP v1.1; D2/D5/Brief-3 rulings — all as recorded in prior placements and this dossier's companion files.
- **Gate A status:** PROVISIONAL only — bit-exact (raw-storage comparator) vs. ancestor `flight2_production.py` at pinned `4d9a622`, all three ancestor F-forms, in a NON-CONFORMING env (py3.12.3/numpy2.4.4). **Authoritative regression** awaits: frozen env (py3.14.x/numpy2.4.4), the complete integrated stage-1 commit, and the **cross-confirmed** ancestor digest.
- **Ancestor digest (L1-established, cross-confirmation PENDING on Mike's clone):** `4f825bbe956a2b225e0c843876189c65a84af1fd74f7325ec94657747b9dbea3`. Verify at next session: `git checkout 4d9a622` → sha256 of `flights/cycle2_round1/02_flight_1_v1_1_parity/flight2_production.py`. AUTHORITATIVE Gate A verifies this fixed identity; it never establishes identity from its own candidate file.

## 2. Remaining commit queue (session 1 of successor)
Per COMMIT_SESSION_MANIFEST.md (in bundle): **Commit 2** build chain (build plans v0.1/v0.2, five L2 packets, received review records, ops log for the code arc) → `flights/mfa_fidelity/build/`. **Commit 3** E1 lineage (telling + v0.1–v0.8 + review records) → `flights/mfa_fidelity/contracts/e1/`. **Commit 4** governance (Mike's orientation note; this dossier) → `flights/mfa_fidelity/governance/`. All files are in `mfa_succession_bundle.zip` on Mike's machine. Digest-gate placements; explicit-path `git add` only; `.gitignore` already covers `__pycache__` — do NOT append (it's the full Python template; note: **`build/` is globally ignored** — never place artifacts under a path containing a `build/` segment).

## 3. Phase 2 scope (next build work, on the corrected boundaries)
1. **Gate B harness:** B1 deterministic rule-equivalence vs. Lineage B's own `run_parity_check` pattern — READ THE SOURCE at `4d9a622` (`cycle3/wave_two/c3_w2_tcop.py`) before writing; B2 declared-ensemble distributional comparison. Facts already source-read: p_become = sigmoid(LOGIT_L+u_t+κ·g_q); survival = rand < CONSTANT p_survive ("bare p_Λ", LAMBDA=0.40); ONE shared rand_grid; LOGIT_L=log(Λ/(1−Λ)); B's own 8-term neighbor-count expression (transcribed in dynamics.py).
2. **Observables rebuild** (B window/block machinery on Generator draws; measurement-side only; never feeds Q).
3. **N3 permanent branch choice** (Mike's ruling): implement the common-Q skeleton for become_survive, or freeze the current mechanical rejection as the supported subset. Currently: construction raises on nonzero-Q become_survive.
4. **TelemetryWriter B-field handling** (currently symmetric-chain family only; B sink fields unconsumable — resolve at Phase-2 integration, flagged by L2).
Then Phases 3–5 per SUBSTRATE_BUILD_PLAN_v0_2 §2 (closure/R0 → nulls/classifier → dense-grid → audits/ensembles → qualification-locked sweep → resource benchmarks → FINAL AUTHORITATIVE A/B/R0 on the integrated commit → stage-1 package → Mike's freeze word → tranche → production).

## 4. Ledgers
- **Later-hardening (recorded, not actioned):** verifier independence — reconstruct pre-update activity, Local_Density, ds, Psi_local, rho_global from primitive activity telemetry rather than trusting persisted derived columns.
- **Environment:** authoritative pins py3.14.x/numpy 2.4.4 (spec §7.4; no Python pin in lock-file — 3.14 rests on executable hard-fail, unverified claim withdrawn); `MFA_PINNED_REPO_ROOT` is the integration input for pinned-ancestor tests (3 tests skip when unset — 67 passed, 3 skipped is the expected no-clone result; Mike's machine reproduced exactly this).
- **Channel facts (hard-won):** L2 returns arrive intact ONLY as file uploads — pasted-text conversion delivers EMPTY documents (twice reproduced); long console output relays via write-to-file + Notepad + file upload; `git push` prints to stderr (read inline, never pipe); semicolon-chained PowerShell usually works but paste can fragment — when output goes missing, go atomic one-command-per-block and verify state before proceeding (this session: a fragmented paste caused `git rm --cached` to land while the re-add did not — caught by staged-count check before commit).
- **Ops-log items this arc (commit with the log):** truncated first Phase-1 packet (digest f9e48749 VOID; ecc47657 valid); manifest file-count error (claimed 18, true 15); patch-ordering staled-pattern fix; checker-capitalization false negative; near-commit of empty instrument tree averted by staged-count verification.

## 5. Standing rules that bind the successor unchanged
Purpose canonical (phenomenology outranks formalism; the flight asks whether Ψ exists and Regime II is real). Never-relax-after-output. Candidates produce/fail-to-produce. Pessimistic-on-passing — including on our own harnesses (a verifier that cannot fail is not a verifier; discriminating negatives required). Source-read before implementation (no complexity floor). Vocabulary quarantine per standing list. Mike: sole execution channel, sole arbiter; thumb economy binding. L2: incremental source-level review continues into Phase 2 (per-phase packets, self-contained, file-upload routed). L3: closed-register reserve only; blind-rebuild executor later. Phil wall up.
