# Ruling Record — D2: Q's Activation Read

**Ruled by:** Mike, 2026-08-18, successor L1 conversation.
**Ruling:** **Option (c) — both reads implemented, separately switchable — with four binding attachments:**

1. **Local read is PRIMARY.** P4's headline verdict attaches to the local-read mode (Q reads the Moore-neighborhood active fraction). Primary designation is verdict-attachment grammar fixed before any data, not a truth claim about the stain's nature.
2. **Global read is a COMMITTED COMPARATOR** — the mimicry/control realization per L2's revised characterization: the projected MFA equation instantiated directly at substrate level. Its results are contracted, emitted, and interpretable as committed secondary findings; nothing about the mode is discarded or informal.
3. **A PRE-SEEDED STAIN DISCRIMINATOR** enters E4's contract: the spatial-texture comparison between contact-staining (patchy imprint tracking where activity sat) and atmospheric staining (uniform imprint at matched mean) receives its own pre-declared observable and verdict rule under the standard evaluability audit, so the local-vs-global question is adjudicated by the instrument rather than glossed after the fact.
4. **An EXPLICIT EVALUABILITY FORECAST for E4** is required before its contract freezes: the doubled gate surface (two Q-modes plus the discriminator) is forecast against the Stage 2/TCOP compound-conservatism precedent and accepted eyes-open, or the design is thinned before seeding — never after.

## Rationale (as ruled)

Mike's governing consideration: whether the ground's memory is contact-local or atmospheric cannot presently be known, and ruling (a) or (b) as a standalone commitment would amount to pre-declaring a result — asserting a fact not in evidence. Option (c) converts the unknowable into an experimental axis, which is this program's standing method for unknowables. The primary designation resolves the apparent tension between (c) and outcome hygiene: with two modes producing two E4 results, the headline claim's attachment must be fixed pre-seed or post-hoc promotion of the cleaner story becomes available; attaching it to the local read follows from ratified-pending MFP (the stream-level realization is the riskier, more informative recovery test — L2's revised input) and anchors the mandatory parity bridge, without asserting local correctness.

## Standing inputs at ruling time

- **MFP** (registration pending ratification with L2's required edits): removed the level-crossing fidelity inference that had favored the global read; both reads stand as distinct theories of the stain.
- **L2 revised D2 input** (post-MFP review, on record): favors local as primary conditional on a pre-registered, load-bearing MFA-parity/recovery bridge; global coherent as comparator. L2's asymmetry accepted as binding: a local substrate failing to recover MFA behavior under the declared comparison is evidence against the stream-level realization — MFP may not be invoked post hoc as a reason agreement was never expected.
- **L2 original adversarial input** (in the brief): the causal-timing construction (pre-update ρ_global → post-update Q → t+1) and decomposed telemetry (separated Q-from-Ψ and Q-from-ρ terms per base) are the implementation standard for both modes.
- **Substrate facts (v1.1, S2):** local read recomputable from existing columns; global read requires one persisted per-tick scalar plus the decomposition columns; costs symmetric to first order, non-deciding.

## Consequences for drafting

- Merge specification: switchable Q-mode under a naming-ledger entry (Q_read ∈ {local, global}; wrong-values-under-right-names surface explicitly guarded); both modes carry L2's telemetry decomposition; global mode adds the per-tick rho_global persistence per S2(c).
- E4 contract: mode-resolved claim ladder (P4 headline on local; comparator findings committed secondary); stain discriminator with pre-declared observable and verdict rule; evaluability forecast gate before freeze.
- Parity bridge: pre-seeded projection-level targets per L2's requirement, entering the merge spec's parity section alongside the Brief 3-dependent ancestor gates.

**D5 and Brief 3 remain open. Brief 3 is next per the agreed order.**

*End of ruling record.*
