# Ruling Record — E1 Threshold Object
**Ruled by:** Mike Bradshaw, Theory Architect, 2026-08-21 (of record)
**Recorded by:** L1 (third succession), drafted from the verbatim carried text at the head of Contract E1 v0.2 (restated v0.3); ratified by Mike at the commit session placing this record.
**Jurisdiction:** Contract E1; binding on the E1 lineage v0.2 forward. The Merge Specification v0.4 FROZEN is unaffected.

## Ruling (verbatim, as carried at the head of E1 v0.2)

E1's object is the **fixed-terrain activation threshold**: Γ_Ψ = Γ_ρ = 0, no base-update arithmetic executed, bases fixed within-run; the sweep varies the frozen initial base-distribution between runs; R1 compares against a fixed-Λ mean-field reference with an independently specified projection map. The **coupled-system ignition boundary** is registered as a **named future object**, sequenced after E4 has exercised and characterized the Q channel (or as a named successor contract with declared Γ provenance); its preconditions are recorded — motivated Q coefficients and a located Λ* to measure departures against. It is scheduled by its dependencies, not suppressed.

## Consequences carried in the contract lineage

1. The instrument's E1 conformance preflight (bases bit-identical within-run under total-Q-disable) implements the ruling's Γ_Ψ = Γ_ρ = 0 condition mechanically; a violated freeze is a spec violation, not a data point.
2. The formal object is null-relative from v0.3 forward: the bracket [m₋, m₊] separates null-consistent from null-exceeding persistence, never "activity exists" from "activity does not exist" (η_floor > 0 at every finite parameter).
3. The ignition boundary's future registration travels with E1's every result: nothing E1 locates is the coupled system's boundary, and no E1 verdict may be quoted as if it were.

## Provenance note

The ruling was given in the predecessor L1 conversation and carried verbatim into the contract heads rather than placed as a standalone record at the time; this record repairs that gap. The v0.2 head text above is the authoritative wording; where any paraphrase elsewhere diverges from it, the v0.2 text governs.
