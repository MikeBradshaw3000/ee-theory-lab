# Gate A — AUTHORITATIVE Regression Record
**Gate:** A (Lineage A fidelity: `symmetric_chain`, the merged instrument against ancestor `flight2_production.py`). **Standing before this record:** PROVISIONAL only (Succession Dossier 2 — bit-exact in a non-conforming environment). **Executed:** 2026-09-16, canonical machine (Windows; Python 3.14.4; NumPy 2.4.4; frozen venv, twenty pins conforming). **Harness:** `mfa_instrument/gates/gate_a.py` (Phase-1, L2-CLOSED). **Candidate:** `dynamics.py` as placed at `948a517` (sha256 `483b8a37…`), unchanged since. **Reports committed at `cec3e8d`.** **Recorded by:** L1. **Status:** RATIFIED by Mike, 2026-09-16; routed to L2 for acceptance.

## 1. Result

**Gate A PASSES under AUTHORITATIVE conditions**, all three ancestor F-forms, at two configurations: the reviewed configuration (12×12, 25 ticks) and E1's production shape (50×50, 3,000 ticks). Each run compares the candidate against the pinned ancestor class itself at matched seed, full state and full telemetry rows, bitwise; the fixed ancestor identity is verified against the frozen expectation `4f825bbe956a2b225e0c843876189c65a84af1fd74f7325ec94657747b9dbea3` (cross-confirmed on the canonical machine), never established from the candidate.

## 2. Run of record (committed identities; the working-tree bytes are identical — reports written with explicit LF newlines)

| form | grid | ticks | committed sha256 | preflight | state | telemetry | gate | elapsed |
|---|---|---|---|---|---|---|---|---|
| F_2_symmetric | 12 | 25 | `c3395536b3936840…` | PASS | bit-exact | bit-exact | PASS | <1 s |
| F_LR | 12 | 25 | `1317029db59c6047…` | PASS | bit-exact | bit-exact | PASS | <1 s |
| F_baseline | 12 | 25 | `c369fd363927ecdf…` | PASS | bit-exact | bit-exact | PASS | <1 s |
| F_2_symmetric | 50 | 3000 | `b68e206894c79257…` | PASS | bit-exact | bit-exact | PASS | 255 s |
| F_LR | 50 | 3000 | `0547140268588ab9…` | PASS | bit-exact | bit-exact | PASS | 274 s |
| F_baseline | 50 | 3000 | `6fa2afc390289f69…` | PASS | bit-exact | bit-exact | PASS | 329 s |

Environment recorded in every report: `python3.14.4/numpy2.4.4`; label `AUTHORITATIVE`; ancestor `4f825bbe956a…`.

## 3. What this certifies

The merged instrument's `symmetric_chain` path reproduces ancestor `flight2_production.py` at commit `4d9a622` exactly — state and telemetry bit-identical under the Phase-1 raw-storage comparator — for all three ancestor F-forms, in the frozen environment, through E1's full run length. Gate A's register standing is now **AUTHORITATIVE**.

## 4. What this resolves

The closure ledger's open question — whether the authorized `dynamics.py` noise guard placed at `948a517` (a refusal in the `become_survive` constructor path) reopened Gate A's standing — is resolved by measurement: the current `dynamics.py` passes Gate A AUTHORITATIVE bit-exact. The A chain is untouched. No ruling by argument is required.

## 5. What this does not certify

Nothing about Gate B (separately certified and closed), Gate R (not yet built), observables validity, or any E-rung phenomenology. Gate A is one of the three preconditions E1 §8 names before seeding; with Gate B closed, R0 is the remaining gate.

## 6. Ratification

Ratified by Mike's word of 2026-09-16. This record commits beside the reports under `flights/mfa_fidelity/gates/gate_a/` and routes to L2 for acceptance.
