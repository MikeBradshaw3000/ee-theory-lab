Stage 1 root-level orientation: ORIENTATION, CURRENT_STATE, MANIFEST (Layer 2 sanity scan incorporated)

Add three root-level orientation documents, third commit cluster of
Stage 1 of the repository restructure. Follows the foundational document
set (pairs 1-3 committed at 79db966, 6603799, a8bc52c). These documents
are derivative material surfacing the now-canonical foundational set at
higher-level orientation; they introduce no new architectural commitments.

Documents committed
-------------------

ORIENTATION.md
  Repository entry point. Reading guidance by reader type (fresh Claude
  starts with the kit; protocol readers go to protocols/foundational/
  README.md; current-state readers go to root CURRENT_STATE.md;
  directory-layout readers go to MANIFEST.md). High-level pointer to
  what the repository contains and does not contain. Authority
  hierarchy from primary source down through specifications,
  foundational set, operations logs, instantiation kit, and this
  document.

CURRENT_STATE.md (root level)
  Project-level current state. Explicitly complementary to
  protocols/foundational/current_state.md (which covers Phase 4B and
  protocol scoped material). This document covers project arc,
  theoretical-architecture state, manuscript state, Phase 4B
  substantive state (with cross-pointers to the foundational document
  rather than duplication), cross-workstream coordination, and cross-AI
  collaboration state.

MANIFEST.md
  Top-level directory listing. What each top-level directory contains;
  what is canonical and what is pending Stage 2-4 of the restructure.
  Sister to canonical_artifacts_index.md but at directory-tree level
  rather than artifact level.

Routing record
--------------

Routed to Layer 2 (ChatGPT) for sanity scan on 2026-05-20 (lighter
routing than full cycle, appropriate for derivative orientation material
per Mike's arbitration of routing-shape distribution). Layer 2 return
surfaced two patches and no hard quarantine violations, no cross-pointer
failures:

  patch 1: ORIENTATION.md authority hierarchy — operations logs
           reordered above instantiation kit; for historical session
           facts, operations logs outrank the kit (the kit is a
           compressed surface; operations logs are primary historical
           record)
  patch 2: CURRENT_STATE.md Section 1 — "Q-mediated terrain reshaping"
           replaced with "Q-mediated structural-base updating" to avoid
           drift toward the quarantined "terrain favorability" phrase
           and to match canonical vocabulary (bases-acted-on-by-Q)

Layer 1 incorporated both patches directly per the sanity-scan
convention (no v2-acceptance pass for derivative material). MANIFEST.md
had no findings.

Stage 1 remaining
-----------------

  - STANDING_ITEMS.md: no Layer 2 routing (operational process artifact)
  - Kit-revision-3: likely Layer 2 sanity scan
  - Session 9 operations log
  - Session 10 handoff folder

Layer 1 boundary-crossings registered
-------------------------------------

Direct drafting of all three documents, the routing package, and this
commit message; all disclosed in-session per Rule 4.
