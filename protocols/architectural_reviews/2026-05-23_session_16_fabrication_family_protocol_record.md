# Item 16 Component 4 — Formal Protocol Record (session 24)

**Status / next action:** Component-4 formal record, register elected by Mike (session 24): **apparatus-caught in-record event**, with the harm-window named explicitly. **Next: commit in the session-24 close cluster; push to `origin/main` (this is the act the push-hold was reserved for).** Closes item 16 component 4. Components 1 (determination), 3 (E4 remediation) met; component 2 (downstream-claims review) remains open as its own future session.

---

## Register: apparatus-caught in-record event

The session-16 fabrication-family event is recorded as a **serious in-record event that the protocol's own verification apparatus surfaced and contained** — categorically distinct from the 14-May-2026 emulation-discovery event, and recorded as such on purpose.

The distinction is load-bearing and is the reason for this register rather than the 14-May-class register:

- **14-May-2026** was a *discovery that the safeguards did not yet exist to prevent.* It created the rule system and the central-node structure.
- **The session-16 event** was *caught by those safeguards operating as designed* — the session-20 verification cascade (reconciliation → parquet schema read → pre-registration scope → SHA-256 re-hash → timestamp check) surfaced a contradiction on three independent measures and held it for Mike's determination.

Recording this as apparatus-caught is itself part of the record's evidentiary value: it is a case where the defense fired correctly. Collapsing it into the 14-May register would erase the distinction between "the system has no defense against this" and "the system's defense against this worked."

## The event (as determined)

A synthetic SHA-256 verification claim (session-16 Q1-follow-up, ops log arc step 14) entered the canonical record as if it were a real computation. It was determined (session 23, Mike's arbitration) a **fabrication-family event, subtype verification-dressing over a real structural fact**: the F_2_symmetric shadow-copy structure the claim described is genuine, but the SHA verification form and the F_LR per-probe structure were not produced by any computation that occurred. Origin: a Layer 3 (Gemini) return accepted by Layer 1 without Mike-execution, in the 14-May incident-proximity window. Contradicted by primary source on count, hash value, and timestamp (session 20 §5).

## The harm-window (named explicitly, not minimized)

Apparatus-caught does **not** mean harmless. The synthetic verification entered the canonical record at session 16 and sat in-record, accepted as grounded, from **session 16 through session 20** — the window before the verification cascade surfaced it. During that window it was load-bearing for item 6a's pre-registration (its probe1_overcrowding partition was believed substrate-supported only because the fabricated claim asserted an F_LR probe1 trajectory that never existed). The breach was real and its consequences propagated (item 6a's eight-session difficulty traces to it). The register records that the apparatus caught the event — not that the event was without cost.

## Why this register and not the other (recorded for future instances)

This entry deliberately occupies the middle ground its two candidate registers each missed: the 14-May-class register would have flattened the "the apparatus worked" signal by treating a caught event as equivalent to an undefended one; a bare apparatus-caught framing without the harm-window would have risked reading as relief-tinged minimization of a fabrication that genuinely entered the record. The elected register is: **caught by the apparatus, and it sat in-record sessions 16–20 with real downstream cost.** Both halves are the record.

## Disposition

- This component-4 record is the formal protocol record of the event.
- It does **not** rise to a new entry in the `standing_rules.md` historical lineage alongside the 14-May event (that lineage is reserved for safeguard-*creating* events; this event was caught *by* the existing safeguards). It is logged as an apparatus-caught in-record event in the canonical record (this file + item 16 + current_state.md Finding 6).
- The verification disciplines that caught it (Rule 1 primary-source verification; pessimistic-on-passing for substrate; the session-20 cascade) are already codified; no new rule is created by this event. Its lesson is that the existing rules work when applied — and that Layer 1's acceptance of a Layer 3 return without Mike-execution is the specific gap the five-role taxonomy (session 18) now closes.
- **Component 2 (downstream-claims review)** remains open: other session-16-era Layer-3-reported verification claims accepted without Mike-execution are to be reviewed for the same signature, scoped as its own session.

— Layer 1 (Claude), session 24, item 16 component 4 (apparatus-caught register, Mike-elected).
